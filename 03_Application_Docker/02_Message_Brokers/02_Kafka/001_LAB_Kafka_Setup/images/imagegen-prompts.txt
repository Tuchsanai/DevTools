IMAGE 1: concept-queue-vs-log.png

INITIAL PROMPT
Use case: infographic-diagram. Create a finished educational PNG infographic for a beginner Apache Kafka lab. Landscape 1536 x 1024 pixels, 3:2 aspect ratio. Flat vector aesthetic, modern and very clean, white background, crisp lines, rounded corners, no shadows, no texture, no photographs. Restricted palette: blue #2563EB, green #16A34A, orange #EA580C, slate gray #64748B, white, and very pale tints of these colors. All text must be English only, typeset in large, perfectly legible clean sans-serif. Render the specified strings verbatim, including case, punctuation, digits and ASCII -> when specified. No extra prose, no gibberish, no company logos, no watermark. Generous spacing, professionally aligned layout. Two equally sized side-by-side comparison panels divided by a thin vertical gray line.
LEFT PANEL: Heading exactly "RabbitMQ = QUEUE". Show a horizontal queue box holding exactly three envelope icons. An incoming arrow runs from a source labeled "Producer" into the left end of the queue. An outgoing arrow runs from the right end of the queue toward a recipient labeled "Consumer". One additional envelope is exiting the queue, with a small trash bin icon nearby and the label "ack -> deleted". Arrange source and recipient labels above or below their icons so everything fits. At the bottom of this panel place the exact prominent caption "Read = message removed".
RIGHT PANEL: Heading exactly "KAFKA = LOG". Show a horizontal log strip with exactly five consecutive rectangular cells. Directly beneath these five cells place exactly the digits "0", "1", "2", "3", "4", respectively, one centered under each cell. A small label "offset" identifies this row of digits. A source labeled "Producer" above the right side points with an arrow into the RIGHTMOST end of the log, labeled "append only". A recipient labeled "Consumer" below the log has an arrow pointing UP toward a single cell in the log, labeled "just a pointer". Make it visually clear that a read position is tracked while the log remains intact. At the bottom of this panel place the exact prominent caption "Read = nothing is deleted". Match the two bottom captions in baseline and size. Make text large enough to read in markdown; do not truncate any labels.

FINAL EDIT PROMPT
Use case: style-transfer. Edit this infographic. CRITICAL: replace the entire colored glowing backdrop with a PURE SOLID OPAQUE WHITE #FFFFFF background. Completely remove ALL gradients, glows, shadows, blur, soft color halos, color washes, texture, glass effects, embossed text, glossy effects and 3D effects. This must look like a crisp flat vector diagram printed on white paper, with only solid color strokes and solid flat fills. Every white area should be uniformly white, no tinted lighting at all. Use only blue #2563EB, green #16A34A, orange #EA580C, slate gray #64748B, and white. Make all body text solid slate gray or appropriate saturated color on white so the text is clearly legible. Preserve the layout, icons, exact wording, arrow directions, counts, and all other content from the original. Preserve landscape 1536x1024. No new text or logos. Remove the extra word "Queue" above the queue. Keep headings in solid blue and solid green text on white, with no colored banner. Keep the two bottom exact sentences on white with thin colored outlines.


IMAGE 2: concept-replay.png

INITIAL PROMPT
Use case: infographic-diagram. Create a finished educational PNG infographic for a beginner Apache Kafka lab. Landscape 1536 x 1024 pixels, 3:2 aspect ratio. Flat vector aesthetic, modern and very clean, white background, crisp lines, rounded corners, no shadows, no texture, no photographs. Restricted palette: blue #2563EB, green #16A34A, orange #EA580C, slate gray #64748B, white, and very pale tints of these colors. All text must be English only, typeset in large, perfectly legible clean sans-serif. Render the specified strings verbatim, including case, punctuation, digits and ASCII -> when specified. No extra prose, no gibberish, no company logos, no watermark. Generous spacing, professionally aligned layout. Explain replay with a spacious simple composition. Center-left: horizontal log strip of exactly FOUR equal cells, each containing one envelope icon. Under the cells put the digits "0", "1", "2", "3", respectively. Above the log, a clear pill label reads exactly "topic: hello".
Above the log create a solid BLUE curved arrow starting over cell 0 at the LEFT and ending with an arrowhead over cell 3 at the RIGHT. It depicts reading left to right. Place the exact label "Run 1: reads 0,1,2,3" near that upper arrow, without overlapping it.
Below the log create a dashed GREEN curved arrow starting under cell 0 at the LEFT and ending with an arrowhead under cell 3 at the RIGHT. It depicts reading the same cells again left to right. Place the exact label "Run 2: reads 0,1,2,3 again" near that lower arrow, without overlapping it.
At the far right, separate from the log and arrows, place an outlined clock icon and the exact text "log kept 7 days by default". This text may wrap to three short lines while retaining exact word order.
At the bottom, a wide emphasized pale-blue rounded band contains the large exact sentence "Consumer moves. The log does not." All four envelope messages remain visible and intact. No extra heading or text.

FINAL EDIT PROMPT
Use case: style-transfer. Edit this infographic. CRITICAL: replace the entire colored glowing backdrop with a PURE SOLID OPAQUE WHITE #FFFFFF background. Completely remove ALL gradients, glows, shadows, blur, soft color halos, color washes, texture, glass effects, embossed text, glossy effects and 3D effects. This must look like a crisp flat vector diagram printed on white paper, with only solid color strokes and solid flat fills. Every white area should be uniformly white, no tinted lighting at all. Use only blue #2563EB, green #16A34A, orange #EA580C, slate gray #64748B, and white. Make all body text solid slate gray or appropriate saturated color on white so the text is clearly legible. Preserve the layout, icons, exact wording, arrow directions, counts, and all other content from the original. Preserve landscape 1536x1024. No new text or logos. Specifically make both run labels and clock/retention label fully opaque high contrast. Retain the solid blue upper arrow and dashed green lower arrow, four cells and all four offsets. Keep bottom banner white with thin blue border.


IMAGE 3: arch-ports.png

INITIAL PROMPT
Use case: infographic-diagram. Create a finished educational PNG infographic for a beginner Apache Kafka lab. Landscape 1536 x 1024 pixels, 3:2 aspect ratio. Flat vector aesthetic, modern and very clean, white background, crisp lines, rounded corners, no shadows, no texture, no photographs. Restricted palette: blue #2563EB, green #16A34A, orange #EA580C, slate gray #64748B, white, and very pale tints of these colors. All text must be English only, typeset in large, perfectly legible clean sans-serif. Render the specified strings verbatim, including case, punctuation, digits and ASCII -> when specified. No extra prose, no gibberish, no company logos, no watermark. Generous spacing, professionally aligned layout. Create an accurate nested-box port-routing architecture diagram. Exactly THREE containment levels and two service boxes. Plan clear routing corridors so arrows never cross labels.
Outer large rounded rectangle (level 1) labeled "Your computer" near upper left. In its top band, OUTSIDE the second box, a small generic browser icon and exact URL "http://localhost:8411".
Inside outer rectangle: a large rounded rectangle (level 2) labeled "devtools container" at its upper left; immediately below label put exact text "-p 2222:22  -p 8411:8411".
Inside second rectangle: large rounded rectangle (level 3) labeled "docker compose". Inside this third rectangle are TWO separate service cards side by side, LEFT "kafka", RIGHT "kafka-ui". Under "kafka" put "9092  for programs". Under "kafka-ui" put "8080  for humans". A gray or green connector goes FROM kafka-ui toward kafka, with arrowhead entering kafka on the LEFT, and exact label "kafka:19092". Route that connector underneath the service cards with enough whitespace for the label.
A long ORANGE arrow starts at browser icon in outer top band, passes THROUGH the second and third box boundaries, and ends AT the kafka-ui card. Route it in a dedicated corridor on the right side, with exact label "8411 -> 8411 -> 8080" placed horizontally above or beside the route on white whitespace. Clearly show the arrowhead entering kafka-ui.
Inside level 2 but OUTSIDE level 3, place a small generic Python script icon (code-file pictogram, not a corporate logo), labeled exactly "send.py / receive.py". A short BLUE arrow from this icon goes into the kafka card and is labeled exactly "localhost:9092". It crosses the third rectangle boundary to reach kafka. Reserve a left or top corridor for this script icon and connection.
Containment must be unambiguous. Browser is outside devtools container but inside Your computer. Script is inside devtools container but outside docker compose. Both kafka and kafka-ui are inside docker compose. All labels must be large, readable and fully inside the canvas.

FINAL EDIT PROMPT
Use case: style-transfer. Edit this infographic. CRITICAL: replace the entire colored glowing backdrop with a PURE SOLID OPAQUE WHITE #FFFFFF background. Completely remove ALL gradients, glows, shadows, blur, soft color halos, color washes, texture, glass effects, embossed text, glossy effects and 3D effects. This must look like a crisp flat vector diagram printed on white paper, with only solid color strokes and solid flat fills. Every white area should be uniformly white, no tinted lighting at all. Use only blue #2563EB, green #16A34A, orange #EA580C, slate gray #64748B, and white. Make all body text solid slate gray or appropriate saturated color on white so the text is clearly legible. Preserve the layout, icons, exact wording, arrow directions, counts, and all other content from the original. Preserve landscape 1536x1024. No new text or logos. Replace the blue/yellow Python logo on the document icon with a simple blue generic code symbol using angle brackets, no yellow. Make the entire outside canvas white and ALL box interiors pure white, distinguishing containment using colored outlines only. Keep every label and the three routes exactly as they are.

