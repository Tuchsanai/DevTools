services:
  users:
    labels:
      traefik.http.routers.users.middlewares: users-strip@docker
  orders:
    labels:
      traefik.http.routers.orders.middlewares: orders-strip@docker
  products:
    labels:
      traefik.http.routers.products.middlewares: products-strip@docker
