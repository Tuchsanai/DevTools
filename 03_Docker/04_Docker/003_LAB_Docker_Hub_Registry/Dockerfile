# ── นามบัตรออนไลน์ : static site บน nginx ──────────────────────────
# image เล็ก build ไว → เหมาะกับการฝึก tag / push / pull กับ Docker Hub

FROM nginx:1.27-alpine

COPY index.html /usr/share/nginx/html/index.html

EXPOSE 80
