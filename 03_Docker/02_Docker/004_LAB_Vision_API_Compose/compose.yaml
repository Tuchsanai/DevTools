name: vision

services:

  backend:
    build: ./backend                 # แทน docker build -t vision-backend:1.0 backend/
    image: vision-backend:1.0
    environment:
      CANNY_LOW: ${CANNY_LOW:-100}   # อ่านจากไฟล์ .env
      CANNY_HIGH: ${CANNY_HIGH:-200}
    ports:
      - "8000:8000"                  # แทน -p 8000:8000
    networks:
      - visionnet                    # แทน --network visionnet
    healthcheck:                     # compose รอ healthy ก่อนค่อยปล่อย frontend
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz')"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s

  frontend:
    build: ./frontend
    image: vision-frontend:1.0
    environment:
      BACKEND_URL: http://backend:8000   # แทน -e BACKEND_URL=...  (ชื่อ service = ชื่อ host)
      APP_TITLE: ${APP_TITLE:-Vision API}
    ports:
      - "8501:8501"
    depends_on:
      backend:
        condition: service_healthy       # รอจน backend สุขภาพดีจริง ไม่ใช่แค่ "สตาร์ทแล้ว"
    networks:
      - visionnet

networks:
  visionnet:
