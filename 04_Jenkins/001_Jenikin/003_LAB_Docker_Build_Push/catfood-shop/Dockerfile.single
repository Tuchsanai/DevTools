# Dockerfile แบบ "ชั้นเดียว" สำหรับการทดลองเปรียบเทียบขนาด image เท่านั้น — ไม่ใช้ใน Pipeline
FROM node:22
WORKDIR /app
COPY . .
RUN npm ci --no-audit --no-fund && npm run build
ENV PORT=3000
EXPOSE 3000
CMD ["npm", "start"]
