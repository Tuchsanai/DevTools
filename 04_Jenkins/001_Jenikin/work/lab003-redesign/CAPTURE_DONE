PARENT REVIEW HANDOFF — ACTION REQUIRED BEFORE FINAL SCREENSHOTS/CLEANUP
The current browser shows mojibake in job description and ALL Thai Jenkinsfile/parameter descriptions after updatejob. Before finalizing: fix harness XML transport Content-Type to application/xml; charset=UTF-8 (and XML declaration if needed), re-upload the UTF-8 original final Jenkinsfile, rerun final version so Thai stage logs are readable. Do not alter browser DOM or fake screenshots. Diagnose exact encoding cause as needed. Initial job created correctly; update caused the corruption.
Parent has NOT finished screenshots. Treat this as feedback handoff replacing the old completion signal. KEEP experiment alive until new marker CAPTURE_FINAL_DONE exists, not merely this file. Read CAPTURE_FINAL_DONE contents before cleanup. Parent will create that marker only after final successful UI screenshots and will include complete captions. BUILD_READY should identify corrected UTF-8 run numbers. Reuse existing experiment; no new setup/restart. Existing captures in host-captures are ready for setup sections; final Build Parameters/stage screenshots will follow corrected run.
Screenshot captions so far: jenkins-01-unlock from earlier isolated run today (omit if not useful); jenkins-02-signin blank actual login; jenkins-03-dashboard actual authenticated dashboard; jenkins-04-credentials saved credential list; jenkins-05-ssh-credential and 06-dockerhub-credential are Update forms inspecting SAVED masked credentials (not creation); jenkins-07-pipeline-config before update, generic screen; jenkins-08-new-pipeline selection preview, no duplicate created; jenkins-09-plugins installed Pipeline plugins; github-01-source and github-02-dockerfile public actual source; hub-01-logged-in actual authenticated repository list.
