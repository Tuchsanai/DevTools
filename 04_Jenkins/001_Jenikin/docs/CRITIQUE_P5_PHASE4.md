# CRITIQUE P5 — Adversarial Review Phase 4b

วันที่ตรวจ: 2026-08-20  
คำตัดสิน: **HOLD — ยังไม่ควร ship**

นิยามตำแหน่ง `:ภาพ` ในตารางหมายถึงหลักฐานที่เปิดตรวจจากไฟล์ raster จริง ซึ่งไม่มีเลขบรรทัด

| id | severity | ไฟล์:บรรทัด | ปัญหา | ข้อเสนอแก้ |
|---|---|---|---|---|
| P4b-01 | BLOCKER | `docs/PLAN_P5_GITHUB.md:8,90`<br>`tools/ui/deck_offline_test.py:51-58,119-124`<br>`slides_assets/lab4_jenkins_scm.png:ภาพ`<br>`slides_assets/lab4_poll_build.png:ภาพ`<br>`slides_assets/lab5_webhook_config.png:ภาพ`<br>`slides_assets/lab5_delivery.png:ภาพ`<br>`slides_assets/lab5_auto_build.png:ภาพ` | G1 บังคับว่า active tree ต้องไม่มี Gitea แต่ยังมีภาพ Gitea/URL `http://gitea:3000` เหลืออย่างน้อย 16 ไฟล์ นอกจากห้าไฟล์ที่ระบุในคอลัมน์ตำแหน่งยังพบชุด `lab4_s02_student_registration`, `lab4_s04_repo_files`, `lab4_s06_pipeline_from_scm`, `lab4_s07_manual_build_console`, `lab4_s09_git_polling_log`, `lab4_s10_scm_build_history`, `lab5_s04_add_webhook_form`, `lab5_s05_webhook_list`, `lab5_s06_delivery_response`, `lab5_s07_webhook_build_cause`, `lab5_s08_delivery_request` ตัว gate ตรวจเพียงว่าไฟล์ legacy ห้าไฟล์ “ไม่ถูก embed” และค้นคำในชื่อ asset จึงรายงาน zero Gitea ทั้งที่ binary ยังอยู่ใน active tree | ลบ/ย้ายภาพ legacy ทั้งหมดออกนอก active tree แล้วเปลี่ยน gate เป็น exact allowlist ของไฟล์ `lab4_*`/`lab5_*`/`lab6_*` ที่อนุญาต ไม่ใช่ blacklist เฉพาะชื่อหรือเฉพาะภาพที่ embed จากนั้นรัน secret/legacy scan และ deck build ใหม่ |
| P4b-02 | BLOCKER | `docs/PLAN_P5_GITHUB.md:26,86-87`<br>`005_LAB_Webhook_Trigger/check.sh:218-289` | `check.sh` ของ LAB 5 ไม่เคยเก็บ build baseline ก่อน Add hook, ไม่ตรวจว่า ping ทำให้ delta เป็น 0 และไม่ตรวจว่า push ทำให้ delta เป็น exactly +1 มันเพียงอ่าน push delivery กับ `lastBuild` ล่าสุด ดังนั้นสถานะที่ ping เคยสร้าง build หรือ push เดียวสร้างหลาย build ยัง PASS ได้ เป็น false pass ตรง frozen acceptance | ให้ตัวตรวจสร้างหลักฐานใหม่ที่ผูกกับ run: รอ queue/build settled → เก็บ baseline → สร้าง/recreate hook และยืนยัน ping 2xx พร้อม delta 0 → push probe หนึ่ง SHA → รอ `baseline+1` → รอ quiet window แล้ว assert ยังเป็น `baseline+1`; ตรวจ queue และ cause/SHA ของ build หมายเลขนั้นโดยตรง |
| P4b-03 | BLOCKER | `docs/PLAN_P5_GITHUB.md:89`<br>`006_LAB_CICD_Capstone/README.md:298-303,362-400`<br>`006_LAB_CICD_Capstone/check.sh:257-313,474-480` | Isolation ของ LAB 6 พิสูจน์ไม่ได้สองชั้น: README เก็บ `nextBuildNumber` ก่อน push แต่ภายหลังเอาไปเทียบกับ `lastBuild.number`; สำหรับ job ที่ไม่ถูก trigger ค่าแรกคือ `N+1` แต่ค่าหลังคือ `N` จึงไม่มีวัน “เท่ากับ baseline” ตามข้อความ นอกจากนี้ `check.sh` อ่านเฉพาะ build ล่าสุดของ `webapp-deploy` และไม่อ่าน delta ของ `hello-ci-pipeline` เลย จึง PASS ได้แม้ cross-trigger | เปลี่ยน README ให้เก็บ `lastBuild.number` ของทั้งสอง jobก่อนแต่ละ push และเพิ่ม helper/check ที่ทำ controlled push สองทิศ รอ target job เสร็จ แล้ว assert `(webapp +1, hello +0)` และ `(hello +1, webapp +0)` ด้วยหมายเลขก่อน/หลังจริง |
| P4b-04 | MAJOR | `005_LAB_Webhook_Trigger/README.md:187-224,228-235,246-302`<br>`tools/ui/lab5_payload.py:68-139`<br>`tools/ui/lab5_push_build.py:83-144`<br>`docs/INTEGRATION.md:241` | เส้นทางนักศึกษาของ LAB 5 ยิง local main probe ที่ `triggered:true` แล้วไป Add hook ทันที โดยไม่มีคำสั่งรอ queue/build ให้ settled หรือคำสั่งอ่าน baseline/after-ping แม้ acceptance จะอ้าง `before ping = after ping` ปัญหา race นี้เคยเกิดจริงตาม integration log ตัวแก้มีแล้วใน helper แต่ README ระบุ `lab5_payload.py --action add-hook` เป็นทางเลือกผู้สอนเท่านั้น และไม่ expose `lab5_push_build.py` ที่พิสูจน์ exactly-one | ทำขั้น settled baseline เป็นส่วนบังคับของ student path ก่อน Add webhook พร้อมแสดงเลขก่อน/หลัง หรือให้ผู้เรียนรัน helper ที่ไม่ข้าม UI แต่ทำ postcondition จาก hook ที่เพิ่งสร้าง จากนั้นเพิ่ม `lab5_push_build.py`/คำสั่งเทียบ delta เป็นขั้นบังคับหลัง push |
| P4b-05 | MAJOR | `tools/bootstrap/common.sh:804-836`<br>`005_LAB_Webhook_Trigger/check.sh:193-215`<br>`006_LAB_CICD_Capstone/check.sh:196-218`<br>`tools/ui/lab6_github_repo.py:90-102` | การ assert ว่า webhook Secret ว่างเป็นหลักฐานเทียม: code อ่านผลจาก **List repository webhooks** แล้วถือว่า `config.get("secret") is None` แปลว่าไม่มี secret แต่ response ของ list/get hook ไม่คืน field secret อยู่แล้ว ส่วน config endpoint คืนค่าที่ตั้งไว้แบบ mask ดังนั้น hook ที่มี secret ก็ผ่านเงื่อนไขนี้ได้ และ bootstrap จะเก็บ hook เดิมนั้นไว้ ไม่ converge ตาม I-05v4 ([GitHub REST webhook schema](https://docs.github.com/en/rest/repos/webhooks?apiVersion=2022-11-28)) | อย่าอ้าง absence ของ field จาก list response ให้พิสูจน์ด้วย provenance ของ create/update ที่จงใจไม่ตั้ง secret และ/หรือดู delivery request ว่าไม่มี `X-Hub-Signature-256`; หากต้องรองรับ hook เดิมให้ตรวจ config endpoint ตาม semantics ที่ทดสอบจริงหรือ recreate เฉพาะ hook ที่ course เป็นเจ้าของ |
| P4b-06 | MAJOR | `docs/PLAN_P5_GITHUB.md:56`<br>`tools/bootstrap/jobs/hello-ci-webhook.xml:46-51`<br>`005_LAB_Webhook_Trigger/check.sh:112-130,261-272`<br>`006_LAB_CICD_Capstone/check.sh:129-147,261-280`<br>`tools/slides_src.html:73,81` | XML/README ใช้ I-05v4 ถูก แต่ checks ไม่ assert `causeString` เลย และ runtime ยอมรับเพียง cause ที่มี substring `github push`; ค่าอย่าง `GitHub push WRONG` จึง PASS แม้ไม่ใช่ `GitHub push $after` หรือ SHA ของ delivery ขณะเดียวกัน slide Capstone ของ `webapp` ระบุ token/filter แต่ตก `ref=$.ref`, `after=$.after` และ Cause ทำให้ contract สอง job ปรากฏไม่ครบเท่ากัน | เพิ่ม exact XML assertion `causeString == "GitHub push $after"` และ exact runtime assertion `shortDescription == "GitHub push <origin SHA>"` ใน LAB 5/6; เติม post-content parameters และ Cause ใน Capstone slide แล้วทำ deck gate แยก assertion ต่อ job ไม่ใช่ค้น token รวมทั้ง deck |
| P4b-07 | MAJOR | `004_LAB_Pipeline_From_Git/README.md:119-162`<br>`tools/ui/annotations/lab4.json:24-28`<br>`slides_assets/lab4_s03_github_repo_files.png:ภาพ`<br>`tools/slides_src.html:64` | README และ I-04v3 บอกให้ push/ตรวจสี่ไฟล์รวม `.course-cicd2569` แต่ภาพจริงแสดงเพียง `Jenkinsfile`, `expected.txt`, `hello.sh`; annotation ยังเขียนว่า “ตรวจไฟล์ครบสามตัว” และ slide ฝังภาพเดียวกันพร้อม caption ที่ไม่กล่าวถึง marker ภาพจึงเป็นหลักฐานก่อน fix F01 และขัดกับข้อความปัจจุบัน | capture ใหม่หลัง push fixture สี่ไฟล์ ให้ marker ครอบรายการรวม `.course-cicd2569`, แก้ annotation/caption/slide ให้ระบุสี่ไฟล์ และเพิ่ม visual manifest assertion ว่าภาพหลักฐาน I-04 มาจาก commit หลัง marker fix |
| P4b-08 | MAJOR | `docs/PLAN_P5_GITHUB.md:34,69`<br>`tools/bootstrap/common.sh:506-558` | `ensure_github_repo` สร้าง repo ก่อนแล้วจึง push marker; ถ้า push ล้มจาก network/PAT ชั่วคราว repo ว่างจะค้างอยู่ รอบถัดไปเข้าแขนง “repo มีอยู่” แล้ว fail-closed เพราะไม่มี marker พร้อมสั่งผู้เรียน rename เอง จึงไม่ idempotent/convergent ตาม DoD และทำให้มือใหม่ติดจาก failure ที่พบได้จริง | เมื่อ repo ถูกสร้างใน run นี้ ให้ trap rollback ลบเฉพาะ repo ที่ยืนยัน `created_by_this_run=true` หาก fixture push ไม่สำเร็จ หรือเก็บ provenance ที่ปลอดภัยเพื่อ retry initialization ใน run ถัดไป ห้ามผ่อน fail-closed สำหรับ repo ที่มีอยู่ก่อน |
| P4b-09 | MAJOR | `docs/PLAN_P5_GITHUB.md:58,88`<br>`tools/bootstrap/common.sh:737-783`<br>`005_LAB_Webhook_Trigger/check.sh:147-183`<br>`006_LAB_CICD_Capstone/check.sh:150-186`<br>`005_LAB_Webhook_Trigger/README.md:237-244` | Relay evidence ยัง false-pass: checks ไม่ตรวจ pinned image, network `cicd-net` หรือ restart policy `unless-stopped`; ทั้ง check และ bootstrap grep `Connected` จาก log history ทั้งหมด จึงผ่านได้แม้ session ปัจจุบัน reconnect ไม่สำเร็จ README ขั้น restart ยังกล่าวว่า `docker inspect` ต้องยืนยัน URL เดิมแต่ block รันแค่ `docker restart` กับ `docker logs` และไม่มี controlled push หลัง reconnect ตาม Relay resilience gate | ตรวจ image digest/network/restart/args ครบจาก `docker inspect`; บันทึกเวลา/URL ก่อน restart แล้วใช้ `docker logs --since` รอ Connected รอบใหม่ จากนั้น push probe ใหม่และผูก delivery→POST 200→exactly-one build; เพิ่มคำสั่ง inspect ที่ README อ้าง |
| P4b-10 | MAJOR | `docs/PLAN_P5_GITHUB.md:9`<br>`004_LAB_Pipeline_From_Git/README.md:175-182,204-214,229-235`<br>`tools/ui/annotations/lab4.json:39-61`<br>`005_LAB_Webhook_Trigger/README.md:74-85,136-157,203-216`<br>`tools/ui/annotations/lab5.json:13-16,48-72` | G2 “ทุกคลิกมี screenshot+marker+caption” ยังไม่ครบ ตัวอย่างชัด: LAB 4 ไม่มี marker ของ Save ใน SCM/Poll, ไม่มีภาพ/marker ของ Build Now และการเปิด Console; LAB 5 ไม่มี marker ของ checkbox restart และ Save; หลัง Add webhook สั่งให้ “ดู delivery เป็น 2xx” แต่มีเพียงภาพ smee ping ไม่มีหน้า GitHub Recent Deliveries/ลำดับคลิก/marker ค่า 2xx | ทำ screenshot matrix ต่อ action แล้วเติมเฉพาะช่องว่าง: Save/Build Now/Console, restart checkbox/login return, GWT Save และ GitHub Webhooks → Recent Deliveries → ping → response 2xx ทุกภาพต้องมี marker และ caption postcondition |
| P4b-11 | MAJOR | `docs/INTEGRATION.md:195-200,212-223,235-255`<br>`004_LAB_Pipeline_From_Git/README.md:119-150`<br>`004_LAB_Pipeline_From_Git/check.sh:106-133` | รายงาน integration ล่าสุดยังประกาศสถานะรวม FAIL เพราะ LAB 4 push แค่สามไฟล์/check ให้ INFO และบอกว่า `P5-INT-F01..F04` pending ทั้งหมด แต่ tree ปัจจุบัน push สี่ไฟล์และ check hard-fail marker แล้ว; F04 ก็ถูกแก้เป็น `curl -gfsS` รายงานจึงไม่ใช่หลักฐานของ commit ปัจจุบันและทำให้ผู้ตรวจไม่รู้ว่าควรเชื่อสถานะใด | เก็บส่วนเดิมเป็น historical run พร้อม commit SHA แล้วเพิ่ม post-fix integration run ที่ supersede ชัดเจน อัปเดต acceptance matrix/finding status ทีละข้อและห้ามประกาศ PASS จน P4b blockers ถูกทดสอบใหม่ |
| P4b-12 | MAJOR | `docs/PLAN_P5_GITHUB.md:36`<br>`tools/bootstrap/github_preflight.sh:21-36,57-85`<br>`005_LAB_Webhook_Trigger/check.sh:37-69` | D11 บังคับทุก GitHub API ผ่าน helper contract เดียว แต่ preflight ใช้ `curl` ตรง ไม่มี connect/max timeout, ไม่อ่าน `Retry-After`, ไม่นับ request และขยาย Bearer token ลง process argv; จึงทั้ง hang ได้และเปิด token ให้ process listing ระหว่าง request ต่างจาก helper ใน checks ที่ใช้ temp curl config | ย้าย `gh_api` เป็น source ร่วมที่ preflight/common/check เรียกจริง หรือทำ implementation เดียวกันทุกข้อ: timeout, API headers, Retry-After, request count, chmod 600 temp config และ trap cleanup โดยไม่ให้ tokenอยู่ใน argv/log |
| P4b-13 | MAJOR | `readme.md:25-32,168-176`<br>`004_LAB_Pipeline_From_Git/README.md:43-50,139,335`<br>`tools/slides_src.html:13,62-63` | Onboarding PAT ไม่สอดคล้องและเสี่ยงสะดุด: slide “Prerequisite ก่อนเริ่ม ต้องพร้อม 4 อย่าง” ไม่มีบัญชี GitHub/PAT classic/scopes เลย ทั้งที่ LAB 4 ต้องใช้ `public_repo + admin:repo_hook`; ผู้เรียนที่เตรียมตาม deck จะหยุดกลางคาบ นอกจากนี้ตัวอย่างให้แทน token จริงใน `export GITHUB_TOKEN='...'` ซึ่งมักค้างใน shell history แม้ส่วน git push จะสอน Password prompt ถูกต้องแล้ว | เติม GitHub prereq และ scope exact ลง slide เดิมก่อนครั้งที่ 2; เปลี่ยนการรับ tokenเป็น hidden prompt เช่น `read -rsp` แล้ว export/unset พร้อมคำเตือน history ที่ปฏิบัติได้จริง ส่วน push คง Username/PAT ที่ credential prompt และข้อห้าม URL/store เดิม |
| P4b-14 | MINOR | `docs/PLAN_P5_GITHUB.md:50`<br>`readme.md:142,157,175`<br>`001_LAB_Jenkins_On_Docker/README.md:167,284`<br>`002_LAB_Declarative_Pipeline/README.md:463`<br>`003_LAB_Docker_Build_Push/README.md:10,308`<br>`tools/slides_src.html:54` | R8 ยังไม่ถูกใช้กับ README ทั้งชุด: มี `cd ... && ...`, login `&& docker logout` และสามคำสั่งคั่นด้วย `;` ใน LAB 1–3/root; deck ยังแสดง `docker rm -f jenkins && docker run ...` จึงขัดกับคำสั่ง “ทีละคำสั่งทีละบรรทัด” ที่ frozen ไว้ | แตกทุกจุดเป็นคำสั่งคนละบรรทัดและใช้ subshell หลายบรรทัดเมื่อจำเป็น จากนั้นเพิ่ม parser ของ fenced `bash` blocks ที่ fail เมื่อพบ `&&` หรือ command separator `;` นอก syntax ที่ allowlist ไว้ |
| P4b-15 | MINOR | `004_LAB_Pipeline_From_Git/README.md:53-60`<br>`tools/bootstrap/github_preflight.sh:77-85`<br>`readme.md:179-184`<br>`tools/bootstrap/up_to_lab4.sh:6` | Expected output ไม่ตรงเส้นทางที่แนะนำ: PAT แบบแคบจะพิมพ์ `scope ผ่านชุด public_repo + admin:repo_hook` แต่ README แสดง `scope ผ่านชุด repo`; root แสดง `[assert] LAB 4 ready: ...` แต่ script จริงพิมพ์ `[assert] LAB 4 พร้อม: public GitHub ...` ผู้เรียนอาจคิดว่า preflight/bootstrap ไม่ผ่านทั้งที่ผ่าน | แสดง output ของ narrow profile ที่ prerequisite บังคับ หรือ normalize เป็น `<SCOPE_PROFILE>` พร้อมบอกสองค่าที่อนุญาต; copy assert บรรทัดจริงจาก script มาใช้ใน root และเพิ่ม golden-output test |
| P4b-16 | MINOR | `slides_assets/lab5_s03_smee_channel.png:ภาพ`<br>`slides_assets/lab6_s03_smee_channel.png:ภาพ`<br>`tools/ui/annotations/lab5.json:19-23`<br>`tools/ui/annotations/lab6.json:22-26` | Mask ด้านบนปิด channel id ได้และไม่พบ id จริงหลุด แต่ occurrence เดิมในตัวอย่าง CLI/Node ด้านล่างถูกวาง placeholder ทับไม่ครบจนอ่านเป็น `<SMEE_HELLO_<SMEE_HELLO_URL>` และ `<SMEE_WEBAF<SMEE_WEBAPP_URL>` ภาพจึงสื่อ command ที่ผิดและดูเหมือน mask พัง | mask ทุก occurrence ด้วย bounding box ที่ครอบข้อความเดิมทั้งหมด หรือ crop ภาพให้เหลือเฉพาะ URL หลักที่ใช้สอน แล้วเปิดตรวจ final raster ที่ resolution จริงอีกครั้ง |
| P4b-17 | MINOR | `docs/PLAN_P5_GITHUB.md:78`<br>`docs/INTEGRATION.md:225-233`<br>`tools/slides_src.html:15` | U-P5-INT กำหนด student replay พร้อมจับเวลา/ปรับ timebox แต่รายงานยอมรับเองว่าตัวเลขที่วัดเป็น automation ไม่ใช่ผู้เรียน และไม่แก้ student timebox ขณะที่ deck ยังประกาศ 40/30/45 นาทีเป็นค่าจริง จึงยังไม่มีหลักฐานว่า timebox สำหรับมือใหม่รวม login/PAT/UI/recovery ได้ | ก่อน ship ให้ทดลองอย่างน้อย 2–3 novice runs จับเวลาต่อช่วงและ p80/p90 หรือเปลี่ยนข้อความเป็น “ประมาณการ” พร้อม buffer; บันทึก human timing แยกจาก automation timingใน integration report |

## สรุปจำนวน

- BLOCKER: 3
- MAJOR: 10
- MINOR: 4
- รวม: 17

## จุดที่ตรวจแล้วผ่าน (ไม่เปิด finding)

- relative links ใน README ทั้งชุด resolve ครบ 80 ลิงก์ และไฟล์ภาพทุกชื่อที่ README อ้างมีอยู่จริง
- ลำดับ LAB 5 เปิด smee tab → relay → Connected → Add webhook ถูกต้องในเอกสาร
- ภาพ mock final 4 ภาพมี badge `ภาพจำลอง UI`; caption ที่นำไปใช้ทั้ง 4 จุดระบุ “ภาพจำลอง — UI จริงอาจต่างเล็กน้อย”, มี GitHub Docs link และ postcondition
- ไม่พบรูปแบบ token จริงหรือ smee channel id จริงใน student-facing text และ OCR/vision ของภาพที่ตรวจ; finding P4b-16 เป็นความเสียหายด้านความอ่านง่ายของ mask ไม่ใช่ secret leak
- static gates ปัจจุบัน (`int_consistency.py`, `deck_consistency_test.py`, `deck_offline_test.py`) และ `bash -n` ของ LAB 4–6/common/preflight ผ่าน แต่มี blind spots ตาม P4b-01, P4b-02, P4b-03, P4b-05, P4b-06 และ P4b-09
