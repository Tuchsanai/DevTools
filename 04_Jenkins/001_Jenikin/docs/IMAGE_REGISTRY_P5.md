# IMAGE REGISTRY — Phase 5 GitHub UI mocks

ภาพในทะเบียนนี้เป็น **mock** ที่สร้างจาก HTML/CSS และ render ด้วย Playwright Chromium เนื่องจากข้อจำกัด E5 (ไม่มี web session สำหรับหน้า GitHub ที่ต้อง login) ภาพทุกไฟล์มี badge `ภาพจำลอง UI` บนภาพ และ **ห้ามใช้เป็นหลักฐาน runtime/delivery** เมื่อฝังในบทเรียนหรือสไลด์ให้ใช้ caption: **“ภาพจำลอง — UI จริงอาจต่างเล็กน้อย”** พร้อม API postcondition ตามแล็บที่นำภาพไปใช้

| file | ชนิด | หน้าจริงที่แทน | ค่า contract ในภาพ | เหตุผล | GitHub Docs URL อ้างอิง |
|---|---|---|---|---|---|
| `slides_assets/mock/github_new_repo_hello.png` | mock | `https://github.com/new` — ฟอร์ม Create a new repository สำหรับ `hello-ci` | Owner=`<GITHUB_USER>`; Repository name=`hello-ci`; visibility=`Public`; Add a README file=ไม่เลือก; ปุ่ม `Create repository` | E5: ไม่มี web session สำหรับหน้า GitHub ที่ต้อง login | [Creating a new repository](https://docs.github.com/en/repositories/creating-and-managing-repositories/creating-a-new-repository) |
| `slides_assets/mock/github_new_repo_webapp.png` | mock | `https://github.com/new` — ฟอร์ม Create a new repository สำหรับ `webapp` | Owner=`<GITHUB_USER>`; Repository name=`webapp`; visibility=`Public`; Add a README file=ไม่เลือก; ปุ่ม `Create repository` | E5: ไม่มี web session สำหรับหน้า GitHub ที่ต้อง login | [Creating a new repository](https://docs.github.com/en/repositories/creating-and-managing-repositories/creating-a-new-repository) |
| `slides_assets/mock/github_add_webhook_hello.png` | mock | Repository Settings → Webhooks → Add webhook ของ `<GITHUB_USER>/hello-ci` | Payload URL=`<SMEE_HELLO_URL>`; Content type=`application/json`; Secret=ว่าง; SSL verification=`Enable`; events=`Just the push event`; Active=เลือก; ปุ่ม `Add webhook` | E5: ไม่มี web session สำหรับหน้า GitHub ที่ต้อง login | [Creating webhooks](https://docs.github.com/en/webhooks/using-webhooks/creating-webhooks) |
| `slides_assets/mock/github_add_webhook_webapp.png` | mock | Repository Settings → Webhooks → Add webhook ของ `<GITHUB_USER>/webapp` | Payload URL=`<SMEE_WEBAPP_URL>`; Content type=`application/json`; Secret=ว่าง; SSL verification=`Enable`; events=`Just the push event`; Active=เลือก; ปุ่ม `Add webhook` | E5: ไม่มี web session สำหรับหน้า GitHub ที่ต้อง login | [Creating webhooks](https://docs.github.com/en/webhooks/using-webhooks/creating-webhooks) |
