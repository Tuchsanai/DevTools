# LAB README Template (บังคับทุก unit U1–U6)

โครงหัวข้อตามลำดับนี้เท่านั้น — ห้ามเพิ่มหัวข้อแนว "ทำให้พัง / 💥 / ลองผิดก่อน"

````markdown
# LAB N — <ชื่อไทยสั้น ๆ>

<ย่อหน้าเดียว: แล็บนี้ตอบคำถามอะไร จบแล้วนักศึกษาทำอะไรเป็น>

## ทฤษฎีก่อนลงมือ

<อธิบายแนวคิดหลักของแล็บ 3–6 ย่อหน้าสั้น + code/config ตัวอย่างถ้าจำเป็น
อ้างสไลด์ตอนที่เกี่ยวข้อง เช่น "รายละเอียดดู slide ตอนที่ 3">

## 🎯 แล็บนี้ใน 30 วินาที

- <bullet 3–5 ข้อ: ทำอะไรบ้าง ตามลำดับ>

## สภาพตั้งต้น

ต้องมีสถานะจบ LAB N-1: <ระบุ container/volume ที่ต้องมี>

```bash
docker ps --format '{{.Names}}\t{{.Status}}'   # ต้องเห็น: <รายชื่อ>
```

> ยังไม่มี? ย้อนไปทำ [LAB N-1](../00X_LAB_.../README.md) ก่อน (ใช้เวลา ~X นาที)

## การทดลองที่ 1 — <คำถามสั้น ๆ>

**คำถาม:** <ประโยคเดียว>

```bash
<1–2 คำสั่ง>
```

✅ **สิ่งที่ต้องเห็น** :

```
<ผลรันจริง — ตัดจาก log จริงตอนทดสอบ>
```

> 📝 <1–2 บรรทัด เฉพาะจุดที่พลาดบ่อย ≤250 ตัวอักษร — ไม่บังคับทุกการทดลอง>

## การทดลองที่ 2 — ...
...

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุ | วิธีแก้ |
|---|---|---|
````

## กติกาเพิ่มเติม

1. **1 การทดลอง = 1 คำถาม = 1–2 คำสั่ง = 1 สิ่งที่ต้องสังเกต** — ขั้น UI (คลิกเว็บ) นับเป็น 1 การทดลองได้ ให้เขียนเป็น bullet ลำดับคลิกสั้น ๆ + screenshot
2. Screenshot ในเอกสารอ้างแบบ relative: `![...](../slides_assets/labN_xxx.png)` — ไฟล์เดียว ใช้ร่วมกับ slide
3. จำนวนการทดลองต่อแล็บ: 5–9 ข้อ · ความยาว README ทั้งไฟล์ ~150–300 บรรทัด
4. คำสั่งทุกบล็อกต้อง copy-paste ได้ทันที (ไม่มี placeholder ที่ต้องแทนเอง ยกเว้นจุดที่ตั้งใจสอน)
5. "✅ สิ่งที่ต้องเห็น" ต้องมาจากผลรันจริงเท่านั้น (ตัดให้สั้น แต่ห้ามแต่งขึ้น) — คนตรวจจะ diff กับ log
6. creds ที่ใช้: Jenkins `admin/admin2569` · Gitea `student/student2569` (email `student@example.com`) · webhook token แยกต่อ job: `cicd2569-hello` / `cicd2569-webapp` · Docker Hub ใช้ **placeholder เท่านั้น**: `<DOCKER_USER>` `<DOCKER_TOKEN>` — ห้ามใช้ค่าอื่น ห้ามใส่ creds จริงทุกชนิด
7. ชื่อทรัพยากรตาม interface map: network `cicd-net` · containers `jenkins`,`gitea`,`webapp` · volumes `jenkins_home`,`gitea_data` · Gitea repos `hello-ci` (LAB 4–5), `webapp` (LAB 6) · Docker Hub repos `<DOCKER_USER>/ci-demo` (LAB 3), `<DOCKER_USER>/cicd-webapp` (LAB 6)
8. หัวเรื่อง/อีโมจิ: ใช้ได้พอประมาณ (🎯 ✅ 📝 เท่าที่ template กำหนด) — อย่าเติมสีสันเกิน
