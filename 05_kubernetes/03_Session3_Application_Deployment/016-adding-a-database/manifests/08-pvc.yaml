apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-data
  namespace: lab016
spec:
  # ReadWriteOnce ให้หนึ่ง node mount แบบเขียนได้
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
