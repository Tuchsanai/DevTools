apiVersion: v1
kind: Service
metadata:
  name: mystery-web
  namespace: lab020
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 3000
      targetPort: 3001 # endpoints มี แต่ปลายทางไม่มี process ฟัง
