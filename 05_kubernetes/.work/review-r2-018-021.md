# ผลตรวจเชิงลึก (yolo1 reviewer) แล็บ 018-021 — ต้องแก้ทั้ง 4
## ข้ามแล็บ
- GLOBAL FAIL: curl localhost:8080 ใน container (018: 118,140,183,303,337 · 019: 181,280,314 · 020: 240,290,343 · 021: 146,148,211,227,239,242,245,279,311) และลูป `until curl -fsS http://localhost:8080/info` (019 L314, 020 L240/343, 021 L146/227/311) จะค้างตลอดกาล → port 80
- GLOBAL WARN (019/020/021): บรรทัด ✅ วางติด `> 📝` ไม่มีบรรทัดว่าง 15/15/14 จุด → GFM กลืนเข้า blockquote → ใส่บรรทัดว่างก่อนทุก ✅ (018 ถูกแล้ว) · L4→L5 บล็อก 💡 ติด "(ไฟล์ของแล็บนี้…)" รวมเป็น quote เดียว
- GLOBAL NIT: ปี 2026 (018) vs 2569 (019-021)
## LAB 018
- L54-59 FAIL: ไม่มี docker exec -it (และเทอร์มินัล 2-4 ต้อง docker exec -it เช่นกัน)
- L177+L261,271,334 FAIL: สั่ง "แก้ image ในไฟล์เป็น v2" ด้วยมือ แต่ขั้นหลังสมมุติว่าไฟล์ยังเป็น v1 (Expected L288/L311 v1, L350-353) → ใช้ `kubectl set image deploy/web web=k8s-lab-web:v2 -n lab018` หรือให้ sed -i + `git checkout -- manifests/02-web-deployment.yaml` ก่อนขั้น 6
- L188,205 FAIL: อ้าง loop "ไม่มี FAIL" ระหว่าง rollout v1→v2 แต่ runlog loop 120 รอบจบก่อน apply (L190/191) ไม่มีหลักฐาน → รันซ้ำเปิด loop ก่อน apply แล้ววางผลจริง หรือตัด L205 และแก้ L188 ให้อ้าง RS/Pod
- L42,199,205 WARN: "shot monitor"/shot.js เป็นเครื่องมือผู้เขียน → เขียนว่าภาพถ่ายด้วยสคริปต์ของผู้สอน นักศึกษา refresh เบราว์เซอร์แทน
- L131,196,315,353 WARN: JSON ย่อ/ตัด field ทั้งที่ jq pretty → jq -c '{pod,version,theme,db}' หรือวางจริง
- L248-258 NIT: "FAIL ติดกัน 17 ครั้ง" จริง 17 แต่ไม่ติดกันและลำดับสลับ → "รวม 17 ครั้ง" คงลำดับ · L280-288 NIT: 3/5 FAIL เกิดหลัง rolled out → คงลำดับ · L67-73 NIT: ตัด header/prefix · L136 NIT: "เปิดค้างไว้จนจบขั้น 7" · L340 NIT: port-forward ไม่ใช้ในแล็บ
## LAB 019
- ทั้งไฟล์ WARN: ไม่กล่าวถึง HPA/ResourceQuota/LimitRange (outline ขั้น 5) → เพิ่ม 2-3 ประโยค
- L247 vs 255-259 WARN: sed -n '/Last State:/,/Requests:/p' ไม่ใช่คำสั่งจริง (runlog L874 ใช้ /Containers:/,/Conditions:/) และผลตัดหลายบรรทัด → รันจริงหรือใช้คำสั่งเดิม
- L120-123,170-173 NIT: describe node ตัดบรรทัดไม่กำกับ · L118 NIT: อธิบาย 200m/250Mi มาจาก Pod ระบบ · L207-209 NIT: บล็อก get pods มาจากรอบสอง → ใช้ L835-838 · L181 NIT: ใช้ loop until curl -fsS หรือหมายเหตุ parse error
## LAB 020
- L265-285 WARN: Exercise เฉลยตรง (targetPort 3001→3000 + คำสั่ง) ทั้งที่ outline ว่าไม่มีเฉลยตรง → เหลืออาการ+เกณฑ์ ย้ายเฉลยเป็น "แนวเฉลยสำหรับผู้สอน" (<details>)
- L128 vs 133-145 WARN: get pods,service,endpoints รวม แต่ผลต่อจากสองคำสั่ง (ไม่มี pod/ prefix, service/web, Warning หาย) → วางผลคำสั่งรวมจริง (runlog L293-305)
- L178-180 WARN: Events ตัดคอลัมน์/ข้อความ "failed to pull and unpack image…" (runlog L66-70) → วางจริง/กำกับ · L160 NIT: ใส่ … · L196-197 NIT: กำกับ RESTARTS/AGE/STATUS ต่างกันได้ (CrashLoopBackOff อาจเห็น) · L273-301 NIT: เพิ่ม `kubectl delete service mystery-web -n lab020` ท้าย Exercise · L332-334 NIT: วางผลจริง · L109-122 NIT: yaml สรุปมีบรรทัดว่างแทรก
## LAB 021
- L237-262 WARN: เปลี่ยน loop รอผล (runlog L409 รอ 404, L418 รอ 200, L424 รอ 503) เป็น curl ครั้งเดียว → ใส่ loop หรือ sleep 5 + หมายเหตุ
- L110,263 WARN: คำว่า "outline" หลุด → เขียนใหม่ · L362 WARN: HPA/StatefulSet/Job/RBAC/Helm/Kustomize/GitOps แค่ไล่ชื่อ → เติม "ใช้เมื่อไร" ต่อรายการ
- L19-28 NIT: ตารางขาดคอลัมน์ "แล็บที่เรียน" · L215-221 NIT: web ยัง 1/1 หลังตัด 20 วิ (runlog L203-204) → ปรับคำอธิบาย · L129-141 NIT: apply แสดง 4/10 บรรทัดสลับลำดับ · L153-154,289,322 NIT: JSON ตัด field · L173 NIT: access log ย่อ "..." → วางจริง
