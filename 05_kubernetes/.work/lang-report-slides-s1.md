ปรับไฟล์ [Kubernetes_Session1_Slides.html](/root/workspace/DevTools/05_kubernetes/01_Session1_Kubernetes_Basics/Kubernetes_Session1_Slides.html) เรียบร้อยแล้ว

- แก้ภาษาไทยครบ 90/90 หน้า
- ฝัง SVG ล่าสุดครบ 28 คีย์ โดยคงคีย์ `ASSETS` ทั้ง 61 คีย์และลำดับเดิม
- HARD: `90 → 0`
- SOFT: เหลือ 8 จุด เนื่องจาก linter จับคำย่อย “ลอง” ในวลีบังคับ `การทดลองจำลองความล้มเหลว` ตามข้อ B3
- `check-lang-diff.py`: `OK`
- `check-slides.js`: 90 หน้า, `brokenImages=0`, `externalRefs=0`, `pageErrors=0`, `imagesWithoutAlt=0`
- Overflow 1600×900: `overflowPages=0`, ไม่พบข้อความทับ footer
- ตรวจ PNG ด้วยสายตา 20 หน้า ครอบคลุมปก สารบัญ ทฤษฎี YAML ปฏิบัติการ สรุป และหน้าปิด ไม่พบข้อความล้นหรือทับกัน
- CSS, runtime JS, `<pre>` 31 บล็อก และ `<code>` 164 บล็อกเหมือนเดิม

ตัวอย่างก่อน → หลัง:

1. `เครื่องเดียว ไม่มีใครเฝ้า` → `เครื่องเดียวโดยไม่มีผู้ดูแล`
2. `เส้นทางวันนี้ — 7 ปัญหา 7 แล็บ` → `โครงสร้างการเรียนรู้ — 7 ปัญหา 7 ปฏิบัติการ`
3. `ทาย รัน สังเกต อธิบาย ทำให้พัง และแก้กลับ` → `คาดการณ์ ดำเนินการ สังเกต อธิบาย จำลองข้อขัดข้อง และคืนสภาพ`
4. `คำถามก่อนเริ่ม — ทายก่อนกด Enter` → `คำถามนำก่อนเริ่มปฏิบัติการ — คาดการณ์ก่อนกด Enter`
5. `มองภาพนี้:` → `ข้อสังเกตจากภาพ:`
6. `ผิดบ่อย` → `ข้อผิดพลาดที่พบบ่อย`
7. `ทดลองให้พัง — อ่านอาการ แล้วแก้กลับ` → `การทดลองจำลองความล้มเหลว — ตรวจสอบอาการและคืนสภาพ`
8. `เราไม่ได้สั่งว่า "ทำอะไร" แต่บอกว่า "อยากได้สภาพแบบไหน"` → `ผู้ใช้มิได้กำหนดขั้นตอน แต่ประกาศสภาพที่ต้องการ`

สิ่งที่ไม่ได้แก้: โค้ด/ผลลัพธ์ ป้ายอังกฤษ path ชื่อไฟล์ ลำดับหน้า CSS/JS และค่า JPEG/PNG ใน `ASSETS` ตามข้อห้าม ส่วน `d09-yaml-anatomy.svg` ไม่มีคีย์ใน `window.ASSETS` จึงคงการอ้างอิงผ่าน path เดิมไว้ครับ