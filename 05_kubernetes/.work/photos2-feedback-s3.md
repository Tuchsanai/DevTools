# feedback yolo1 — ภาพถ่าย v2 ครั้งที่ 3 (หลัง cyolo1 ตรวจ .work/photos2-review-s3.md) · 2026-09-03
ภาพรวม: แนวคิดตรงหัวข้อ 14/15 (yolo1 ดู 15 ภาพแล้ว) แต่มีเครื่องหมาย/stencil/pseudo-text หลุดมา 5 ภาพ → สร้างใหม่ 5 + จัดหน้าคั่นบท 2 หน้า + กรอบภาพ

## ต้องสร้างใหม่ (ชื่อไฟล์เดิม · องค์ประกอบเดิมตาม photo-spec-v2 · สไตล์เดียวกับชุด · **ผิวตู้/เครน/เครื่องต้องเรียบ ไม่มีเครื่องหมาย stencil ตัวพิมพ์ โลโก้ ธง หรือรอยขาวคล้ายอักษร**)
1. cover-s3.jpg — ท่าเรือมุมสูง เรือ+เครน+ลานตู้น้ำเงิน/เขียว ถนนลากสายตาไปประตูรั้วเดียวให้เด่น (ตู้และเรือไม่มีเครื่องหมายใด ๆ)
2. lab017-scene.jpg — สายไฟเบอร์สีน้ำเงิน (ไม่ใช่ท่อดำ) ลอดช่องผนัง **จุดเดียว** เข้าที่ patch panel เดียว แล้วค่อยแตกเป็นพัดไปหลายแร็ค เห็นต้นทาง-ปลายทางต่อเนื่อง ไม่มีป้าย
3. lab019-scene.jpg — blade rack: สล็อตใช้งานสลับฝาปิดน้ำเงิน + มิเตอร์เดียวเข็มใกล้เส้นแดง · ผิว tab/ejector เรียบ ไม่มี glyph/ตัวพิมพ์จิ๋ว
4. concept-s3-2.jpg — ซ้าย 3 เครื่องช่องไดรฟ์ว่างชัด · ขวา storage array ไดรฟ์เต็มไฟเขียว · ไม่มีสัญลักษณ์คล้ายโลโก้/pseudo-text
5. concept-s3-3.jpg — มุมดาดฟ้าเรือ ตู้น้ำเงิน (v1) กับเขียวมรกต (v2) ปนครึ่งทาง เครนยกตู้เขียว · ไม่มี stencil บนตู้และเครน

## หน้าคั่นบท (ภาพเต็มหน้า มี gradient มืดบังครึ่งซ้าย ~58%)
- หน้า 64 (lab020_scene) และ 74 (lab021_scene): จุดโฟกัส (บล็อกแดง / เส้นทางสาย) ถูก overlay บัง → แก้ด้วย `object-position` ของ <img> ให้จุดโฟกัสอยู่ในส่วนขวาที่มองเห็น (เช่น object-position:right center หรือค่า % ที่เหมาะ) แล้ว render ดูยืนยัน ถ้ายังไม่เห็นให้ gen ใหม่โดยวางจุดโฟกัสไว้ครึ่งขวาของภาพ
- ตรวจหน้าคั่นบทที่เหลือ (015-019, 021) ด้วยหลักเดียวกัน: จุดโฟกัสต้องมองเห็นในครึ่งขวา
- ภาพ 5 ใบที่ gen ใหม่ (ข้อ 1-5) ถ้าถูกใช้บนหน้าคั่นบท/ปก ให้จัดองค์ประกอบให้จุดโฟกัสอยู่ครึ่งขวาตั้งแต่ตอน gen

## ข้อความ
- lab017_scene: alt ตามที่ cyolo1 เสนอ (สายไฟเบอร์ลอดผนังจุดเดียว → patch panel → หลายแร็ค = Ingress) · alt/photos.md ของภาพที่ gen ใหม่ให้ตรงภาพ · concept_s3_6 เป็นพื้นหลังหน้าปิด ไม่ต้องมี h2/มองภาพนี้

## กรอบภาพหน้าแนวคิด (6 หน้าที่มีกรอบ)
- แถบขาวบน-ล่างในกรอบ แก้ตาม prompt ขั้นที่ 3
