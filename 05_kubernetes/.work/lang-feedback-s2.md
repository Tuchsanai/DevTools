# feedback yolo1 — ภาษาวิชาการ ครั้งที่ 2 (หลัง cyolo1 ตรวจ .work/lang-review-s2.md) · 2026-09-03
ผลตรวจอัตโนมัติผ่าน (diff OK · HARD=0 · linter 0 FAIL · overflow 0) แต่การอ่านจริงพบสำนวนย่อ/ภาษาพูดจำนวนมาก (B 165 รายการ) และความหมายเปลี่ยน 2 จุด → แก้รอบนี้ให้ครบ

## 1. ทำครบทุกรายการในรายงานตรวจ
- ส่วน **B** ทุกข้อทุกไฟล์ (README ครั้ง · YAML_Guide · lab008-014 · photos.md · สไลด์) — ใช้ข้อความที่ผู้ตรวจเสนอ ยกเว้นเมื่อขัดกับความหมายเดิมใน .work/lang-before/ ให้ยึดความหมายเดิม
- ส่วน **C** 2 จุด: README ครั้ง บรรทัด 41 → "ฝัง (hard-code) Pod IP ไว้ฝั่ง web แล้วลบ Pod ปลายทาง" · lab010 บรรทัด 339 → "ลบ namespace `lab010` ทั้งหมดและพิสูจน์ว่าไม่เหลือทรัพยากร" (ตรวจทุกไฟล์ของครั้งนี้ที่มีคำว่า "ลบ namespace ทั้งหมด" แล้วระบุชื่อ namespace ของปฏิบัติการนั้นทุกจุด)
- ข้อความเทคนิคที่ผู้ตรวจชี้ว่าไม่แม่นยำ (YAML_Guide บรรทัด 227 `default medium` · คำอธิบาย PVC/storage และ writable layer ในสไลด์ · lab012 การจำแนก Pod restart กับ container restart และ "ข้อมูลอยู่/หาย") ให้แก้ให้ถูกต้องตามความหมายเดิมและตามพฤติกรรม Kubernetes จริง (emptyDir อยู่รอดเมื่อ container restart แต่หายเมื่อ Pod ถูกลบ · PVC อยู่นอก lifecycle ของ Pod)

## 2. ศัพท์ให้สม่ำเสมอทั้งครั้งที่ 2
- desired state → "สภาพที่ต้องการ (desired state)" ครั้งแรกในไฟล์/หน้า จากนั้น "สภาพที่ต้องการ" · actual state → "สภาพจริง (actual state)" · "คาดผล" → "คาดการณ์ผลลัพธ์"
- ลักษณนาม "ตัว" กับ Pod/container/env → "รายการ" หรือละไว้ ("Pod หลายตัว" → "Pod หลายรายการ" · "env ตัวเดียว" → "ตัวแปรสภาพแวดล้อมเพียงรายการเดียว")
- สำนวนย่อในตาราง/รายการตรวจสอบ/checklist ต้องเป็นประโยคหรือวลีสมบูรณ์ ("db down" → "db หยุดทำงาน" · "ยิง" → "ส่งคำขอ" · "กด Ctrl+C" → "กดปุ่ม Ctrl+C เพื่อยุติ" · "กันไว้/จับ Pod เก่า/Pod ดี/กลับมาเอง" → เรียบเรียงใหม่ · "ไม่ขึ้นไฟ" → "ไฟสถานะไม่ติด")
- สไลด์: ข้อความใน overview/metadata (title/description/ป้าย agenda) ต้องเป็นวิชาการเช่นเดียวกับเนื้อหา

## 3. ข้อเท็จจริงที่ผิดอยู่เดิม
- README ระดับครั้ง บรรทัด 15: จำนวนหน้าสไลด์ → 88 หน้า (ตรวจด้วย check-slides.js)

## 4. หลังแก้
- รัน check-lang-diff.py / check-register.py / check-readme.py / check-yaml-teaching.py / check-slides.js + overflow check ซ้ำ · เปิดดู PNG ของทุกหน้าสไลด์ที่แก้
- รายงานจำนวนรายการ B/C ที่แก้ และรายการที่ไม่แก้พร้อมเหตุผล
