ตรวจภาพจริงครบ 9 ไฟล์แล้ว โดยคะแนนเรียงเป็น: ตรงขั้นตอน / หลักฐานมองเห็น / คุณภาพภาพ / ความสม่ำเสมอ / ความปลอดภัย

| แล็บ | ไฟล์ | คะแนน 5 หมวด | ปัญหา | ถ่ายใหม่? |
|---|---|---:|---|---|
| 003 | `03-first-page-on-kubernetes.png` | 5 / 5 / 5 / 5 / 5 | ไม่มี—เห็น Pod `web`, v1, API โหมดเดี่ยว และ DB unknown ครบ | ไม่ |
| 004 | `05-pod-v2-after-apply.png` | 5 / 5 / 5 / 5 / 5 | ไม่มี—เห็น Pod `web`, v2 และ theme emerald ตรงขั้นตอน | ไม่ |
| 005 | `03-web-b-v2-label.png` | 5 / 5 / 5 / 5 / 5 | ไม่มี—เป็นภาพ optional ตาม outline และเห็น `web-b`, v2, emerald ชัด | ไม่ |
| 006 | `02-pod-name-a.png` | 5 / 5 / 5 / 5 / 5 | ไม่มี—เห็น Pod `web-dhqdn`, v1 | ไม่ |
| 006 | `02-pod-name-b.png` | 5 / 5 / 5 / 5 / 5 | ไม่มี—เห็น Pod `web-q4tks`; ต่างจากภาพ A ชัด | ไม่ |
| 006 | `03-after-delete-new-pod.png` | 5 / 5 / 5 / 5 / 5 | ไม่มี—เห็น Pod ใหม่ `web-s8hwd`; README ระบุแล้วว่าชื่อ Pod ต่างกันได้ | ไม่ |
| 006 | `break-mixed-v1-v2.png` | 5 / 4 / 5 / 4 / 5 | ภาพแสดง request ที่ตกถึง v2 เท่านั้น หลักฐานว่ามี v1 ปนต้องอ่านร่วมกับตาราง `kubectl` รอบภาพ; ชื่อไม่ขึ้นต้นด้วยเลขเพราะ outline กำหนดชื่อนี้โดยตรง | ไม่ |
| 007 | `03-web-v2-after-rollout.png` | 5 / 5 / 5 / 5 / 5 | ไม่มี—เห็น Pod จาก RS v2, ป้าย v2 และ emerald | ไม่ |
| 007 | `05-web-v1-after-undo.png` | 5 / 5 / 5 / 5 / 5 | ไม่มี—เห็น Pod จาก RS v1, ป้าย v1 และ blue; ความต่างก่อน/หลังชัด | ไม่ |

ภาพทั้งหมด:

- ขนาด 1280×800
- ถอดรหัส PNG ผ่านครบ ไม่มีไฟล์เสีย
- ขนาด 122–131 KB ต่ำกว่า 600 KB
- ไม่เบลอและการ์ด WEB/API/DB ไม่ถูกตัด
- ไม่มี email, token, ชื่อบุคคลจริง, IP `172.30.*` หรือชื่อ `devtools-k8s-lab-*`

รายการภาพบังคับตาม outline ที่ยังไม่มี: **ไม่มี**  
ไม่มีภาพใดได้ต่ำกว่า 4 จึงไม่ต้องถ่ายใหม่และไม่ได้แก้ README

ยืนยัน cleanup: ไม่ได้สร้าง container เพราะไม่เข้าเงื่อนไขถ่ายใหม่ และตรวจ `docker ps -a --filter name=devtools-k8s-shots-s1` แล้วว่าง—ไม่มี container `devtools-k8s-shots-s1` ค้างอยู่