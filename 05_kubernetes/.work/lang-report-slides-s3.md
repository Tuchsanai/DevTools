ปรับปรุงไฟล์ [Kubernetes_Session3_Slides.html](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/Kubernetes_Session3_Slides.html) เรียบร้อยแล้ว

ผลการแก้ไข

- เนื้อหาเปลี่ยนแปลงรวม 89/90 หน้า
  - แก้ข้อความใน markup 85 หน้า
  - อีก 4 หน้าได้รับ SVG ฉบับล่าสุด
  - หน้า 1 คงเดิม เนื่องจากข้อความเป็นภาษาวิชาการอยู่แล้ว
- ฝัง SVG ใหม่ครบ 15/15 KEY
- ASSETS ยังคง 44 KEY ลำดับเดิม
- JPEG/PNG เดิมไม่เปลี่ยน
- anchor ครั้งที่ 3 เปลี่ยนถูกต้อง 1 จุด และไม่เหลือ anchor เดิม
- HARD: `74 → 0`
- SOFT เหลือ 2 จุด: เป็นการจับคำว่า “ลอง” ภายในคำวิชาการ “จำลอง” มิใช่ภาษาพูด

ผลการตรวจ

- `check-lang-diff.py`: `OK`
  - `<pre>` เหมือนเดิม
  - ASSETS keys เหมือนเดิม
  - จำนวนหน้า 90 หน้า
  - CSS เหมือนเดิม
- `check-register.py`: `HARD=0 · SOFT=2`
- `check-slides.js`:
  - `slides=90`
  - `images=47`
  - `brokenImages=0`
  - `externalRefs=0`
  - `externalImports=0`
  - `pageErrors=0`
  - navigation ผ่านครบ
- Overflow ที่ viewport 1600×900:
  - `overflowPages=[]`
  - `issueCount=0`
  - ไม่พบข้อความทับ footer
- ตรวจเพิ่มเติม:
  - SVG ที่ถอดรหัสจาก ASSETS ตรงกับไฟล์ต้นฉบับ `15/15`
  - JS เหมือนเดิมเมื่อไม่นับ payload SVG ที่ได้รับอนุญาตให้เปลี่ยน
- เปิดตรวจ PNG 21 หน้า ครอบคลุมปก, agenda, ทฤษฎี, YAML anatomy, ปฏิบัติการ, สรุป และหน้าปิด ไม่พบข้อความล้นหรือทับกัน

ตัวอย่างก่อน → หลัง

1. `เส้นทางวันนี้ — 7 ปัญหา 7 แล็บ` → `โครงสร้างการเรียนรู้ — 7 ปัญหา 7 ปฏิบัติการ`
2. `วิธีเรียนของทุกแล็บ` → `วงจรในปฏิบัติการ`
3. `ผิดบ่อย` → `ข้อผิดพลาดที่พบบ่อย`
4. `มองภาพนี้:` → `ข้อสังเกตจากภาพ:`
5. `คำถามก่อนเริ่ม — ทายก่อนกด Enter` → `คำถามนำก่อนเริ่มปฏิบัติการ — คาดการณ์ก่อนกด Enter`
6. `Pod / Service ที่พังโดยตั้งใจ` → `Pod / Service ผิดพลาดโดยเจตนา`
7. `ฉันอธิบายได้หรือยัง — ไม่ใช่แค่พิมพ์ตามได้` → `อธิบายได้หรือไม่ — มิใช่เพียงทำตามคำสั่ง`
8. `#5-manifest-ที่พังโดยตั้งใจของแล็บ-020` → `#5-manifest-ที่มีข้อผิดพลาดโดยเจตนาของปฏิบัติการ-020`

สิ่งที่ไม่ได้แก้

- `<pre>/<code>`, คำสั่ง และผลลัพธ์ คงเดิมทุกตัวอักษร
- CSS/JS, โครงสร้าง, ลำดับ และจำนวนหน้าไม่เปลี่ยน
- ป้ายอังกฤษ เช่น `KUBERNETES`, `SESSION 03` และ `LAB 0NN` คงเดิม
- path, ชื่อไฟล์, URL และ technical terms คงเดิม
- ไม่เพิ่ม `d09` เนื่องจากไม่มี KEY เดิมใน `window.ASSETS` และข้อกำหนดห้ามเพิ่มหรือลด KEY
- ไม่สร้างไฟล์รายงานเพิ่มเติมในโฟลเดอร์ส่งมอบ