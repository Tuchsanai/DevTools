ตรวจครบโดยไม่แก้ไขหรือสร้างไฟล์ใด ๆ

หมายเหตุ: โจทย์ระบุว่า “5 หมวด” แต่แจกแจงเกณฑ์จริง 4 หมวด จึงให้คะแนนตาม 4 หมวดที่ระบุ โดยคะแนนแต่ละหมวดอยู่ในช่วง 1–5

## A. คะแนนรายไฟล์

| ไฟล์ | ระดับภาษา | เทคนิค | สม่ำเสมอ | โครงสร้าง | ข้อสังเกตหลัก |
|---|---:|---:|---:|---:|---|
| [README หลัก](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/README.md:1) | 4 | 3 | 3 | 5 | ระบุจำนวนสไลด์ 79 หน้า แต่ไฟล์จริงมี 90 หน้า; ยังมี “overview grid”, “หลาย terminal” |
| [YAML_Guide.md](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/YAML_Guide.md:1) | 3 | 4 | 3 | 5 | desired/actual state ไม่ตรงศัพท์มาตรฐาน และมี “งานที่รัน”, “จับอาการล้ม” |
| [LAB 015](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/015-assembling-a-real-application/README.md:1) | 3 | 4 | 3 | 5 | ยังมีสำนวนกระชับแบบภาษาพูด เช่น “ผล grep ว่าง”, “หลักฐานสามมุม”, “ส่งค่ารัน” |
| [LAB 016](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/016-adding-a-database/README.md:1) | 2 | 3 | 3 | 5 | มีประโยคไม่สมบูรณ์หลายแห่ง และประโยค replication/StatefulSet เปลี่ยนความสัมพันธ์ทางเทคนิค |
| [LAB 017](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/017-ingress-the-front-door/README.md:1) | 2 | 4 | 3 | 5 | มี “หา db”, “ไปคนละ Service”, “วาง path/ชน prefix ผิด”, “เข้าประตูได้” |
| [LAB 018](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/018-updating-without-downtime/README.md:1) | 2 | 2 | 3 | 5 | สำนวนตารางยังเป็นภาษาพูด และ `refresh` ถูกเปลี่ยนเป็น “ปรับปรุง” ซึ่งเปลี่ยนความหมาย |
| [LAB 019](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/019-telling-kubernetes-what-you-need/README.md:1) | 2 | 2 | 3 | 5 | ถ้อยคำเกี่ยวกับ OOMKilled และ image คลาดเคลื่อน; “ลบ namespace ทั้งหมด” กำกวม |
| [LAB 020](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/020-reading-the-symptoms/README.md:1) | 2 | 3 | 2 | 5 | ใช้ `layer`/“ชั้น” ปะปนมากที่สุด และ “ไฟล์หาย” ถูกเปลี่ยนเป็น “ไฟล์สูญหาย” |
| [LAB 021](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/021-the-big-picture/README.md:1) | 2 | 4 | 2 | 5 | มีข้อความย่อแบบบันทึกจำนวนมาก เช่น “request เดิน”, “ตัดสี่ชิ้น”, “ทำ output สั้น” |
| [photos.md](/root/workspace/DevTools/05_kubernetes/03_Session3_Application_Deployment/slides_assets/photos/photos.md:1) | 5 | 5 | 5 | 5 | ไม่พบจุดที่ต้องแก้ |
| สไลด์ HTML | 2 | 2 | 2 | 5 | หน้า 40 มีประโยคเสียรูป; หน้า 49 มีคำสั่งกับคำอธิบายขัดกัน; `layer`/“ชั้น” ปะปน |

ผลด้านโครงสร้าง:

- `check-lang-diff.py`: ผ่าน `OK` ครบ 11/11 ไฟล์
- `check-register.py`: `HARD=0` ทุกไฟล์; ค่า `SOFT` ที่รายงานทั้งหมดมาจากการจับคำว่า “ลอง” ภายในคำมาตรฐาน “ทดลอง/จำลอง” จึงเป็น false positive
- B1/B3: หัวข้อและป้ายบังคับครบและตรงตามเกณฑ์
- `check-readme.py`: โครงสร้าง LAB ทั้งเจ็ดผ่าน แต่เตือนความยาวเกินเป้าหมาย 400 บรรทัดใน LAB 015–021 ยกเว้น LAB 016 ที่ 401 บรรทัด
- `check-yaml-teaching.py`: `FAILS=0`
- สไลด์: 90 หน้า, 47 ภาพ, ไม่มี broken image, alt ขาด, external asset, navigation error หรือ page error
- ตรวจภาพ render จริง 20 หน้า ได้แก่ 1, 2, 4, 12, 18, 23, 30, 35, 42, 47, 54, 59, 63, 64, 69, 74, 79, 86, 89 และ 90 ไม่พบข้อความล้นหรือซ้อนกัน
- การเทียบก่อน–หลัง: สุ่มเทียบอย่างน้อย 10 ย่อหน้าต่อไฟล์ใหญ่ทุกไฟล์ และ 10 หน้าสไลด์จาก 85 หน้าที่มีการแก้ภาษา

## B. จุดที่ต้องแก้ทั้งหมด

รายการนี้ไม่รวมข้อความใน code block, output ตัวอย่าง, inline code และหัวข้อที่เกณฑ์ B/D กำหนดให้คงเดิม

### ความสม่ำเสมอร่วมทั้งชุด

- `desired state` ควรใช้ “สภาพที่ต้องการ (desired state)” เมื่อกล่าวครั้งแรก และ “สภาพที่ต้องการ” หลังจากนั้น  
  จุดที่พบ: README หลัก 10; YAML Guide 9, 194, 215, 229, 373; LAB 015 บรรทัด 115; สไลด์หน้า 4, 5, 76, 86–87
- `actual state` ควรใช้ “สภาพจริง (actual state)” เมื่อกล่าวครั้งแรก และ “สภาพจริง” หลังจากนั้น  
  จุดที่พบ: YAML Guide 10; LAB 015 บรรทัด 47, 256, 391; LAB 016 บรรทัด 364; สไลด์หน้า 4
- `layer` ในข้อความภาษาไทยควรใช้ “ชั้นของระบบ” ให้สม่ำเสมอ  
  จุดที่พบ: README หลัก 93; LAB 015 บรรทัด 27; LAB 020 บรรทัด 1, 11, 26, 51; LAB 021 บรรทัด 24; สไลด์หน้า 2, 12, 23, 35, 47, 59, 69–70, 79, 84, 88
- “มุมผู้ใช้” → “มุมมองของผู้ใช้”  
  LAB 016:310, LAB 018:334, LAB 019:333, LAB 020:333, สไลด์หน้าทฤษฎี 8, 19, 31, 43, 55, 65, 75
- “หลักฐานสามมุม/ครบสามมุม” → “หลักฐานจากสามแหล่ง”  
  LAB 015:330, LAB 016:172, LAB 019:344
- “ลบปิดท้าย” → “ลบทรัพยากรเมื่อสิ้นสุด”  
  LAB 016:47, LAB 018:369, LAB 019:383, LAB 020:382, LAB 021:369
- “รัน” ในข้อความอธิบาย → “ทำงาน/เรียกใช้งาน/ดำเนินการ” ตามบริบท  
  YAML Guide:173; LAB 015:400; LAB 016:310; LAB 020:117,427; LAB 021:21

### README หลัก

- 15: “สไลด์ประกอบ 79 หน้า” → “สไลด์ประกอบ 90 หน้า”
- 31: “เปิด overview grid” → “เปิดมุมมองภาพรวมแบบตาราง”
- 54: “แยก ... ได้ถูก” → “แยก ... ได้อย่างถูกต้อง”
- 74: “เปิดหลาย terminal” → “เปิดหน้าต่าง terminal หลายหน้าต่าง”

### YAML Guide

- 11: “YAML ใช้ย่อหน้า 2 ช่อง” → “YAML ใช้การเยื้อง (indentation) ขนาด 2 ช่อง”
- 67–68: “ผู้ใช้ได้ API endpoint...” → “ผู้ใช้ได้รับ API endpoint...”
- 123: “จึงอย่าตัด `i` ทิ้ง” → “จึงไม่ควรละอักษร `i`”
- 139: “ตั้ง...แล้วคิดว่า...อาจผิด” → “การกำหนด...โดยสันนิษฐานว่า...อาจทำให้ค่าคลาดเคลื่อน”
- 173: “งานที่รันสองรุ่นพร้อมกันไม่ได้” → “งานที่ไม่สามารถทำงานสองรุ่นพร้อมกันได้”
- 176: “จับอาการล้ม” → “ตรวจพบความล้มเหลว”
- 186: “process ที่ยังรับงานไม่ได้อาจเข้าปลายทาง” → “process ที่ยังไม่พร้อมรับงานอาจถูกเพิ่มเป็นปลายทางเร็วเกินไป”
- 223: “จะกลบ...” → “อาจบดบัง...”
- 256: “`get` หาอาการ → `describe` หาเหตุผล → `logs` ฟัง process → `events` มองภาพรวม” → “`get` ระบุอาการ → `describe` ตรวจสอบสาเหตุ → `logs` ตรวจข้อความจาก process → `events` พิจารณาภาพรวม”

### LAB 015

- 64: “คำสั่งแรกเริ่มทำงานเครื่องเดิม” → “คำสั่งแรกเริ่ม container ที่มีอยู่เดิม”
- 78: “ถ้าผล `grep` ว่าง → ทำขั้น build/load” → “หาก `grep` ไม่แสดงผล ให้ดำเนินขั้นตอน build/load”
- 114: “ปฏิบัติการนี้เริ่มศึกษาระบบจาก...” → “การศึกษาระบบในปฏิบัติการนี้เริ่มจาก...”
- 130: “คืนแหล่งความจริงให้ระบบ” → “คืนสภาพระบบตาม source of truth”
- 304: “ตรวจสอบ response จริง” → “ตรวจสอบผลตอบสนองจริง”
- 330: “ครบสามมุม” → “ครบทั้งสามแหล่งหลักฐาน”
- 345: “DNS cache ยังไม่หมด” → “DNS cache ยังไม่หมดอายุ”
- 363: “poll Ingress ... rule sync” → “ตรวจสอบ Ingress ซ้ำจน controller ประสานกฎเสร็จสมบูรณ์”
- 400: “ConfigMap ส่งค่ารัน...” → “ConfigMap ส่งค่ากำหนดการทำงาน...”

### LAB 016

- 23: “ต้องกลับมาเห็น data directory เดิม” → “ต้องกลับมาเข้าถึง data directory เดิมได้”
- 24: “เพิ่ม replica ได้ตรง ๆ” → “เพิ่ม replica ได้โดยตรง”
- 47: “Cleanup, Clean Re-run และลบปิดท้าย” → “Cleanup, Clean Re-run และการลบทรัพยากรเมื่อสิ้นสุด”
- 55: “เริ่มทำงาน container” → “เริ่ม container ดังกล่าว”
- 79: “ถ้าผล `grep` ว่าง...” → “หาก `grep` ไม่แสดงผล...”
- 99: “อ่านชุดนี้เป็นสองกลุ่ม” → “ชุดไฟล์นี้แบ่งเป็นสองกลุ่ม”
- 119: “หา key/Secret” → “ระบุ key/Secret”
- 125: “ถูกต่อให้ใช้ Secret” → “ได้รับการกำหนดให้ใช้ Secret”
- 172: “การตรวจสอบหลักฐานสามมุม” → “การตรวจสอบหลักฐานจากสามแหล่ง”
- 256: “เลือก Pod ใหม่สุด” → “เลือก Pod ล่าสุด”; “database ทำจริง” → “พฤติกรรมจริงของฐานข้อมูล”
- 270: ต้องเติมเครื่องหมายและคำเชื่อมตามข้อ C
- 298, 300: “แก้ของจริง” → “แก้ไขทรัพยากรจริง”; “ตัว database” → “ระบบฐานข้อมูล”
- 310: “ครอบคลุมตัวรัน เส้นทาง และ storage · ... `/info` เป็นมุมผู้ใช้ · `logs` เป็นมุมแอป” → “ครอบคลุม workload เส้นทางเครือข่าย และ storage · ... `/info` แสดงหลักฐานจากผู้ใช้ · `logs` แสดงหลักฐานจากแอปพลิเคชัน”
- 328: “DB Pod ใหม่ขึ้นแต่ข้อมูลหาย” → “DB Pod ใหม่เริ่มทำงานได้ แต่ข้อมูลสูญหาย”
- 345: “ให้ลบ namespace ทั้งหมด” → “ให้ลบ namespace `lab016`”
- 374: “ไม่ได้แปลว่า” → “มิได้หมายความว่า”
- 377: “ข้อมูลรอด” → “ข้อมูลยังคงอยู่”
- 378: “ตอนสร้าง...ถ้าหาย Pod เก่ายังวิ่ง...” → “ระหว่างการสร้าง...หากข้อมูลสูญหาย อาจเกิดจาก Pod เดิมยังคงทำงาน...”
- 379: “ไม่พอ” → “ไม่เพียงพอ”
- 387: “ทิ้ง/สร้าง/scale” → “ลบ สร้างใหม่ และเพิ่มจำนวน replica”

### LAB 017

- 25: “จุดเข้าหลัก” → “จุดเข้าถึงหลัก”
- 38: “กำหนด backend ให้ผิด” → “กำหนดค่า backend ไม่ถูกต้อง”
- 115: “ไปคนละ Service” → “ไปยัง Service คนละรายการ”
- 119: “ให้ api หา db” → “ให้ api เชื่อมต่อกับ db”
- 120: “Ingress ผิดบ่อย” → “ข้อผิดพลาดที่พบบ่อยของ Ingress”
- 139: “ถ้าเปลี่ยนค่านี้” → “ผลเมื่อเปลี่ยนค่า”
- 201: “แยกห้อง” → “แยก namespace”
- 225: “`jq` จัด JSON” → “`jq` จัดรูปแบบ JSON”
- 278: “สาเหตุอยู่ชั้น route” → “สาเหตุอยู่ที่ชั้นการกำหนดเส้นทาง”
- 292: “`curl` อ่าน 503” → “`curl` ตรวจพบสถานะ 503”
- 318: “ละ body” → “ไม่แสดง response body”
- 334: “เปิดแล้วได้...” → “เมื่อเข้าถึงแล้วได้รับ...”
- 335: “backend ว่าง” → “backend ไม่มี endpoint”
- 337: “วาง path/ชน prefix ผิด” → “กำหนด path หรือลำดับ prefix ไม่ถูกต้อง”
- 338: “ตัดสิน route” → “ประเมินการกำหนดเส้นทาง”
- 354: “สร้าง/apply/wait ... curl สอง path...” → “สร้าง namespace นำ manifest ไปใช้ รอให้ workload พร้อม แล้วตรวจสอบ path ทั้งสองด้วย `curl`”
- 387: “container port ที่เดาเอง” → “container port ที่กำหนดโดยไม่มีหลักฐาน”
- 388: “เข้าประตูได้แล้ว แต่เดินต่อ...” → “request เข้าถึง Ingress ได้ แต่ไม่สามารถส่งต่อไปยัง backend”
- 406: “ทำ ... ให้ผิดจนเห็น 503 จริงแล้ว” → “กำหนด ... ให้ไม่ถูกต้องและยืนยันว่าปรากฏสถานะ 503 แล้ว”

### LAB 018

- 12: “เว็บล่ม” → “บริการเว็บขัดข้อง”
- 16: “แล้วค่อย” → “แล้วจึง”
- 21: “ระหว่าง update เพิ่มเกินได้เท่าไร” → “จำนวน Pod ที่อนุญาตให้เพิ่มเกินค่าที่กำหนดระหว่างการปรับปรุง”
- 22: “ยอมให้ตัวพร้อมหายกี่ตัว” → “จำนวน Pod ที่พร้อมทำงานซึ่งยอมให้ลดลงได้”
- 23: “Pod ใหม่รับ request ได้หรือยัง” → “Pod ใหม่พร้อมรับ request หรือไม่”
- 24: “ต้องพร้อมนิ่งนานเท่าไรก่อนเดินต่อ” → “ระยะเวลาที่ Pod ต้องอยู่ในสถานะ Ready ต่อเนื่องก่อนดำเนิน rollout ต่อ”
- 42: “refresh หน้าเว็บ” ถูกเปลี่ยนเป็น “ปรับปรุงหน้าเว็บ”; ควรเป็น “โหลดหน้าเว็บใหม่”
- 103, 108: “ให้ web/api หา...” → “ให้ web/api เชื่อมต่อกับ...”
- 109: “เพื่อเห็น downtime” → “เพื่อสังเกตช่วงหยุดให้บริการ”
- 124: “ถ้าเปลี่ยนค่านี้” → “ผลเมื่อเปลี่ยนค่า”
- 127: “มากขึ้นเร็วขึ้น...” → “ค่าที่มากขึ้นทำให้ rollout ดำเนินการเร็วขึ้น...”
- 133: “รับ request เร็วไป” → “รับ request เร็วเกินไป”
- 160: “`wait` รอ” → “รอจน...”
- 174: “สำหรับยิง request” → “สำหรับส่ง request”
- 182: “ไม่ซ่อน failure” → “ไม่ปกปิดความล้มเหลว”
- 215: “controller เดินครบ” → “controller ดำเนิน rollout ครบถ้วน”
- 227: “refresh browser” → “โหลดหน้าเว็บในเบราว์เซอร์ใหม่”
- 261: “คำเตือนบอกว่า” → “คำเตือนระบุว่า”
- 273: “request loop เปิดเผยช่วงที่ผู้ใช้เข้าไม่ได้” → “request loop แสดงช่วงเวลาที่ผู้ใช้ไม่สามารถเข้าถึงบริการ”
- 291: “แก้กลับ” → “คืนสภาพ”
- 324: “แต่ละจังหวะ” → “แต่ละขั้น”
- 349: “rollout ค้าง” → “rollout ไม่เสร็จสมบูรณ์”
- 353: “ผลแปลก” → “ผลที่ไม่สอดคล้องกับที่คาด”
- 397: “ไม่ได้เกิดจากคำว่า...” → “มิได้เกิดจากการระบุ...”
- 399: “ตัวใหม่” → “Pod ใหม่”
- 400: “readiness กัน...” → “readiness ป้องกันไม่ให้ Pod รับ request ก่อนพร้อมทำงาน”
- 401: “ไม่ต้อง rebuild” → “ไม่จำเป็นต้อง build image ใหม่”

### LAB 019

- 23: “ไม่มี node พอแล้ว Pod ค้าง” → “เมื่อไม่มี node ที่มีทรัพยากรเพียงพอ Pod จะอยู่ในสถานะ Pending”
- 30: “เมื่อเครื่องตึง” → “เมื่อ node มีทรัพยากรตึงตัว”
- 37: “การปฏิเสธ Pod ... ของ scheduler” → “scheduler ปฏิเสธ Pod ที่ขอ CPU เกินความจุ”
- 39: “ใช้ memory ถึง limit” → “ใช้ memory เกิน limit”
- 50: “แก้กลับ” → “คืนสภาพ”
- 126: “ถ้าเปลี่ยนค่านี้” → “ผลเมื่อเปลี่ยนค่า”
- 131: “อาจจบเป็น OOMKilled” → “process อาจสิ้นสุดและระบบรายงาน `OOMKilled`”
- 137: “สองอาการเกิดคนละจังหวะ จึงอย่าแก้...” → “อาการทั้งสองเกิดในคนละขั้น จึงไม่ควรปรับ limit เมื่อปัญหาอยู่ที่ scheduling”
- 150: “`sed` ตัดมา...” → “`sed` แสดงเฉพาะ...”
- 186: “กันการรอ”; “หยิบ” → “ป้องกันการรอ”; “เลือก”
- 225: “loop รอ” → “ตรวจสอบซ้ำจน...”
- 266: “Pod ลูก” → “Pod ที่ Deployment ดูแล”
- 285: “เปิดเวลาให้ container ชน limit” → “เว้นเวลาให้ container ใช้หน่วยความจำเกิน limit และเริ่มทำงานใหม่”
- 323: “QoS กับ usage เป็นคนละเรื่อง” → “QoS และการใช้ทรัพยากรจริงเป็นคนละแนวคิด”
- 327: “ชุดตรวจสั้น” → “ชุดคำสั่งตรวจสอบแบบย่อ”
- 350: “เพิ่งเริ่ม” → “เพิ่งเริ่มทำงาน”
- 354: “image ไม่ทำงาน” → “ไม่พบ image ที่กำหนด”
- 369: “ลบ namespace ทั้งหมด” → “ลบ namespace `lab019`”
- 383: “ingress ยัง sync; ลูปรอ...” → “Ingress ยังประสานกฎไม่เสร็จสมบูรณ์ จึงต้องตรวจสอบซ้ำจนได้รับผลตอบสนองที่ถูกต้อง”
- 390: “กันของค้าง” → “ป้องกันทรัพยากรคงค้าง”
- 408: “หยิบ field” → “เลือก field”
- 414: “คำสัญญา” → “ปริมาณทรัพยากรที่ scheduler สำรอง”
- 418: “กันไว้” → “สำรองไว้”

### LAB 020

- 12: “ไล่หลักฐาน” → “ตรวจสอบหลักฐานตามลำดับ”
- 117: “ก่อนรัน” → “ก่อนเรียกใช้งาน”
- 133: “ไม่ใช่ลบ Pod ซ้ำ” → “มิใช่การลบ Pod ซ้ำ”
- 139: “จงใจผิดคนละจุด” → “กำหนดข้อผิดพลาดไว้คนละตำแหน่งโดยเจตนา”
- 172: “Warning บอกว่า” → “คำเตือนระบุว่า”
- 173: “ไม่ได้จับคำว่า...” → “ไม่ปรากฏข้อความ...”
- 183: “ลบ Pod immutable” → “ลบ Pod ซึ่งไม่สามารถแก้ไข field ดังกล่าวโดยตรงได้”
- 203: “คำตอบ registry” → “ข้อความตอบกลับจาก registry”
- 223: “ไฟล์สูญหาย” → “ไม่พบไฟล์”
- 238: “fixed file นำ command ที่ผิดออก” → “ไฟล์ fixed ตัด command ที่ไม่ถูกต้องออก”
- 276: “ถือว่าพลาด” → “ถือว่าไม่สำเร็จ”
- 299: “ให้ apply” → “ให้นำ manifest ไปใช้ด้วย `apply`”
- 300: “เรียกแล้ว” → “เมื่อเรียกใช้แล้ว”
- 302: “ชี้ field” → “ระบุ field”
- 305: “ไม่ได้แปลว่า” → “มิได้หมายความว่า”
- 312: “Service ปริศนา”; “exec ยิงจากใน Pod” → “Service สำหรับแบบฝึกหัด”; “ส่ง request จากภายใน Pod”
- 323: “แนวแก้” → “แนวทางแก้ไข”
- 349: “จังหวะ sampling” → “ช่วงเวลาที่เก็บตัวอย่าง”
- 350: “ดึง log” → “เรียกดู log”
- 352: “เว็บเข้าไม่ได้”; “ไล่” → “ไม่สามารถเข้าถึงเว็บ”; “ตรวจสอบตามลำดับ”
- 356: “ให้ลบ resource สร้าง broken ใหม่ และใช้ fixed แก้ไขทั้งชุด” → “ให้ลบทรัพยากรเดิม สร้างกรณีขัดข้องใหม่ แล้วนำไฟล์ fixed มาใช้แก้ไขทั้งชุด”
- 369: “รอหาย”; “Pod immutable สามตัว”; “ทุกตัว” → “รอจนถูกลบ”; “Pod แบบ immutable สามรายการ”; “ทุกรายการ”
- 371: “broken กลับมาครบถ้วน” → “กรณีขัดข้องถูกสร้างขึ้นครบถ้วน”
- 391: “ตอบจริง” → “ตอบสนองได้จริง”
- 406–407: “ตรวจว่า”; “ไฟล์แก้” → “ตรวจสอบว่า”; “ไฟล์ที่แก้ไขแล้ว”
- 427: “การรันจริง” → “การดำเนินการจริง”
- 430–431: “กลับมา”; “เปิด...แล้วเห็น” → “กลับสู่สภาพปกติ”; “เมื่อเปิด...จะแสดง...”

### LAB 021

- 21: “ส่งค่ารัน” → “ส่งค่ากำหนดการทำงาน”
- 27: “กัน Pod...” → “ไม่รวม Pod...”
- 28: “บอกค่าจอง/เพดาน · schedule/ความเสถียรคาดเดายาก” → “กำหนดปริมาณที่สำรองและขีดจำกัด มิฉะนั้นการจัดวางและความเสถียรจะคาดการณ์ได้ยาก”
- 30: “ไม่ได้อยู่... inject ตอนสร้าง” → “ไม่ได้จัดเก็บอยู่ใน image แต่ถูกนำเข้าเมื่อสร้าง Pod”
- 182: “HTTP error ว่าพลาด · `/info` วิ่งไป web · `/api` วิ่งตรง” → “HTTP error ระบุความล้มเหลว · `/info` ส่ง request ผ่าน web · `/api` ส่ง request ไปยัง api โดยตรง”
- 206: “Deployment log เลือก Pod ให้” → “Kubernetes เลือก Pod ของ Deployment สำหรับการอ่าน log”
- 221: “จับทุก Pod · สองบรรทัดท้าย” → “ครอบคลุมทุก Pod · แสดงสองบรรทัดสุดท้าย”
- 252: “ลบเฉพาะชื่อ/ทางเชื่อม...” → “ลบเฉพาะ Service ซึ่งให้ชื่อและเส้นทางเชื่อมต่อ โดยไม่ลบ db Pod”
- 269: “readiness cascade ฟื้นครบ” → “องค์ประกอบทั้งหมดกลับสู่สถานะ Ready ตามลำดับ”
- 291: “ตัด API...ทั้งหน้ากลายเป็น...ตรงชิ้นนั้น” → “เมื่อหยุด API หน้าเว็บจะแสดงสถานะขัดข้องในส่วนที่ขึ้นกับ API”
- 306: “การ์ดแดง...ไม่ได้แสดง...นานพอ; connection เดิมตอบต่อ...” → “การ์ดสถานะสีแดงอาจยังไม่ปรากฏทันที เนื่องจากการเชื่อมต่อเดิมอาจตอบสนองต่อได้ชั่วคราว”
- 315: “วาง object ถูกตำแหน่ง” → “กำหนด object ในตำแหน่งที่ถูกต้อง”
- 324: “ตรวจมุม browser”; “ทำ output สั้น” → “ตรวจสอบจากมุมมองของเบราว์เซอร์”; “แสดงผลลัพธ์แบบย่อ”
- 341–344: “กลับมา/ทั้งหน้าเป็น/ตัด dependency/retry” → “กลับสู่สภาพปกติ/ทั้งหน้าแสดง/ยกเลิก dependency/ลองใหม่”
- 359: “รอหาย” → “รอจนถูกลบ”
- 389: “ประกอบ/ซ่อม” → “ประกอบระบบและคืนสภาพ”
- 398, 410: “request เดิน” → “request ถูกส่งผ่าน”
- 400: “ตัดปลายทาง...ลูกโซ่” → “การนำปลายทางออกทำให้เกิดผลกระทบต่อเนื่อง”
- 411: “ย้อนกลับขึ้นมา” → “กลับสู่สถานะพร้อมทำงาน”
- 419: “ตามหลักฐาน” → “โดยอาศัยหลักฐาน”
- 421: “ตัดสี่ชิ้น” → “นำองค์ประกอบทั้งสี่ออกทีละรายการ”

### สไลด์

- หน้า 1: “ตาม request” → “ติดตาม request”
- หน้า 2: “ต่อ object ให้กลายเป็นระบบ” → “ประกอบ object เป็นระบบ”; “stateless ไม่เหมือน stateful” → “ความแตกต่างระหว่าง stateless และ stateful”; “สำรองให้พอ จำกัดการใช้รวม” ต้องแก้ตามข้อ C
- หน้า 3: “เห็นหลักฐานก่อนเชื่อ” → “พิจารณาหลักฐานก่อนสรุปผล”
- หน้า 4: “ก่อนคาด” → “ก่อนคาดการณ์ผล”
- หน้า 11, 22, 34, 46, 58, 68, 78: “การอธิบายเหตุและผลด้วยหลักฐานจากปฏิบัติการได้ แสดงว่าเข้าใจหัวข้อนี้” → “หากสามารถอธิบายเหตุและผลด้วยหลักฐานจากปฏิบัติการได้ ย่อมแสดงว่าเข้าใจหัวข้อนี้”
- หน้า 12, 23, 35, 47, 59, 69, 79: “คาดการณ์ก่อนกด Enter” → “คาดการณ์ก่อนดำเนินคำสั่ง”
- หน้า 14: “namespace แยกห้องทดลอง” → “namespace แยกสภาพแวดล้อมของปฏิบัติการ”
- หน้า 18–19: “web กับ api กำหนด scale เป็น 3 ได้” → “web และ api สามารถเพิ่มเป็น 3 replicas ได้”
- หน้า 20: “อยู่ข้ามอายุ Pod” → “คงอยู่หลังจาก Pod สิ้นสุด”
- หน้า 23: “ทำแบบเดียวกัน” → “ดำเนินการด้วยวิธีเดียวกัน”
- หน้า 25: “wait รอ...” → “รอจน...”
- หน้า 27: “DB Pod เปลี่ยน” → “DB Pod ถูกสร้างใหม่”
- หน้า 28: “class หากไม่มีผู้รับจะไม่ route” → “หากไม่มี controller ที่รองรับ ingress class ระบบจะไม่กำหนดเส้นทาง”
- หน้า 29: “web-svc เมื่อไม่มีอยู่จริง...” → “เมื่อ `web-svc` ไม่มีอยู่จริง Ingress จะไม่สามารถส่ง request ไปยัง backend”
- หน้า 32: “ปรับ proxy” → “กำหนดค่า proxy”
- หน้า 36: “Ingress รับ URL เดียวที่ port 80 แล้วใช้ path เลือก backend / ส่งไป Service web · /api ส่ง...” → “Ingress รับ request ผ่าน port 80 แล้วเลือก backend ตาม path โดย `/` ส่งไปยัง Service web และ `/api` ส่งไปยัง Service api”
- หน้า 37: “describe ตีความ rule เป็น backend” → “`describe` แสดงการจับคู่กฎกับ backend”
- หน้า 38: “สิ่งที่ไม่ต้องเปิด” → “องค์ประกอบที่ไม่จำเป็นต้องเปิด”
- หน้า 40: ประโยค RollingUpdate ต้องแก้ตามข้อ C
- หน้า 44: “ปนครึ่งทาง” → “ผสมกันระหว่างช่วงเปลี่ยนผ่าน”
- หน้า 45: “คุม” → “ควบคุม”
- หน้า 49: คำสั่งกับคำอธิบายขัดกันตามข้อ C
- หน้า 50: “refresh หน้าเว็บ”; “Pod ใหม่เข้าทีละตัว” → “โหลดหน้าเว็บใหม่”; “Pod ใหม่ถูกสร้างทีละรายการ”
- หน้า 52: “ลด Pod เก่า” → “ลดจำนวน Pod เดิม”
- หน้า 56: “CPU เกินถูก throttle” → “เมื่อใช้ CPU เกิน limit process จะถูก throttle”
- หน้า 60: “ระหว่างทำงาน · ขอเกิน node” → “บังคับใช้ระหว่างทำงาน · request เกินทรัพยากรที่ node มี”
- หน้า 66: “ไฟสี่แบบ” → “สถานะไฟสี่แบบ”
- หน้า 72: “tag ไม่มี” → “ไม่มี image tag ที่ระบุ”
- หน้า 76: “inject ตอนสร้าง” → “ถูกนำเข้าในระหว่างการสร้าง”
- หน้า 80: “เส้น request ต่อ Browser ถึง PostgreSQL” → “เส้นทาง request เชื่อม Browser ถึง PostgreSQL”
- หน้า 84: “ตัด Service db — อาการแดง” → “ลบ Service db — ปรากฏสถานะขัดข้อง”
- หน้า 86: “งานแบบครั้งเดียว”; “identity/volume ต่อ replica”; “จัดชุด/ปรับ”; “จับ manifest เสี่ยง” → “งานที่ทำงานครั้งเดียว”; “identity และ volume ประจำแต่ละ replica”; “จัดชุดและปรับ”; “ตรวจหา manifest ที่มีความเสี่ยง”

ไม่พบรายการแก้ไขใน `photos.md`

## C. ความหมายเชิงเทคนิคที่เปลี่ยนโดยไม่ตั้งใจ

| ตำแหน่ง | ก่อนแก้ภาษา | ปัจจุบัน | ปัญหา/ข้อความที่ควรเป็น |
|---|---|---|---|
| LAB 016:270 | “replication ที่ PostgreSQL เข้าใจ, StatefulSet ที่มี PVC แยก หรือ managed database” | “replication ที่ PostgreSQL รองรับ StatefulSet ที่มี PVC แยก หรือ managed database” | เครื่องหมายคั่นหาย ทำให้เสมือน PostgreSQL “รองรับ StatefulSet”; ควรเป็น “replication ที่ PostgreSQL รองรับร่วมกับ StatefulSet ซึ่งมี PVC แยก หรือใช้ managed database” |
| LAB 016:345 | “ลบทั้งห้อง” | “ลบ namespace ทั้งหมด” | อาจหมายถึงลบทุก namespace; ควรเป็น “ลบ namespace `lab016`” |
| LAB 018:42 | “refresh หน้าเว็บ” | “ปรับปรุงหน้าเว็บ” | จากการโหลดหน้าใหม่กลายเป็นการแก้ไขหน้าเว็บ; ควรเป็น “โหลดหน้าเว็บใหม่” |
| LAB 019:39 | “ชน memory limit” | “ใช้ memory ถึง limit” | `OOMKilled` เกิดเมื่อการจัดสรรเกินขีดจำกัด ไม่ใช่เพียงถึงค่า; ควรเป็น “ใช้ memory เกิน limit จนเกิด `OOMKilled`” |
| LAB 019:354 | “image ไม่ขึ้น” | “image ไม่ทำงาน” | ขยายความหมายไปถึง runtime failure; ตามสาเหตุในตารางควรเป็น “ไม่พบ image ที่กำหนด” |
| LAB 019:369 | “ลบทั้งห้อง” | “ลบ namespace ทั้งหมด” | กำกวมเช่นเดียวกับ LAB 016; ควรระบุ “ลบ namespace `lab019`” |
| LAB 020:223 | “log ปัจจุบันบอกไฟล์หาย” | “log ปัจจุบันระบุว่าไฟล์สูญหาย” | ระบบไม่ได้พิสูจน์ว่าไฟล์เคยมีแล้วสูญหาย เพียงเปิดไม่พบ; ควรเป็น “log ปัจจุบันระบุว่าไม่พบไฟล์” |
| สไลด์หน้า 2 | “จองให้พอ จำกัดไม่ให้กินหมด” | “สำรองให้พอ จำกัดการใช้รวม” | `limits` จำกัดแต่ละ container ไม่ใช่การใช้รวมของระบบ; ควรเป็น “สำรองตาม requests และจำกัดแต่ละ container ด้วย limits” |
| สไลด์หน้า 40 | “RollingUpdate ค่อย ๆ แทน Pod เก่า” | “RollingUpdate แทนที่ Pod ตามลำดับ เก่า” | การแทนคำทำให้ประโยคเสียรูป; ควรเป็น “RollingUpdate ค่อย ๆ แทนที่ Pod เดิมด้วย Pod ใหม่” |

ความคลาดเคลื่อนทางเทคนิคที่มีอยู่ก่อนรอบแก้ภาษา: สไลด์หน้า 49 อธิบายว่า “เปลี่ยน Pod template เป็น v2” แต่คำสั่งคือ `kubectl apply -f manifests/02-web-deployment.yaml` ซึ่งไฟล์ baseline ใช้ v1 ขณะที่ README ใช้ `kubectl set image ...:v2` การแก้จุดนี้ต้องเป็นรอบแก้เนื้อหา/โค้ดแยกต่างหาก เพราะเกณฑ์ D ห้ามเปลี่ยนคำสั่งในรอบภาษา

## D. สรุป 3 บรรทัด

ยังไม่พร้อมใช้สอนในสภาพปัจจุบัน เนื่องจากยังมีภาษาพูด/ประโยคแบบบันทึก และมีความคลาดเคลื่อนทางเทคนิคที่อาจทำให้ผู้เรียนตีความผิด  
ไฟล์ดีที่สุดคือ `slides_assets/photos/photos.md`; ไฟล์ที่ต้องปรับมากที่สุดคือสไลด์ HTML รองลงมาคือ LAB 018–020  
ก่อนใช้ต้องแก้รายการใน C ทั้งหมด แก้คำสั่งหน้า 49 ในรอบที่ได้รับอนุญาตให้เปลี่ยนโค้ด และทำศัพท์ desired/actual state, layer/ชั้น และถ้อยคำปฏิบัติการให้สม่ำเสมอทั้งชุด