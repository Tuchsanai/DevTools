# ผลตรวจเชิงลึก (yolo1 reviewer) แล็บ 013-017
## ข้ามแล็บ
- X1 FAIL: curl localhost:8080 ใน container ใช้ไม่ได้ (Ingress ที่ port 80; ~/.curlrc retry=5 ทำให้ค้าง ~15 วิ) — บรรทัด 013: 140,284,313 · 014: 156,178,286 · 015: 158,181,200,245,263,287,320,321 · 016: 158,184,189,269,297,334 · 017: 173-175,216,242,245,271,272,307,308
- X2 WARN: JSON /info ถูกย่อ/ตัด field ด้วยมือทั้งที่คำสั่งเป็น `| jq` (013: 149-153,291-292,323-324 · 014: 166,187,295-296 · 015: 165-166,189,272,297-298,331-332 · 016: 142,168,202,283,310-311,350-352 · 017: 183-185,257,324) → ใช้ `jq -c '{pod,api,db}'` ในคำสั่งหรือวางผลจริง
- X3 NIT: ปี 2569 (013-015) vs 2026 (016-017)
## LAB 013 — ต้องแก้
- L116-141 FAIL: ไม่แสดง YAML ของ 01-pvc.yaml และส่วน volumeMounts/volumes/PGDATA ของ 02-db.yaml พร้อมอธิบายทีละ field → แทรก ```yaml + โน้ตต่อ field
- L17-33 WARN: ไม่มีตารางเทียบ named volume ↔ PVC/PV/StorageClass และ emptyDir ↔ PVC → เพิ่ม
- L185-207 WARN: ไม่มี `kubectl describe pvc db-data -n lab013 | grep -E 'Used By|Finalizers'` (runlog มี Used By: db-7979ccf8db-d7p49) → เพิ่ม
- L260 WARN: 📝 "apply db ยืนยัน template ที่มี readiness probe" เป็นเศษจากแก้ไฟล์กลางทาง → ตัดคำสั่ง apply 02-db.yaml ซ้ำหรือแก้คำอธิบาย
- L245-250 NIT: ไม่มี "ต่างกันได้" · L276 NIT: ขยาย PVC ถูกปฏิเสธตอน apply ไม่ใช่ Event · L301 NIT: แถว "ไม่มี db readiness probe" ล้าสมัย · L285-293 NIT: get events ไม่มีบรรทัด event ใน Expected
## LAB 014 — ต้องแก้
- L143-144 FAIL: ผลปลอม `{"status":"ok","pod":"api-7cf4f8dcc-pj8kz"}` / `{"status":"ready",...,"pod":"api-7cf4f8dcc-s5sp9"}` ไม่มีใน runlog (ของจริง btrpq/w4bh7) → รันใหม่หรือใช้บรรทัดจริง
- L109-127 FAIL: ไม่แสดง/อธิบาย readinessProbe/livenessProbe (httpGet path/port, periodSeconds, timeoutSeconds, failureThreshold, initialDelaySeconds) → แทรก ```yaml
- L151-167 WARN: หลัง scale db=0 ต้องรอ ~15 วิก่อน READY 0/1 → sleep 15 หรือ -w · L194-207 WARN: liveness 10s×3 → restart หลัง ~30 วิ → sleep 35 · L174-188 WARN: เพิ่ม `kubectl wait --for=condition=ready pod -l app=api` ก่อน get pods
- L202-206 vs 140-144 NIT: เล่าจากสองรอบ (7cf4f8dcc vs 68c74dcd8c) → ระบุหรือจับใหม่ · L320 NIT: `logs --previous` อยู่ในตารางแต่ไม่มีขั้นใช้ → เพิ่มในขั้น 6 พร้อมผลจริง · L22-25 NIT: ตารางไม่มีคอลัมน์ Compose · L119-127 NIT: apply ตัด 9 บรรทัดไม่บอก
## LAB 015 — ผ่าน (แก้เฉพาะ X1/X2 + WARN ต่อไปนี้)
- L112-128 WARN: sed -n 1,120p แต่ Expected แสดงแค่ ls; ไม่แสดง YAML (envFrom.configMapRef, NODE_NAME fieldRef) → cat + ```yaml + โน้ต
- L196-214 WARN: wget api:8000/health ทันทีหลังสร้าง Service คืนอาจล้ม (negative DNS cache) → "ถ้า bad address รอ 5-10 วิแล้วรันซ้ำ" + แก้ 📝
- L240-255 WARN: get pods ทันทีหลัง rollout restart เห็น ContainerCreating → sleep 5
- L75 NIT: crictl วาง 4 บรรทัดจริง · L167 NIT: วางข้อความเต็ม ([Errno -5] …) · L24-25 NIT: เว้นวรรค "สอง Podและ"
## LAB 016 — ต้องแก้
- L58-62,84-87 FAIL: ไม่มี `docker exec -it devtools-k8s bash` → เพิ่ม (P2)
- L259 FAIL: "ผลนี้เบี่ยงจาก outline ที่คาด…" หลุด → "ต่างจากที่หลายคนคาด (CrashLoopBackOff/Pending)"
- L238-257 WARN: บรรทัด 86jqs RESTARTS 1 (14s ago) จับ ~40 วิหลัง; ทันทีหลัง scale=1 คือ RESTARTS 0 + n6cvl Terminating → เพิ่ม rollout status + sleep 30 หรือย้ายการสังเกตไปขั้น 7
- L137-144 WARN: รายการ Pod ต่อขึ้นจากช่วงแก้ api (AGE ไม่สอดคล้อง, pod/ prefix เติมเอง) → วางผลจาก Clean Re-run
- L155-171 WARN: jq '.[-1]' ผลจริง 10 field → ใช้ `jq '.[-1] | {id,title,status}'` · L152-160 WARN: ใช้ Ingress /api โดยไม่อธิบาย (เป็นของ LAB 17) → เพิ่ม 1 ประโยค หรือใช้ wget ผ่าน api:8000 · L264-284 WARN: sleep 8 หลัง rollout restart · L129,334 NIT: loop รอ 200 · L61,70-78 NIT: get nodes -o wide แต่ผลไม่ wide · L247 NIT: "พังเชิงความถูกต้อง" → "พังแบบเงียบ"
## LAB 017 — ต้องแก้
- L55-61,82-86 FAIL: ไม่มี docker exec -it → เพิ่ม (P2)
- L214-233 WARN: curl --retry 0 ทันทีหลัง apply ingress-bad อาจยัง 200 → sleep 3/loop · L239-246 WARN: หลัง patch เช่นกัน · L222 NIT: อธิบาย --retry 0 (เพราะ ~/.curlrc retry 5)
- L183-185,257,324 WARN: /info และ /api/dashboard ถูกตัด field → วางจริงหรือ jq -c '{pod,api,db}'
- L15-28 WARN: ไม่กล่าวถึง TLS (outline ให้ 2-3 บรรทัด: tls secretName, 8443) → เพิ่ม
- L204-208 NIT: log ถูกละ "..." → แสดง 1 บรรทัดเต็ม · L264,356 NIT: เว้นวรรค · L96 NIT: git pull ไม่มี code block/Expected
