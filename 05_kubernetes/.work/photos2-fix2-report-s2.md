แก้แล้วใน [Kubernetes_Session2_Slides.html](/root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/Kubernetes_Session2_Slides.html)

| หน้า | KEY | วิธีที่ใช้ | ยืนยันจาก PNG |
|---:|---|---|---|
| 10 | `lab008_scene` | gen ใหม่ | เห็นสายหลุด ช่องแร็คว่าง และเครื่องบนรถเข็นด้านขวาชัด |
| 21 | `lab009_scene` | gen ใหม่ | เห็นกรง สายภายในสีน้ำเงิน และสายส้มออกนอกกรงด้านขวา |
| 31 | `lab010_scene` | ภาพเดิม ไม่ต้องปรับ | การ์ดสีเขียวและมืออยู่ด้านขวาที่มองเห็นได้ |
| 42 | `lab011_scene` | gen ใหม่ | กุญแจฮาร์ดแวร์ จุดเสียบ และไฟเขียวอยู่ด้านขวาชัด |
| 53 | `lab012_scene` | gen ใหม่ | เห็นเครื่องใหม่ช่องไดรฟ์ว่างและเครื่องเก่าที่มีไดรฟ์บนรถเข็น |
| 65 | `lab013_scene` | ภาพเดิม ไม่ต้องปรับ | storage และสายเชื่อมอยู่ในส่วนขวาที่มองเห็น |
| 76 | `lab014_scene` | gen ใหม่ | ไฟเหลืองของเครื่องที่สามและพอร์ตดับมองเห็นชัดเทียบกับไฟเขียว |

`object-position` ใช้ไม่ได้ผล เพราะภาพและสไลด์เป็น 16:9 เท่ากัน จึงไม่มีพื้นที่ crop ให้เลื่อน ผมจึงใช้ skill `imagegen` สร้างใหม่เฉพาะ 5 ภาพที่ไม่ผ่าน แล้วแก้ alt และ `photos.md` ให้ตรงภาพใหม่

ภาพใหม่ทั้งหมดเป็น JPEG q88 ขนาด 1280×720, 130–187 KB และความสว่าง 0.411–0.457 ต้นฉบับเก็บไว้ที่ [.work/photos-src2-s2](/root/workspace/DevTools/05_kubernetes/.work/photos-src2-s2)

ผล `check-slides`: ผ่าน — 88 หน้า, 48 ภาพ, ไม่มีภาพเสีย, ไม่มี alt ขาด, ไม่มี external reference/import และไม่มี page error; ตรวจ PNG ครบทั้งหน้าปกและหน้าคั่นทั้ง 7 แล้ว.