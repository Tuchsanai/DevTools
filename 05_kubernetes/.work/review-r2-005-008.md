# ผลตรวจเชิงลึก (yolo1 reviewer) แล็บ 005-008 — ต้องแก้ทั้ง 4
## ข้ามแล็บ (นโยบายทั้งชุด)
- S1 FAIL: `curl http://localhost:8080/...` ใน container devtools-k8s ไม่ตอบ — ingress-nginx ฟัง port 80 ใน container (kind hostPort 80) ส่วน 8080 มีเฉพาะฝั่ง host (-p 8080:80) runlog ทุกไฟล์ใช้ localhost:80 → ใน code block ให้ใช้ `http://localhost/info` (port 80) และเขียนกำกับว่าเบราว์เซอร์บนเครื่องของคุณเปิด `http://localhost:8080` (กระทบ 006/007/008 และแล็บอื่นทั้งชุด)
- S2 FAIL: 005/006 ไม่มีขั้น `docker exec -it devtools-k8s bash` ก่อนส่วนที่ 1 → นักศึกษาจะ clone/รัน kubectl บน host (ทำแบบ 007)
## LAB 005
- L55-66 FAIL (S2) · L59-61 WARN container ID ไม่มีใน runlog + "รอบตรวจรับใช้ชื่อแยก" หลุด → เขียน "ได้ container ID 64 ตัว (ต่างกันทุกเครื่อง)" · L74-75 NIT grep k8s-lab แสดง 2/4 บรรทัด
- L164-170 WARN Expected output แบบสรุป (`version=v1: web-a web-c`) แต่งขึ้น → วางตาราง NAME/READY/STATUS จริง 4 ชุด (runlog 50-71)
- L211-219 WARN `logs pod/web-a` ไม่เคยรัน (runlog รัน web-b L73) และตัดเหลือ 2/5 บรรทัด → รันจริงหรือย้ายไปก่อนลบ web-b/c
- L236,238 NIT ตัดข้อความจริง ("deleted from ingress-nginx namespace", FailedScheduling เต็มบรรทัด) · L249-258 WARN อ้าง reload error ไม่มีหลักฐาน (log Pod ใหม่ L253-260 ไม่มี error) → ตัดประโยค+แถวตาราง 289 แสดง log จริง
- L199-203,276 NIT AGE ถูกแก้ (1s/3s → 29s/30s/46s) → copy จริง · L122 NIT ไม่มีคำอธิบาย field ของ manifest → แสดง spec web-a + อธิบาย · L206-220 NIT ขาดสะพานไป selector ของ RS/Service (outline ข้อ 7) · L175-185 NIT บอก Ctrl+C ใน 📝
## LAB 006
- L142,177,294,330 FAIL (S1 port 80) · L52-80 FAIL (S2 + เทอร์มินัลที่ 2 ต้อง docker exec อีกครั้ง)
- L140-154 WARN ต้องรอ nginx sync ก่อน loop (runlog 67) → `until curl -sf localhost/info >/dev/null; do sleep 1; done` + แถวตารางแก้ปัญหา
- L149-153 WARN loop 12 รอบแสดง 5 และค่าแถว 5 ถูกเปลี่ยน (runlog 88 = web-cztmv) → วาง 12 บรรทัดจริง L84-95
- L155-157,187,266-268 WARN screenshot ทั้ง 4 มาจากคนละรอบกับข้อความ (ภาพ dhgdn/g4tks/s8hwd/2l752 vs ตาราง cztmv/fmg6f/hx2rj/f48br/zzgh7) และ L268 อ้าง "ขณะเดียวกัน" ทั้งที่ไม่ใช่ → เขียนกำกับ "ภาพจากอีกรอบ ชื่อ Pod จึงต่างจากตาราง" และเลิกอ้างขณะเดียวกัน (หรือรันใหม่ให้รอบเดียวกัน)
- L166-173 WARN ตัดบรรทัด `web-cztmv 0/1 Error` (runlog 123-127) → คงไว้ + 📝 อธิบาย Error = SIGTERM ไม่ใช่ความผิดปกติ
- L176-185 FAIL output แต่ง: ตัดเหลือ Pod เดียว แก้ AGE 13s→1s; curl แสดง web-f48br บรรทัดเดียวแต่ runlog L144-148 = web-hx2rj หลายบรรทัด → ใช้ `jq -c` และค่าจริง (เหมือนกัน L302, L342)
- L203-219 WARN ไม่มี `get rs,pods` หลัง scale 5 และ scale 1 (runlog 215-245) → แทรกพร้อมผลจริง · L227 NIT "รอบตรวจรับ" หลุด + ตัด " from lab006 namespace"
- L253-268 WARN ไม่ได้ใช้หลักฐาน loop 15 รอบ v1/v2 สลับ (runlog 338-353) → เพิ่ม loop + ผลจริง · L257-264 NIT Pod v2 ยังมี label version=v1 (runlog 317-321) น่ากล่าวถึง; เรียงตามจริง
- L273 WARN คำสั่ง jq ยาวเกินและไม่ได้รัน (runlog 362 ลบด้วยชื่อ) → ลบด้วยชื่อ · L293,301 WARN custom-columns OWNER ประกอบขึ้น (runlog 47-51 เป็น Pod ชุดแรก) → ใช้ผลจริง
- NIT: บรรทัดว่างก่อนหัวข้อ/รูป (L8-9,16-17,31-32,47-48,50-51)
## LAB 007
- L159,188,240,290,327 FAIL (S1 port 80)
- L95-102 WARN manifest มี field ที่ยังไม่สอน (readinessProbe, env fieldRef POD_NAME/NODE_NAME, ports.name http + targetPort http) และ README ไม่แสดง YAML/ไม่อธิบาย → แสดง YAML ส่วนสำคัญ + 📝 ต่อ field หรือถอด readinessProbe/NODE_NAME (006/008 ไม่มี → ไม่สม่ำเสมอ)
- L129,149 NIT ไม่ wait หลัง scale กลับ 3 ก่อน apply v2 (runlog 55 มี wait) · L156,168 WARN ตัดบรรทัด progress "Waiting for deployment … 1 out of 3" (runlog 60-73) และไม่มี get pods -w (outline ข้อ 3) · L157,169-171 NIT แสดงตาราง Pod ด้วย (runlog 79-83)
- L184,196,261,271 WARN `rollout undo` พิมพ์ Warning last-applied-configuration ทุกครั้ง (runlog 103,361) → ใส่ใน Expected + อธิบาย 1 ประโยค
- L213-231 NIT ตัด "Found 3 pods, using pod/…" และ Events 10→2 บรรทัดไม่มี … · L239,250-255 WARN ไม่มีตาราง RS (runlog 201-204 web-6b87887f58 1/1/0 = หลักฐานสำคัญ) · L234-258 NIT เพิ่ม describe pod v3 | tail -6 (runlog 210-268 "pull access denied")
- L289,301-302 WARN rollout history ตอนท้ายไม่มีใน runlog; ค่าจริงมี revision 4 (v3) CHANGE-CAUSE ค้าง → รันจริง+อธิบาย · L304 NIT event scale-down ไม่มีใน runlog · L348-364 NIT pgrep port-forward เป็น noise (ไม่ใช้ในแล็บนี้) ตัด · L41-43 NIT เลขข้อข้าม 3 · L83 NIT "เข้" → "เข้า" · L381-382 NIT บรรทัดว่างก่อน จำภาพเดียว · L390 NIT คำถามข้อ 3 ให้ใช้ตาม outline (compose up --build)
## LAB 008
- L136,158,244,277,311 FAIL (S1 port 80)
- L133,144 NIT until wget พิมพ์ refused 1-2 ครั้งก่อนสำเร็จ (runlog 37-39) ใส่ 1 บรรทัดใน Expected · L134-136,145-147 WARN curl /info ทันทีหลัง rollout ได้ HTML 404 → jq parse error (runlog 48-50) → เพิ่ม sleep 3/loop รอ 200 + ย้ายแถว 297 มาเป็นหมายเหตุ
- L185,200-205 WARN ผล loop ถูกแต่ง (runlog 176-216 ค่าที่ 6 = w8d8z ไม่ใช่ 8twmk) → seq 1 10 วาง 10 บรรทัดจริง
- L217,228-230 NIT ตัด condition met 2 บรรทัด; `get endpoints` มี Warning deprecated ทุกครั้ง (runlog 221/230) → ใส่และย้ายหมายเหตุ 210 มาครั้งแรก (L182)
- L219,231-232 WARN nslookup จริงมี NXDOMAIN 3-4 บรรทัด + exit code 1 (runlog 141-157) → แสดงจริง + 📝 BusyBox ลอง search suffix
- L220,233-234 WARN logs api --tail=8 จริงคือ traceback psycopg [Errno -5] + GET /ready 503 (runlog 299-308) → วางจริง + อธิบายว่า 503 เพราะยังไม่มี db
- L242-247 NIT ต้อง sleep 2 หลัง patch ก่อน get endpoints (runlog 258, 285-291)
- L285-289 WARN บล็อกประกอบขึ้น (wget /health s862d ไม่มีใน runlog; JSON ตัด key error; เติม pod) → ใช้ JSON จริง runlog 175/313
- L308,324,330 WARN apply 01 แล้ว 05 ในคำสั่งเดียว → created+configured object เดียวกัน; JSON ตัด "error" (runlog 348/374) → อธิบายใน 📝 หรือแยกไฟล์ door; ใช้ JSON จริง
- L131-132,155,215-216 NIT ตัวแปร $API_POD หายเมื่อเปิด terminal ใหม่ → หมายเหตุ · L337 NIT pgrep port-forward noise · L28 NIT ประโยคอ่านไม่ลื่น
