งาน: สร้าง "ภาพถ่ายสมจริง" ชุดใหม่ 15 ภาพสำหรับสไลด์ครั้งที่ 2 ของชุดสอน Kubernetes แทนชุดเดิมทั้งหมด แล้วฝังลงสไลด์ให้ตรงข้อความ
เหตุผล (ผู้สอนสั่ง): ภาพชุดเดิมเป็นอุปมาสำนักงาน/พัสดุ/ตู้เอกสาร ไม่สื่อถึงเนื้อหา Kubernetes เลย ชุดใหม่ต้องสื่อเนื้อหาของแต่ละ section โดยตรง

อ่านก่อน (ต้องทำตามเป๊ะ): /root/workspace/DevTools/05_kubernetes/.work/photo-spec-v2.md — หลักการ 6 ข้อ + ตาราง "ครั้งที่ 2" ระบุองค์ประกอบของทั้ง 15 ภาพ
บริบทเนื้อหา: /root/workspace/DevTools/05_kubernetes/.work/lab-outline.md เฉพาะครั้งที่ 2

ขั้นที่ 1 — สำรองของเดิม: ย้ายไฟล์ .jpg ทั้ง 15 ไฟล์ใน /root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/slides_assets/photos/ ไป /root/workspace/DevTools/05_kubernetes/.work/photos-v1-backup/s2/ (สร้างโฟลเดอร์) — photos.md ให้เขียนทับใหม่ทีหลัง
ขั้นที่ 2 — สร้างภาพด้วย skill imagegen (built-in image_gen) ทีละภาพตามตาราง ชื่อไฟล์ตามตารางเป๊ะ (นามสกุล .jpg)
  · ต้นฉบับความละเอียดเต็มเก็บที่ /root/workspace/DevTools/05_kubernetes/.work/photos-src2-s2/ (PNG)
  · ไฟล์ส่งมอบ: ย่อเป็น 1280×720 (crop 16:9 ถ้าจำเป็น) บันทึก JPEG quality 88 ≤ 350 KB ด้วย Pillow
  · เปิดดูทุกภาพจริง ๆ (view_image) แล้วตรวจ: (ก) สื่อหัวข้อตามตารางใน 3 วินาทีไหม (ข) ไม่มีตัวอักษร/ตัวเลข/โลโก้/ธง/ใบหน้าชัด (ค) ไม่มีวัตถุผิดรูป (มือ 6 นิ้ว สายลอย) (ง) ความสว่าง grayscale mean/255 ≥ 0.40 (วัดด้วย Pillow ImageStat แนบตัวเลข)
    ถ้าไม่ผ่านข้อใดให้สร้างใหม่ (สูงสุด 3 รอบต่อภาพ) แล้วเลือกภาพที่ดีที่สุด
ขั้นที่ 3 — เขียน /root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/slides_assets/photos/photos.md ใหม่ทั้งไฟล์ หนึ่งบรรทัดต่อภาพ: ไฟล์ | ใช้ในหัวข้อ/แล็บไหน | alt ภาษาไทยที่บรรยาย "ภาพใหม่" (1 ประโยค ระบุองค์ประกอบที่เห็นและสื่อแนวคิดอะไร)
ขั้นที่ 4 — ฝังลงสไลด์ /root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/Kubernetes_Session2_Slides.html (ไฟล์ใหญ่ ~5 MB — ห้ามเปิดอ่านทั้งไฟล์ ให้ใช้สคริปต์ Python แก้แบบ regex/replace):
  · ภาพฝังใน window.ASSETS = {"KEY": "data:image/jpeg;base64,..."} โดย KEY = ชื่อไฟล์แทน - ด้วย _ ตัด .jpg (เช่น cover-s2.jpg → cover_s2, lab00X-scene → lab00X_scene, concept-s2-1 → concept_s2_1) — แทน base64 ของทั้ง 15 KEY ด้วยภาพใหม่
  · แก้ alt ของ <img data-a="KEY" ...> ทุกภาพให้ตรงภาพใหม่ (ใช้ alt จาก photos.md)
  · หน้าแนวคิด (ที่ใช้ concept_s2_K): แก้ข้อความใน <h2>ใช้ภาพจริงเป็นอุปมา — <em>…</em></h2> และประโยคหลัง <b>มองภาพนี้:</b> ให้เล่า "ภาพใหม่" และโยงกับแนวคิดของแล็บนั้น (สั้น 1 ประโยค ห้ามยาวจนล้นการ์ด) ห้ามแตะหน้าอื่น/โครงสร้าง/CSS/JS
  · ห้ามเปลี่ยนจำนวนหน้า · ไฟล์ต้องยังเปิด file:// ได้ ไม่มี external ref
ขั้นที่ 5 — ตรวจ: node /root/workspace/DevTools/05_kubernetes/.work/tools/check-slides.js /root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/Kubernetes_Session2_Slides.html --shots /root/workspace/DevTools/05_kubernetes/.work/slide-shots2-s2
  ต้องได้ brokenImages 0 · externalRefs 0 · pageErrors 0 · จำนวนหน้าเท่าเดิม แล้วเปิดดู PNG ของหน้าปก/หน้าคั่นบท/หน้าแนวคิดที่ใช้ภาพใหม่ ว่าภาพขึ้นจริง ข้อความไม่ล้น

กติกาเข้ม: ห้ามสร้างไฟล์อื่นในโฟลเดอร์ส่งมอบนอกจาก .jpg และ photos.md · ห้ามแตะโฟลเดอร์ครั้งอื่น · ห้ามใช้ภาพจากอินเทอร์เน็ต/สต็อก ต้อง gen เอง · ห้ามใส่โลโก้ Kubernetes จริง (ใช้ภาพท่าเรือ/พวงมาลัยเรือได้ แต่ไม่ใช่โลโก้)
รายงานกลับ (คำตอบสุดท้ายถูกเก็บเป็นไฟล์ — ห้ามสร้างไฟล์รายงานเพิ่ม): ตาราง ไฟล์ | KB | ความสว่าง | จำนวนรอบที่ gen | สื่อหัวข้ออะไร (1 ประโยค) · ผล check-slides (หน้า/broken/ext) · หน้าที่แก้ข้อความ · ปัญหาที่แก้ไม่ได้ (ถ้ามี)
