คุณคือผู้ตรวจ "หน้ากายวิภาค YAML" ในสไลด์ครั้งที่ 2 — ผู้สอนสั่งให้ cyolo1 ตรวจหน้า YAML แล้ว **ส่ง feedback** เป็นรายงานต่อหน้า (ตรวจก่อน แล้วจึงแก้)

ไฟล์สไลด์: /root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/Kubernetes_Session2_Slides.html · แหล่งความจริง: /root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/YAML_Guide.md และไฟล์ manifest จริงในโฟลเดอร์แล็บ · สเปก: /root/workspace/DevTools/05_kubernetes/.work/yaml-teaching-spec.md ข้อ 2, 5, 6 · cluster สำหรับ `kubectl explain`: `docker exec devtools-k8s-verify kubectl explain <kind>.<path>` (ห้ามสร้าง/ลบ container)

ขั้นที่ 1 — render ทุกหน้า: node /root/workspace/DevTools/05_kubernetes/.work/tools/check-slides.js /root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/Kubernetes_Session2_Slides.html --shots /root/workspace/DevTools/05_kubernetes/.work/slide-shots-s2  แล้ว **เปิดดู PNG ของทุกหน้าที่มีคำว่า "กายวิภาค YAML" / "YAML" ในหัวเรื่อง** (หา index จาก HTML) ให้คะแนน 1-5 ต่อหน้าใน 5 หมวด + เขียน feedback ชี้จุด:
  1. ความถูกต้องของ YAML — บล็อกบนหน้าต้องตรงกับไฟล์จริง/YAML_Guide (ชื่อ field, ค่า, ย่อหน้า) ไม่แต่ง · field ที่ไฮไลต์เป็น field สำคัญของ object นั้นจริง
  2. ความถูกต้องของคำอธิบาย — การ์ดอธิบาย field/default/ผลเมื่อเปลี่ยน ถูกต้องตาม Kubernetes v1.36 (ตรวจ kubectl explain) · การ์ด "ผิดบ่อย" เป็นข้อผิดพลาดที่เกิดจริง
  3. ความครบถ้วน — ครั้งนี้ต้องมีหน้า YAML ของ object เหล่านี้ครบ: โครง object (ทบทวน) · Deployment (ทบทวน) · Service · Ingress ประตู (ทบทวน) · ConfigMap · Secret · Volume/PVC · Probes · หน้าอยู่ก่อนสไลด์แล็บแรกที่ใช้ object นั้น · มี footer ชี้ YAML_Guide.md#anchor ที่มีจริง
  4. ตัวอักษร/อ่านออก — โค้ดใน pre.code ≥ 18px ที่ 1280×720 ไม่ล้นกรอบ ไม่ถูกตัด ไม่เกิน ~18 บรรทัด · การ์ดไม่ล้น · ภาษาไทยตัดคำถูก
  5. เลย์เอาต์/ความสวยงาม — ซ้ายโค้ด/ขวาการ์ดสมดุล ไม่มีพื้นที่ว่างเกิน 15% · สไตล์เดียวกับหน้าอื่นของไฟล์ (คลาสต้นแบบ) · ไฮไลต์ span สม่ำเสมอ
ขั้นที่ 2 — เขียนรายงาน feedback (ก่อนแก้): ตาราง หน้า | object | คะแนน 5 หมวด | ปัญหา/ข้อเสนอแก้ · รายการ object ที่ยังไม่มีหน้า
ขั้นที่ 3 — แก้ทุกหน้าที่หมวดใดต่ำกว่า 4 (แก้ HTML โดยตรง · เพิ่มหน้าที่ขาด) · render ซ้ำ เปิดดู · รัน check-slides.js ต้องผ่าน (brokenImages ว่าง · externalRefs ว่าง · nav ครบ · labFoldersReferenced 7 · ≤ 15 MB · ≤ 90 หน้า)

รายงานกลับ (คำตอบสุดท้ายถูกเก็บเป็นไฟล์ — ห้ามสร้างไฟล์รายงานเพิ่ม): **ส่วน A: feedback ก่อนแก้** (ตารางขั้นที่ 2 ครบทุกหน้า YAML) · **ส่วน B: สิ่งที่แก้** ต่อหน้า + คะแนนหลังแก้ · ส่วน C: ผล check-slides.js และ object ที่ยังขาด/ข้อจำกัด
