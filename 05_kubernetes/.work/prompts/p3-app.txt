อ่านให้ครบก่อนเริ่ม (ห้ามเริ่มเขียนโค้ดก่อนอ่านจบ):
- ข้อกำหนดแอป (ต้องทำครบทุกข้อ) : /root/workspace/DevTools/05_kubernetes/.work/app-requirements.md
- spec ส่วนที่ 2.5 และส่วนที่ 8 : /root/workspace/DevTools/05_kubernetes/PROMPT_Kubernetes_Course_Spec.txt
- กติกาสภาพแวดล้อม : /root/workspace/DevTools/05_kubernetes/.work/worker-env.md   (ของคุณ: JOB=app · P=20000)
- แกนเนื้อหาแล็บ (เพื่อเข้าใจว่า feature ไหนถูกใช้สอนอย่างไร) : /root/workspace/DevTools/05_kubernetes/.work/lab-outline.md
- ต้นแบบที่ต้องยกโครงมาปรับ : /root/workspace/DevTools/02_Docker/03_Fullstack_App_Example/002_LAB_Fullstack_Compose
  (web = Next.js + Tailwind, api = FastAPI, db = PostgreSQL + initdb) — ศึกษาโค้ดเดิมก่อน แล้วปรับ อย่าเขียนใหม่จากศูนย์

สร้างแอปตัวอย่างสำหรับสอน Kubernetes ลงที่ /root/workspace/DevTools/05_kubernetes/app/ ให้ครบตาม app-requirements.md ทุกหัวข้อ (0-6)

ต้องทำจริง ไม่ใช่แค่เขียนโค้ด:
- ทำทุกอย่างใน container ของตัวเอง (JOB=app, P=20000) ตาม worker-env.md — ทั้ง build image, ทดสอบ compose และทดสอบบน kind
  ภายใน container โค้ดอยู่ที่ ~/labwork/DevTools/05_kubernetes/app (bind mount — แก้ไฟล์บน host ได้เห็นทันที)
- build image ทั้ง 4 ให้ผ่านด้วย ./build-images.sh
- ทดสอบด้วย compose.yaml ให้ผ่านก่อน (web ที่ port 3000 ใน container = http://localhost:20030 บน host) แล้ว down ให้เรียบร้อย
- แล้วทดสอบบน kind ให้ครบทั้ง 10 ข้อในหัวข้อ 5 ของ app-requirements.md (Ingress = http://localhost:20080 บน host)
- screenshot จริงเก็บที่ /root/workspace/DevTools/05_kubernetes/.work/app-screens/ ด้วย shot.js (วิธีใช้ใน worker-env.md) อย่างน้อย: หน้าเว็บโหมดเดี่ยว · หน้าเว็บครบ 3 ชั้นมีข้อมูล · v1 กับ v2 · หลังเปลี่ยน ConfigMap · api ไม่พร้อมเมื่อ db ล่ม
- ทดสอบว่า scale หลาย instance แล้วชื่อ Pod บนการ์ดสถานะเปลี่ยนตามจริง
- ลบ container ทดสอบของตัวเอง (docker rm -f devtools-k8s-app) ก่อนจบงาน และยืนยันว่าไม่เหลือ

รายงานกลับ (คำตอบสุดท้ายของคุณจะถูกเก็บเป็นไฟล์ — ไม่ต้องสร้างไฟล์รายงานเอง):
ไฟล์ที่สร้าง · คำสั่ง build/run ที่ใช้ได้จริง · ผลการทดสอบทั้ง 10 ข้อทีละข้อพร้อมหลักฐาน (output จริงย่อ) · สิ่งที่ยังทำไม่ได้ (บอกตรง ๆ ห้ามกลบ)
