งาน: แก้ README ของแล็บ **015-021** (ครั้งที่ 3) ตามผลตรวจเชิงลึกของ yolo1 (รอบ 2) — เน้น "ความถูกต้องของผลรันจริง" และ "นักศึกษาทำตามได้โดยไม่ติด"

อ่านก่อน (ยึดเป็นหลัก · ไฟล์อยู่ใน /root/workspace/DevTools/05_kubernetes/.work/): review-r2-013-017.md (เฉพาะ 015-017) · review-r2-018-021.md — แก้เฉพาะแล็บ 015-021 ห้ามแตะแล็บอื่น (job อื่นกำลังแก้)
เทมเพลต: /root/workspace/DevTools/05_kubernetes/PROMPT_Kubernetes_Course_Spec.txt ส่วนที่ 0 และ 4 · runlog ดิบ: /root/workspace/DevTools/05_kubernetes/.work/runlogs/labNNN.txt · linter: python3 /root/workspace/DevTools/05_kubernetes/.work/tools/check-readme.py <dir>...

**นโยบายทั้งชุด (ต้องบังคับใช้กับทั้ง 21 แล็บ ไม่ใช่เฉพาะที่ reviewer ชี้):**
P1. คำสั่งที่รัน "ใน container devtools-k8s" ต้องเรียก Ingress ที่ **port 80**: `curl -s http://localhost/info` (ไม่ใช่ :8080 — 8080 มีเฉพาะฝั่งเครื่องหลักที่ map -p 8080:80) · ทุกที่ที่บอกให้ "เปิดเบราว์เซอร์" คงเป็น `http://localhost:8080` (Ingress) หรือ `http://localhost:3000` (port-forward) · เขียนกำกับครั้งแรกของแต่ละแล็บว่า "ในเทอร์มินัลของเครื่องเรียนใช้ localhost (พอร์ต 80) ส่วนเบราว์เซอร์บนเครื่องของคุณใช้ localhost:8080" · ตรวจด้วย grep ว่าไม่เหลือ `curl` ที่ชี้ `localhost:8080` ในทุก README
P2. ส่วน "## 0. เตรียมเครื่องเรียน" ของทุกแล็บต้องมีลำดับเดียวกัน: (1) `docker start devtools-k8s || docker run -d --name devtools-k8s --privileged --shm-size=1g --ulimit nofile=65536:65536 -p 2299:22 -p 8080:80 -p 8443:443 -p 3000:3000 -p 9090:9090 tuchsanai/devtools-k8s:2569_1` (2) `docker exec -it devtools-k8s bash` — เข้าเครื่องเรียน (ทุกคำสั่งหลังจากนี้พิมพ์ในเครื่องเรียน) (3) `k8s-bootstrap` (สคริปต์ข้ามเองถ้ามี cluster แล้ว) (4) `kubectl get nodes` (5) `docker exec devtools-control-plane crictl images | grep k8s-lab` (ถ้าว่าง → ทำขั้น build/load ใน README ระดับชุด/แล็บ 001) · ทุกที่ที่ต้องใช้เทอร์มินัลที่สอง ให้บอก "เปิดหน้าต่างใหม่บนเครื่องหลักแล้ว `docker exec -it devtools-k8s bash` อีกครั้ง"
P3. Expected output ต้องเป็นผลจริง: ห้ามพิมพ์ใหม่/เรียงใหม่/แก้ตัวเลข AGE/ตัดคีย์ JSON — ถ้าย่อให้ตัดทั้งแถวและใส่ `…` พร้อมหมายเหตุ "(ตัดบางแถว/บางคอลัมน์)" · บรรทัด Warning/deprecated/NXDOMAIN/refused ที่นักศึกษาจะเห็นจริง ต้องแสดงและอธิบาย 1 ประโยค ไม่ซ่อน
P4. ทุกจุดที่ต้องรอ (Ingress sync หลัง apply · rollout · postgres initdb · readiness) ให้ใส่คำสั่งรอที่ชัด (`kubectl rollout status …`, `until curl -sf http://localhost/info >/dev/null; do sleep 1; done`, "รอ 5-10 วินาทีแล้วรันซ้ำ") แทนการปล่อยให้เจอ parse error/refused
P5. JSON จาก `/info`, `/ready`, `/api/...`: ถ้า README แสดงบรรทัดเดียว/เลือก field คำสั่งต้องเป็น `jq -c '{pod,version,theme,api,db}'` (หรือชุด field ที่แสดง) — ห้ามแสดงผลย่อของคำสั่ง `| jq` เฉย ๆ · ห้ามตัดคีย์ `error` ออก
P6. รูปแบบ markdown: บรรทัดว่างก่อนทุกบรรทัด `✅ **Expected output**` และก่อนทุกหัวข้อ/รูป (GFM lazy continuation จะกลืนเข้า blockquote) · บล็อก `> 💡` แยกจาก `> (ไฟล์ของแล็บนี้…)` ด้วยบรรทัดว่าง · ตรวจด้วย grep -B1 '^✅' ว่าบรรทัดก่อนหน้าว่าง
P7. YAML ที่เป็นแนวคิดของแล็บ (PVC ใน 013 · probe ใน 014 · Deployment ใน 007 · envFrom ใน 015) ต้องแสดงเป็น ```yaml จากไฟล์จริง (ไม่ใช่ข้อความไทยแทนโค้ด) พร้อม 📝 อธิบายทีละ field · field ที่ยังไม่สอน (readinessProbe/NODE_NAME ใน 007) ให้ถอดออกจาก manifest ถ้าไม่จำเป็น หรืออธิบายสั้น ๆ ว่า "จะเรียนในแล็บ 014"
P8. README ระดับครั้งของครั้งที่ 3 (03_Session*/README.md) ต้องใช้นโยบาย P1 ด้วย

กติกา:
1. แก้ทุก FAIL และ WARN ตามบรรทัดที่ระบุ · NIT แก้ถ้าไม่เสี่ยง
2. **Expected output ที่ถูกชี้ว่า "ไม่มีใน runlog/แต่งขึ้น/ประกอบขึ้น"**: ห้ามเดา ให้เลือกอย่างใดอย่างหนึ่ง (ก) แทนด้วยผลจริงจาก runlog ตามบรรทัดที่ reviewer ชี้ (ข) รันคำสั่งนั้นจริงใน container ของตัวเอง (JOB=fix2-s3 ตาม /root/workspace/DevTools/05_kubernetes/.work/worker-env.md: k8s-bootstrap → build/load image → apply manifest ของแล็บนั้นถึงขั้นที่เกี่ยว) แล้ว copy ผลจริง — รวบรวมคำสั่งที่ต้องรันจริงทั้งหมดก่อน แล้วรันทีเดียวต่อแล็บ · บันทึก transcript ต่อท้าย runlog ของแล็บนั้น · ลบ container ก่อนจบ (ค) ถ้าไม่จำเป็นต่อแนวคิด ให้ตัดคำสั่งนั้นออกทั้งสามส่วน
3. ผลรันที่ถูก "ตัดคอลัมน์/แถว/ข้อความท้าย" ให้เติมหมายเหตุ "(ตัดบางคอลัมน์/บางแถว/ข้อความท้าย)" หรือใส่ผลเต็มถ้าสั้น
4. คำที่รั่วจากกระบวนการภายใน ("outline", "ตรวจรับ", "รอบตรวจรับ", "isolated container", "worker") ให้ตัดหรือเรียบเรียงเป็นภาษาสำหรับนักศึกษา
5. เติมวิธีเปิด terminal ที่สอง (`docker exec -it devtools-k8s bash` อีกหน้าต่างจากเครื่องหลัก) ในทุกจุดที่ README บอกให้เปิด terminal ที่สอง
6. รูปแบบให้เหมือนกันทั้ง 21 แล็บ: หัวเรื่อง `# LAB n — ...` · footer ปีแบบเดียวกัน (ใช้ พ.ศ. 2569 หรือ ค.ศ. 2026 ให้เหมือนกันทั้งชุด — เลือก 2026) · รูปแบบคำถาม/แนวคำตอบ · 🧭 ต่อยอด มีลิงก์เมื่ออยู่ครั้งเดียวกัน
7. ห้ามแก้ manifest/ภาพ (ยกเว้น reviewer ชี้ว่า manifest ไม่ตรง README ให้แก้ README ให้ตรง manifest) · ห้ามเปลี่ยนประโยคแนวคิดหลัก · ทุกแล็บต้องยังผ่าน linter 0 FAIL และยาว 200-400 บรรทัด

รายงานกลับ (คำตอบสุดท้ายถูกเก็บเป็นไฟล์ — ห้ามสร้างไฟล์รายงานเพิ่ม): ตาราง แล็บ | รายการที่แก้ (อ้างบรรทัด) | วิธี (ก/ข/ค) | ยังไม่แก้และเหตุผล · ผล linter หลังแก้ · ยืนยันลบ container
