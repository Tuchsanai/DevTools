คุณคือผู้ตรวจ "screenshot ของแล็บ" ครั้งที่ 1 (ผู้สอนสั่งให้ cyolo1 ตรวจภาพทุกชนิด) — ภาพเหล่านี้ต้องมาจากการรันจริง ณ ขั้นตอนที่นักศึกษาทำ และต้องเป็นหลักฐานของแนวคิดในขั้นนั้น

ไฟล์: /root/workspace/DevTools/05_kubernetes/01_Session1_Kubernetes_Basics/<แล็บ>/images/*.png ของแล็บ 003-007 (001-002 ไม่มี UI) · บริบท: README.md ของแล็บนั้น (ดูว่าภาพถูกแทรกที่ขั้นไหน คำบรรยายว่าอะไร) และ /root/workspace/DevTools/05_kubernetes/.work/lab-outline.md (รายการ screenshot ที่กำหนดต่อแล็บ)

ขั้นที่ 1 — เปิดดูทุกภาพจริง ๆ ให้คะแนน 1-5 ต่อหมวด + ระบุปัญหา:
  1. ตรงขั้นตอน — ภาพแสดงสิ่งที่ README บรรยาย ณ ขั้นนั้นจริง (เช่น ชื่อ Pod/เวอร์ชัน/ธีม/สถานะ api-db บนการ์ดสถานะ ตรงกับ Expected output รอบข้าง) · ชื่อไฟล์ตรงกับที่ outline กำหนด
  2. หลักฐานมองเห็น — การ์ดสถานะ (web Pod · v1/v2 · api · db) อยู่ในภาพและอ่านออก · ถ้าเป็นภาพ "ก่อน/หลัง" ต้องเห็นความต่างชัด (เช่น ชื่อ Pod ต่างกัน สีธีมต่างกัน ข้อความ error) · ไม่ใช่ภาพขาว/กำลังโหลด/หน้า error ที่ไม่ตั้งใจ
  3. คุณภาพภาพ — 1280 px กว้าง · ไม่เบลอ ไม่ถูกตัดกลางการ์ดสถานะ · ไม่มีส่วนที่ไม่เกี่ยว (เช่น dev overlay) · ขนาดไฟล์สมเหตุสมผล (< 600 KB)
  4. ความสม่ำเสมอ — ขนาด/มุมมองเดียวกันทั้งชุด · ชื่อไฟล์ขึ้นต้นด้วยเลขขั้นตอน
  5. ความปลอดภัย — ไม่มีชื่อคน/email/token จริง · ไม่มี IP/ชื่อ container ของเครื่องผลิต (172.30.*, devtools-k8s-lab-*) โผล่ในภาพ
ขั้นที่ 2 — ภาพที่หมวดใดต่ำกว่า 4: **ห้ามตัดต่อ** · ต้องถ่ายใหม่จากระบบจริง: ใช้ container ของตัวเอง (JOB=shots-s1) ตาม /root/workspace/DevTools/05_kubernetes/.work/worker-env.md → k8s-bootstrap → build/load image → apply manifest ของแล็บนั้นตาม README จนถึงขั้นที่ภาพอยู่ → ถ่ายด้วย shot.js ชื่อไฟล์เดิม → ลบ container ก่อนจบ · ถ้าถ่ายใหม่แล้ว Expected output รอบข้างใน README ไม่ตรงกับภาพ (เช่น ชื่อ Pod) ให้แก้ README เฉพาะบรรทัดนั้นให้ตรงกับของจริงและกำกับว่า "ค่าเหล่านี้ต่างกันได้"

รายงานกลับ (คำตอบสุดท้ายถูกเก็บเป็นไฟล์ — ห้ามสร้างไฟล์รายงานเพิ่ม): ตาราง แล็บ | ไฟล์ | คะแนน 5 หมวด | ปัญหา | ถ่ายใหม่? · รายการภาพที่ outline กำหนดแต่ยังไม่มี · ยืนยันการลบ container
