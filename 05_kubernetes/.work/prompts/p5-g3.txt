อ่านให้ครบก่อนเริ่ม (ห้ามเริ่มก่อนอ่านจบ):
- ข้อกำหนด : /root/workspace/DevTools/05_kubernetes/PROMPT_Kubernetes_Course_Spec.txt  (ส่วนที่ 0 · 2 · 2.5 · 3 · 4 · 8 สำคัญที่สุด — ส่วนที่ 4 คือเทมเพลต README ที่ต้องมีครบทุกหัวข้อตามลำดับ)
- แกนเนื้อหาที่ต้องยึด : /root/workspace/DevTools/05_kubernetes/.work/lab-outline.md  (กติการ่วม + รายละเอียดของแล็บที่คุณรับผิดชอบ — ทำตามขั้นตอน/ทดลองให้พัง/คำถาม ที่กำหนดไว้ ปรับได้เฉพาะเมื่อของจริงไม่เป็นไปตามนั้น และต้องรายงาน)
- กติกาสภาพแวดล้อม : /root/workspace/DevTools/05_kubernetes/.work/worker-env.md   (ของคุณ: JOB=lab-g3)
- แอปตัวอย่าง (ผ่านการตรวจแล้ว ห้ามแก้โค้ดแอป) : /root/workspace/DevTools/05_kubernetes/app/  (อ่าน app/README.md และดู manifest อ้างอิงใน app/k8s-reference/ เป็นตัวอย่างการเขียน manifest ที่ถูกต้อง)
- README ต้นแบบที่ต้องเลียนแบบ "น้ำเสียงและวิธีเขียน" (ไม่ใช่โครงหัวข้อ — โครงหัวข้อใช้ของ spec ส่วนที่ 4) : /root/workspace/DevTools/02_Docker/03_Fullstack_App_Example/002_LAB_Fullstack_Compose/readme.md

งานของคุณ: ทำแล็บลำดับที่ 007 ถึง 009 ให้เสร็จสมบูรณ์ ทีละแล็บตามลำดับ ในโฟลเดอร์ที่สร้างไว้แล้ว:
  - /root/workspace/DevTools/05_kubernetes/01_Session1_Kubernetes_Basics/007-deployment-manages-change/   (namespace lab007)
  - /root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/008-why-we-need-service/   (namespace lab008)
  - /root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/009-service-types-and-access/   (namespace lab009)

แต่ละแล็บต้องมี:
1. manifest YAML ตามชื่อไฟล์ใน lab-outline.md · สั้น อ่านจบในหน้าเดียว ตัด field ที่ไม่จำเป็นออก · ทุก image ของแอปใส่ imagePullPolicy: IfNotPresent · ใส่ comment ภาษาไทยสั้น ๆ อธิบาย field สำคัญ
2. **รันจริงบน cluster** ใน container ของตัวเองให้ผ่านทุกขั้นตอน แล้ว copy ผลรันจริงมาใส่เป็น Expected output — ห้ามแต่งผลลัพธ์ขึ้นเองเด็ดขาด
   ค่าที่ไม่คงที่ (ชื่อ Pod, IP, AGE, เวลา, hash) ให้เขียนกำกับว่า "ค่าเหล่านี้ต่างกันได้"
   บันทึก transcript ดิบของทุกคำสั่งที่รัน (คำสั่ง + output เต็ม) ต่อท้ายไฟล์ /root/workspace/DevTools/05_kubernetes/.work/runlogs/labNNN.txt (NNN = เลขแล็บ) — ผู้ตรวจจะใช้เทียบกับ README
3. README.md ครบทุกหัวข้อตามเทมเพลตส่วนที่ 4 เรียงลำดับเดิม ความยาว 200-400 บรรทัด ภาษาไทย ศัพท์เทคนิคคงอังกฤษ
   - ขึ้นต้นด้วยบล็อก `> 💡 **แนวคิดหลักของแล็บนี้:**` หนึ่งประโยค คำต่อคำจาก lab-outline.md
   - "## 0. เตรียมเครื่องเรียน" ใช้คำสั่ง docker run ตามกติกาข้อ 9 ของ lab-outline.md (แบบ `docker start devtools-k8s || docker run ...` ได้) + ตรวจ cluster ด้วย `kubectl get nodes` + ตรวจว่า image แอปอยู่ใน kind แล้ว (`docker exec devtools-control-plane crictl images | grep k8s-lab`) — ถ้ายังไม่มี ให้ชี้ไปที่ขั้น build/load ของแล็บ 001 หรือ README ระดับชุด
   - "## 1. Clone โค้ดแล็บ" : `mkdir -p ~/labwork && cd ~/labwork && git clone https://github.com/Tuchsanai/DevTools.git` แล้ว `cd ~/labwork/DevTools/05_kubernetes/<ครั้ง>/<แล็บ>` + หมายเหตุสำหรับคนที่ clone แล้ว (`git pull`)
   - อ้างไดอะแกรมของแล็บด้วย `![...](../slides_assets/labNNN-architecture.svg)` (ไฟล์นี้อีกทีมกำลังทำ อาจยังไม่มีตอนนี้ — อ้างไว้ก่อนตามชื่อนี้เป๊ะ)
   - ปิดท้ายด้วยบรรทัด *Expected output และ screenshot ในเอกสารนี้มาจากการรันจริงบน tuchsanai/devtools-k8s:2569_1 (kind v0.33.0 · Kubernetes v1.36.4 · kubectl v1.37.0) เมื่อ <วันที่จริงที่รัน>*
4. ทุกคำสั่งต้องมีครบสามส่วน: code block คำสั่ง / `> 📝 **คำอธิบาย:**` ทีละ flag คั่นด้วย · / `✅ **Expected output** — <เงื่อนไขที่ถือว่าผ่าน> :` + code block ผลรันจริง (กฎเหล็ก ห้ามข้ามแม้คำสั่งเล็ก)
5. หัวข้อ "ทดลองให้พัง" ตาม outline ที่ทำแล้วเห็นอาการจริง อ่านอาการ อธิบายสาเหตุ แก้กลับ ยืนยันว่ากลับมาปกติ
6. screenshot หน้าเว็บ (ถ้าแล็บนั้นมี UI) capture จากที่รันจริง ณ ขั้นตอนนั้น ด้วย shot.js (worker-env.md ข้อ 4) เก็บใน images/ ของแล็บ ตั้งชื่อตามที่ outline ระบุ แล้วแทรกในขั้นตอนที่ตรงกัน · ภาพต้องมองเห็นการ์ดสถานะ/ชื่อ Pod ที่เป็นหลักฐานของขั้นนั้น
7. หัวข้อ Cleanup + Clean Re-run ต้องรันจริง (ลบ namespace → สร้างใหม่จากไฟล์ → ผลเหมือนเดิม → ลบปิดท้าย) และพิสูจน์ว่า `kubectl get all -n labNNN` ไม่มีอะไรค้าง
8. เก็บกวาดให้เกลี้ยงก่อนขึ้นแล็บถัดไป (ลบ namespace รอจนหาย · kill port-forward) ไม่ให้ resource ค้างชนกัน

สภาพแวดล้อม: container ของตัวเอง `devtools-k8s-lab-g3` ตาม worker-env.md · สร้าง cluster ด้วย `k8s-bootstrap` · build + load image แอปตาม worker-env.md ข้อ 3 (ทำครั้งเดียวตอนต้น)
เปิดเว็บทดสอบจากฝั่งคุณผ่าน http://$IP:80 (Ingress) หรือ http://$IP:3000 (port-forward) โดย $IP = IP ของ container ตาม worker-env.md ข้อ 1 (localhost:port ใช้ไม่ได้บนเครื่องนี้) แต่ **ในเอกสารเขียน http://localhost:8080 / http://localhost:3000 เสมอ**
ถ้า k8s-bootstrap ล้ม ให้ `k8s-teardown` แล้วลองใหม่ได้ 1 ครั้ง · ถ้ายังล้ม ให้รายงานตรง ๆ
**ลบ container ของตัวเองก่อนส่งงาน** (`docker rm -f devtools-k8s-lab-g3`) และยืนยันว่าไม่เหลือ

ห้ามข้ามการรันจริง ถ้าแล็บไหนรันไม่ผ่านหรือของจริงไม่ตรง outline ให้บอกตรง ๆ ในรายงานว่าติดตรงไหน
อย่าเขียน README ที่อ้างผลที่ยังไม่เคยเห็น · ห้ามใส่ email/ชื่อจริง/token ในเอกสารหรือภาพ

รายงานกลับ (คำตอบสุดท้ายถูกเก็บเป็นไฟล์ — ห้ามสร้างไฟล์รายงานเพิ่ม): แล็บที่ทำเสร็จ · ผลรันของแต่ละแล็บ (ขั้นไหนผ่าน/ไม่ผ่าน) · จุดที่ต้องเบี่ยงจาก outline และเหตุผล · ปัญหาที่เจอ · สิ่งที่ยังค้าง · ยืนยันการลบ container
