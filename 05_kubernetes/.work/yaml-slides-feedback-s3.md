# feedback ของ yolo1 — หน้ากายวิภาค YAML สไลด์ครั้งที่ 3 (ดูหน้า 4,5,6,17,28,29,40,41,53,63 แล้ว)
ภาพรวม: ผ่านเกณฑ์เนื้อหา (ครอบ object ครบ · ข้อเท็จจริงถูก · การ์ดผิดบ่อยทุกหน้า) — แก้เฉพาะ:
1. หน้า 4 "ทบทวนโครง object": ไดอะแกรม d09-yaml-anatomy แสดงเป็น thumbnail เล็ก (~330px) อ่านไม่ออก → ทำให้ไดอะแกรมเป็นภาพหลักกว้างเต็มคอลัมน์ซ้าย (≥ 700px) และย้ายบล็อก YAML ConfigMap ไปไว้ใต้ภาพแบบย่อ 6 บรรทัด หรือแยกเป็น 2 หน้า (ไดอะแกรมเต็มหน้า / การ์ด 4 ใบ)
2. หน้า 6 "Deployment · Service · ConfigMap (ทบทวน)": บล็อก YAML 3 เอกสาร ~22 บรรทัด ตัวอักษรเล็กกว่า 18px → ตัดเหลือบรรทัดสำคัญ ≤ 16 บรรทัด (ใส่ `# ...` ตรงที่ตัด) หรือแยกเป็น 2 หน้า (Deployment+Service / ConfigMap)
3. ทุกหน้า YAML: ตรวจว่า footer "อ่านต่อ: YAML_Guide.md#..." ใช้ anchor ที่มีจริง (หน้า 17 ใช้ #secret · #pvc — ในคู่มือครั้งที่ 3 หัวข้อทบทวน anchor คือ #secret และ #pvc ภายใต้ "## 6." ตรวจให้ตรง)
(รวมกับ feedback ของ cyolo1 จาก .work/yaml-slides-review-s3.md เมื่อมาถึง)

## รวม feedback ของ cyolo1 (จาก .work/yaml-slides-review-s3.md ส่วน A) — ให้แก้ทุกข้อที่ยังคะแนน < 5 ในส่วน B ของรายงานนั้นด้วย
