# ผลตรวจเชิงลึก (yolo1 reviewer) แล็บ 001-004 — ต้องแก้ทั้ง 4
## LAB 001
- L57-60 WARN: docker run ต่อด้วย k8s-bootstrap ทันที → dockerd ยังไม่พร้อม; แยกบล็อก + เติมขั้นรอ `docker exec devtools-k8s docker info`
- L60-72 WARN: ขั้น 0 สั่ง crictl images ทั้งที่ image build ในหัวข้อ 6 → ตัดหรือระบุ "รอบแรกว่างเป็นปกติ"
- L62-73 WARN: ไม่มี Expected output ของ k8s-bootstrap → ใส่ `Set kubectl context to "kind-devtools"` จาก runlog L1-27 + บอกใช้เวลาหลายนาที
- L178-189 NIT: build-images.sh ใช้เวลา 3-5 นาที ให้เตือน
- L193-203 WARN: kind load ไม่มีบรรทัด "not yet present on node ... loading..." (runlog L882-893) → ใส่ตัวอย่าง
- L206-216 WARN: Expected output ของ k9s ไม่มีใน runlog (แต่งขึ้น) → เปลี่ยนเป็นข้อความบรรยาย ไม่ใส่ในบล็อก Expected
- L219-231 WARN: ไม่บอกวิธีเปิด terminal ที่สอง (`docker exec -it devtools-k8s bash` อีกหน้าต่าง)
- L232-248 **FAIL**: ขาด `kubectl wait --for=condition=Ready=false node/devtools-worker2 --timeout=90s` (runlog L911) → นักศึกษาจะเห็น Ready แทน NotReady; ใส่กลับพร้อม 📝
- L271-284 WARN: หลักฐาน metrics-server ขึ้นกับว่า Pod อยู่บน worker2 → ระบุเงื่อนไข/ใช้ `get pods -A -o wide` เลือก Pod บน worker2; เติม "(ตัดข้อความท้าย)"
- L302 NIT: "nodeสำหรับ" เว้นวรรค · L320 NIT: error kubectl localhost:8080 ให้เขียนข้อความจริง "The connection to the server localhost:8080 was refused" · L123-150 NIT: ตารางตัดคอลัมน์/แถวให้หมายเหตุ
## LAB 002
- L270-271 **FAIL**: `lab002 Active 1m` แต่งขึ้น (kubectl ไม่พิมพ์ 1m) → ใช้ผลจริง (runlog มี -o yaml L170 / jsonpath L236)
- L272-274 WARN: ตัด prefix log โดยไม่บอก → คงรูปแบบจริง L227-229 หรือหมายเหตุ
- L60-69 NIT: ผล crictl ตัด docker.io/library และ nodes 13m ไม่ใช่ของแล็บนี้ → ใช้บรรทัดจริง lab001 L894-898 + หมายเหตุตัดคอลัมน์
- L106-118 NIT: ตัดแถว Pod/namespace โดยไม่บอก → "(แสดงบางแถว)" · L209-214 NIT: explain ตัดประโยคกลาง → คง 4 บรรทัดจริง L193-195 · L289 NIT: ตัดคำว่า "ตาม outline"
## LAB 003
- L181-199 WARN: YAML ในบล็อกใส่ args เป็นภาษาไทยแทนโค้ดจริง และไม่แสดง labels → แสดง command/args ตามไฟล์จริง + อธิบาย field
- L236-249 WARN: kubectl run web-broken แล้ว get pod ทันทีจะไม่เห็น ImagePullBackOff → เติม sleep 25 / บอกรอ 20-30 วิ
- L271-283 WARN: Expected ของ get events | tail -12 ประกอบขึ้น (ตัด event web-broken) → copy ผลจริง L111-123 หรือใช้ --field-selector involvedObject.name=web2
- L144, L201 NIT: ตัด "ตาม outline"; เหตุผล localhost = ::1 vs IPv4 ให้ตรงกับ L145
- L166-168 WARN: port-forward foreground ไม่บอกวิธีเปิด terminal ที่สอง
- L137-142 NIT: แยก comment "# พิมพ์ในเชลล์ของ container" · L257 NIT: image เป็น field ที่แก้ได้ (LAB 004) · L245 NIT: ข้อความ Failed ตัดท้าย ใส่ … · L344 NIT: 🧭 ต่อยอด ไม่มีลิงก์ · L62-68 NIT: nodes 13m ไม่ใช่ของแล็บนี้
## LAB 004
- L119-131 WARN: dry-run -o yaml ตัด ports/resources/dnsPolicy/restartPolicy/status ออก (จริง 16 บรรทัด L25-40) → แสดงเต็มแล้วชี้ว่าไฟล์ตัดอะไร
- L256-260 WARN: ผลจริง dry-run=server มี Warning last-applied-configuration (L316) → ใส่และอธิบายว่ามาจาก replace
- L296-297 WARN: custom-columns ไม่มีใน runlog (แต่งขึ้น) → รันจริงหรือใช้ get pod L403-405
- L74-81 WARN: บอกครบ 4 image แต่แสดง 2 · nodes 14m ไม่ตรง (20m L5-7) → ใช้ 4 แถวจริง L11-14
- L62-65 WARN: container ID 64 hex ไม่มีใน runlog + ประโยค "รอบตรวจรับใช้ isolated container" → แสดงผล docker start = devtools-k8s และตัดวงเล็บ
- L104-106 NIT: "repository ที่ตรวจรับ" ภาษาภายใน · L179 NIT: หัวข้อพูดถึง volume แต่ไม่แสดง · L231-241 NIT: เพิ่ม printenv LAB_NOTE (L294-295) · L379-381 NIT: รูปแบบคำถาม/แนวคำตอบให้เหมือนแล็บอื่น · L393 NIT: ปี "2569" vs "2026" ให้เลือกแบบเดียวทั้งชุด
## ข้ามแล็บ
- ไม่บอกวิธีเปิด terminal ที่สอง (001, 003) · ตัดคอลัมน์/แถว/ข้อความท้ายโดยไม่หมายเหตุ (ทุกแล็บ) · คำว่า "outline"/"ตรวจรับ" รั่ว (002-004) · ผล get nodes/crictl ขั้น 0 คัดลอกต่อกันไม่ได้มาจาก runlog ของแล็บนั้น (002-004)
