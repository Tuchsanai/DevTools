ดำเนินการครบแล้ว: แก้ข้อความ 44 ภาพ รวม 88 ไฟล์ (`SVG 44 + Excalidraw 44`) โดยเปลี่ยนเฉพาะข้อความ และตรวจ PNG ตาม Excalidraw quality checklist แล้วว่าไม่ล้นกล่องหรือทับเส้น

PNG ทั้ง 48 ภาพอยู่ใน [.work/academic-diagram-shots](/root/workspace/DevTools/05_kubernetes/.work/academic-diagram-shots) และสคริปต์สแกนอยู่ที่ [scan-diagram-thai.py](/root/workspace/DevTools/05_kubernetes/.work/tools/scan-diagram-thai.py)

### Session 1

| ไฟล์ | ข้อความเดิม ⇒ ใหม่ | PNG |
|---|---|---|
| d01-compose-vs-kubernetes | เครื่องดับ → ทุก container หยุด ⇒ เครื่องขัดข้อง → ทุก container หยุด<br>Control plane เฝ้า desired state ⇒ Control plane ติดตาม desired state<br>node หาย → control plane รับรู้และแก้ส่วนต่าง ⇒ node ขัดข้อง → control plane รับรู้และปรับส่วนต่าง | ผ่าน |
| d02-cluster-architecture | kubectl คุยกับ API server · control plane ตัดสินใจ · worker รัน Pod ⇒ kubectl สื่อสารกับ API server · control plane ตัดสินใจ · worker เรียกใช้ Pod<br>Control plane · สมองของ cluster ⇒ Control plane · ศูนย์ควบคุม cluster<br>API server ประตูของ API ⇒ API server จุดรับคำขอ API<br>etcd เก็บ state ⇒ etcd จัดเก็บ state<br>scheduler + controller เฝ้า API และเทียบสภาพ ⇒ scheduler + controller ติดตาม API และเทียบสภาพ | ผ่าน |
| d03-pod-vs-container | Pod คือ “ซอง” ที่ scheduler ย้ายทั้งชุด และมี container ได้ตั้งแต่หนึ่งตัวขึ้นไป ⇒ Pod คือหน่วยที่ scheduler จัดวางทั้งชุด และรองรับ container ตั้งแต่หนึ่งหน่วยขึ้นไป<br>มองแยกเป็น container ⇒ แยกพิจารณาเป็น container<br>แชร์ IP/localhost · แชร์ volume ได้ · lifecycle เดียวกัน ⇒ ใช้ IP/localhost และ volume ร่วมกัน · lifecycle เดียวกัน | ผ่าน |
| d04-declarative-loop | บอกปลายทางที่ต้องการ แล้ว Kubernetes วนตรวจและแก้ส่วนต่างให้เอง ⇒ กำหนดสภาพที่ต้องการ แล้ว Kubernetes ตรวจสอบและปรับส่วนต่างโดยอัตโนมัติ<br>API server เก็บ spec ⇒ API server จัดเก็บ spec<br>controller วนตรวจตลอดเวลา ⇒ controller ตรวจสอบต่อเนื่อง<br>แก้ส่วนต่าง สร้าง / ลบ / อัปเดต ⇒ ปรับให้สอดคล้อง สร้าง / ลบ / อัปเดต<br>actual state Pod ที่รันจริง ⇒ actual state Pod ที่ทำงานจริง<br>เทียบ desired กับ actual ⇒ เปรียบ desired กับ actual<br>ลูปนี้ทำงานตลอดเวลา · ไม่ต้องมีคนนั่งเฝ้า ⇒ กระบวนการทำงานต่อเนื่อง · โดยไม่ต้องมีผู้ติดตาม | ผ่าน |
| d05-labels-and-selectors | Labels และ selectors คือกาวของ Kubernetes ⇒ Labels และ selectors คือกลไกเชื่อม Kubernetes<br>กลุ่มที่ selector จับได้ ⇒ กลุ่มที่ selector เลือก<br>ชื่อ/IP เปลี่ยนได้ · label ยังบอกบทบาทเดิม ⇒ ชื่อ/IP เปลี่ยนได้ · label ยังคงระบุบทบาทเดิม | ผ่าน |
| d06-deployment-rs-pod | Deployment ดูแลการเปลี่ยนเวอร์ชัน · ReplicaSet ดูแลจำนวน · Pod รันแอป ⇒ Deployment ควบคุมการเปลี่ยนเวอร์ชัน · ReplicaSet รักษาจำนวน · Pod เรียกใช้แอป<br>rollout = scale RS ใหม่ขึ้น / พร้อม scale RS เก่าลง ⇒ rollout = เพิ่มขนาด RS ใหม่ / พร้อมลดขนาด RS เก่า | ผ่าน |
| d07-rolling-vs-recreate | ดูจำนวน Pod ที่ “พร้อมรับงาน” ในแต่ละช่วงเวลา ⇒ ตรวจสอบจำนวน Pod ที่ “พร้อมรับงาน” ในแต่ละช่วงเวลา<br>Recreate · ปิดของเก่าก่อนเปิดของใหม่ ⇒ Recreate · ปิดชุดเดิมก่อนเปิดชุดใหม่<br>RollingUpdate · ค่อย ๆ แทนทีละส่วน ⇒ RollingUpdate · ทยอยแทนทีละส่วน<br>มี Pod พร้อมรับงานตลอด · ของเก่าอยู่จนของใหม่ Ready ⇒ มี Pod พร้อมรับงานตลอด · ชุดเดิมคงอยู่จนชุดใหม่ Ready | ผ่าน |
| d08-learning-loop | วงจรเรียนรู้ประจำทุกแล็บ ⇒ วงจรเรียนรู้ของทุกปฏิบัติการ<br>1 · ทายผล ⇒ 1 · คาดการณ์ผลลัพธ์<br>2 · รัน ⇒ 2 · เรียกใช้<br>5 · ทำให้พัง ⇒ 5 · จำลองความล้มเหลว<br>6 · แก้กลับ ⇒ 6 · คืนสภาพ | ผ่าน |
| lab001-architecture | Lab 001 · รู้จัก Kubernetes cluster ⇒ Lab 001 · องค์ประกอบ Kubernetes cluster<br>ยังไม่รัน SkillSpace · ตรวจ cluster, node และ load image เข้า node ทั้ง 3 ⇒ ยังไม่เรียกใช้ SkillSpace · ตรวจ cluster, node และ load image เข้า node ทั้ง 3<br>kubectl / k9s ในเครื่องเรียน ⇒ kubectl / k9s ในเครื่องผู้เรียน | ผ่าน |
| lab002-architecture | ทุกอย่างคือ object · namespace แบ่ง “ห้อง” แต่ Node/Namespace เป็น cluster-scoped ⇒ ทรัพยากรทั้งหมดคือ object · namespace แบ่งขอบเขต แต่ Node/Namespace เป็น cluster-scoped<br>ไม่มี Pod ในห้องนี้ ⇒ ขอบเขตนี้ไม่มี Pod<br>สร้างห้องแล้ว ยังไม่มี Pod ⇒ สร้างขอบเขตแล้ว ไม่มี Pod | ผ่าน |
| lab003-architecture | ไม่มี Service/Deployment · Browser แอบดู Pod เดี่ยวผ่าน port-forward ⇒ ไม่มี Service/Deployment · Browser เข้าถึง Pod เดี่ยวโดยตรงผ่าน port-forward<br>Pod แต่ละตัวอาจอยู่คนละ node ⇒ Pod แต่ละหน่วยอาจอยู่ต่าง node<br>web + sidecar แชร์ localhost ใน Pod เดียวกัน ⇒ web + sidecar ใช้ localhost ร่วมกันใน Pod เดียวกัน | ผ่าน |
| lab004-architecture | API server เก็บ desired state ⇒ API server จัดเก็บ desired state<br>port-forward ตรงสู่ Pod ⇒ port-forward โดยตรงสู่ Pod<br>desired state อยู่ในไฟล์ เก็บใน Git ได้ ⇒ desired state อยู่ในไฟล์และจัดเก็บใน Git ได้<br>actual + status Kubernetes เขียนกลับ ⇒ actual + status Kubernetes บันทึกผล<br>เทียบ ⇒ เปรียบ | ผ่าน |
| lab005-architecture | Lab 005 · Labels จับกลุ่ม Pod ⇒ Lab 005 · Labels จัดกลุ่ม Pod<br>สาม Pod ถูกสร้างตรง ๆ · selector ทำงานกับ label โดยไม่อ้างชื่อหรือ IP ⇒ Pod จำนวน 3 หน่วยถูกสร้างโดยตรง · selector ทำงานกับ label โดยไม่อ้างชื่อหรือ IP<br>selector จับ web-b และ web-c ในแถวเดียวกัน ⇒ selector เลือก web-b และ web-c ในแถวเดียวกัน | ผ่าน |
| lab006-architecture | ไม่มี Deployment · ReplicaSet ถือ desired replicas = 3 และสร้าง Pod ใหม่เมื่อสมาชิกหาย ⇒ ไม่มี Deployment · ReplicaSet รักษา desired replicas = 3 และสร้าง Pod ใหม่เมื่อ Pod หยุดทำงาน | ผ่าน |
| lab007-architecture | Browser เข้าผ่านประตูเดิม · Deployment ค่อย ๆ ย้าย replicas จาก RS v1 ไป RS v2 ⇒ Browser เข้าผ่านจุดเดิม · Deployment ทยอยย้าย replicas จาก RS v1 ไป RS v2 | ผ่าน |

### Session 2

| ไฟล์ | ข้อความเดิม ⇒ ใหม่ | PNG |
|---|---|---|
| d01-pod-ip-changes | อย่าผูกแอปกับ IP ของ Pod โดยตรง ให้เรียกผ่าน DNS ของ Service ⇒ ไม่ควรผูกแอปกับ IP ของ Pod โดยตรง ควรเรียกผ่าน DNS ของ Service<br>ทางที่เปราะบาง: เรียก Pod IP โดยตรง ⇒ รูปแบบที่เปราะบาง: เรียก Pod IP โดยตรง<br>ทางที่ทนทาน: เรียกชื่อ Service ⇒ รูปแบบที่ทนทาน: เรียกชื่อ Service<br>web ยังจำ IP เก่า จึงหา Pod ใหม่ไม่เจอ ⇒ web ยังคงใช้ IP เดิม จึงไม่พบ Pod ใหม่ | ผ่าน |
| d02-service-types | Service Types — ต่างกันที่ใครเข้าถึงได้ ⇒ Service Types — ขอบเขตการเข้าถึง<br>จากซ้ายไปขวา: ภายใน cluster → พอร์ตบน node → cloud → ประตู HTTP เดียว ⇒ จากซ้ายไปขวา: ภายใน cluster → พอร์ตบน node → cloud → จุดเข้า HTTP เดียว<br>เมื่อ cloud จัดให้ ⇒ เมื่อ cloud รองรับ | ผ่าน |
| d03-config-outside-image | build ครั้งเดียว แล้ว inject ConfigMap / Secret ตามสภาพแวดล้อมตอน deploy ⇒ build ครั้งเดียว แล้ว inject ConfigMap / Secret ตามสภาพแวดล้อมระหว่าง deploy | ผ่าน |
| d04-secret-base64 | Secret: base64 คือการแปลงรูป ไม่ใช่การเข้ารหัส ⇒ Secret: base64 คือการแปลงรูปแบบ ไม่ใช่การเข้ารหัส<br>Kubernetes รับ stringData แล้วเก็บเป็น data แบบ base64; เมื่อ inject เข้า Pod จะเห็นค่าจริง ⇒ Kubernetes รับ stringData แล้วจัดเก็บเป็น data แบบ base64; เมื่อ inject แล้ว Pod ได้รับค่าจริง<br>ถอดกลับได้ในคำสั่งเดียว — ใช้ RBAC + encryption at rest ⇒ แปลงกลับได้ด้วยคำสั่งเดียว — ใช้ RBAC + encryption at rest | ผ่าน |
| d05-container-fs-vs-volume | ข้อมูลอยู่รอดแค่ไหน? Container FS → emptyDir → PVC ⇒ ระดับความคงอยู่ของข้อมูล: Container FS → emptyDir → PVC<br>อายุของข้อมูลขึ้นกับชั้นที่เราเลือกเก็บ ⇒ อายุของข้อมูลขึ้นอยู่กับชั้นที่เลือกจัดเก็บ<br>รอด: container เดิมเท่านั้น ⇒ คงอยู่: เฉพาะ container เดิม<br>รอด: ภายใน Pod เดิม ⇒ คงอยู่: ภายใน Pod เดิม<br>รอด: ข้ามการเกิดใหม่ของ Pod ⇒ คงอยู่: หลังสร้าง Pod ใหม่<br>Pod ใหม่ → หาย ⇒ Pod ใหม่ → สูญหาย (2 จุด)<br>container restart → ยังอยู่ ⇒ container restart → ข้อมูลคงอยู่<br>Pod ใหม่ → ยังอยู่ ⇒ Pod ใหม่ → ยังคงอยู่ | ผ่าน |
| d06-readiness-vs-liveness | Probe ต้องตอบให้ตรงเรื่อง: พร้อมรับงานหรือยัง? หรือ process ยังมีชีวิตไหม? ⇒ Probe ต้องประเมินให้ตรงวัตถุประสงค์: พร้อมรับงานหรือไม่ หรือ process ทำงานอยู่หรือไม่<br>readinessProbe — “ตอนนี้รับงานได้ไหม?” ⇒ readinessProbe — “ขณะนี้พร้อมรับงานหรือไม่”<br>livenessProbe — “process ยังมีชีวิตไหม?” ⇒ livenessProbe — “process ทำงานอยู่หรือไม่”<br>FAIL → ถอด Pod ออกจาก Service ⇒ FAIL → นำ Pod ออกจาก Service<br>PASS → ใส่กลับเอง / ไม่ restart container ⇒ PASS → คืน Pod อัตโนมัติ / ไม่ restart container<br>Pod api / process ค้าง ⇒ Pod api / ไม่ตอบสนอง<br>FAIL → ฆ่าและเริ่ม container ใหม่ ⇒ FAIL → หยุดและเริ่ม container ใหม่<br>อย่าเช็ก dependency ภายนอก เช่น db ใน liveness ⇒ ไม่ควรตรวจ dependency ภายนอก เช่น db ใน liveness | ผ่าน |
| d08-learning-loop | วงจรการเรียนรู้: ทาย → ลงมือ → อธิบาย → แก้กลับ ⇒ วงจรการเรียนรู้: คาดการณ์ → ปฏิบัติ → อธิบาย → คืนสภาพ<br>ทุกแล็บวนครบหนึ่งรอบ เพื่อเปลี่ยนคำสั่งให้เป็นความเข้าใจ ⇒ ทุกปฏิบัติการครบหนึ่งรอบ เพื่อเปลี่ยนคำสั่งเป็นความเข้าใจ<br>1 ทายผล ⇒ 1 คาดการณ์ผลลัพธ์<br>2 รันคำสั่ง ⇒ 2 เรียกใช้คำสั่ง<br>5 ทำให้พัง ⇒ 5 จำลองความล้มเหลว<br>6 แก้กลับ ⇒ 6 คืนสภาพ<br>แนวคิดหลัก “เพราะอะไร” ⇒ แนวคิดหลัก “ระบุเหตุผล” | ผ่าน |
| lab008-architecture | Lab 008 — ทำไมต้องมี Service ⇒ Lab 008 — เหตุผลที่ต้องมี Service<br>Pod IP เปลี่ยน แต่ Service DNS “api” และ Endpoints ตาม Pod ให้เอง ⇒ Pod IP เปลี่ยน แต่ Service DNS “api” และ Endpoints ติดตาม Pod โดยอัตโนมัติ<br>ไม่มี db ในแล็บนี้ · Service เลือก Pod ด้วย label และอัปเดต Endpoints อัตโนมัติ ⇒ ไม่มี db ในปฏิบัติการนี้ · Service เลือก Pod ด้วย label และอัปเดต Endpoints อัตโนมัติ | ผ่าน |
| lab009-architecture | web เปิดด้วย NodePort; api เป็น ClusterIP และเรียกได้เฉพาะภายใน cluster ⇒ web เข้าถึงด้วย NodePort; api เป็น ClusterIP และเรียกได้เฉพาะภายใน cluster<br>ทางเข้าจากภายนอก ⇒ จุดเข้าจากภายนอก<br>api ไม่เปิดออกนอก cluster ⇒ api ไม่เปิดสู่ภายนอก cluster | ผ่าน |
| lab010-architecture | ConfigMap inject SITE_NAME / THEME ตอนสร้าง Pod; image เดิมใช้ได้หลาย config ⇒ ConfigMap inject SITE_NAME / THEME เมื่อสร้าง Pod; image เดิมใช้ได้หลาย config | ผ่าน |
| lab011-architecture | ยังไม่มี PVC · Pod db ใหม่ → ข้อมูลหาย ⇒ ยังไม่มี PVC · Pod db ใหม่ → ข้อมูลสูญหาย | ผ่าน |
| lab012-architecture | Lab 012 — ทำไมข้อมูลหาย ⇒ Lab 012 — ข้อมูลสูญหาย<br>db Pod ใหม่ = container ใหม่; emptyDir รอดแค่ container restart ใน Pod เดิม ⇒ db Pod ใหม่ = container ใหม่; emptyDir คงอยู่เฉพาะ container restart ใน Pod เดิม<br>emptyDir / Pod ใหม่ → หาย ⇒ emptyDir / Pod ใหม่ → สูญหาย | ผ่าน |
| lab013-architecture | Pod db mount claim เดิม → PV เดิม → ข้อมูลยังอยู่ ⇒ Pod db mount claim เดิม → PV เดิม → ข้อมูลยังคงอยู่ | ผ่าน |
| lab014-architecture | Lab 014 — Running ไม่ได้แปลว่า Ready ⇒ Lab 014 — Running มิได้หมายถึง Ready<br>readiness ถอด api จาก Service; liveness ให้ kubelet restart container ⇒ readiness นำ api ออกจาก Service; liveness กำหนดให้ kubelet restart container<br>db ล่ม: api ยัง Running แต่ 0/1 Ready และ Endpoints ว่าง ⇒ db ขัดข้อง: api ยังคง Running แต่ 0/1 Ready และ Endpoints ว่าง<br>readinessProbe /ready / FAIL → ถอด Podออกจาก Endpoints ⇒ readinessProbe /ready / FAIL → นำ Pod ออกจาก Endpoints | ผ่าน |

### Session 3

| ไฟล์ | ข้อความเดิม ⇒ ใหม่ | PNG |
|---|---|---|
| d01-compose-to-objects | 3 services เดิม กลายเป็น 10 objects ที่แยกหน้าที่และซ่อมแซมเป็นส่วน ๆ ได้ ⇒ services เดิมจำนวน 3 รายการแยกเป็น 10 objects ตามหน้าที่และคืนสภาพเป็นส่วนได้<br>แนวคิดหลัก: แอปจริงคือหลาย object ที่ทำงานร่วมกัน ไม่ใช่ object เดียว ⇒ แนวคิดหลัก: แอปใช้งานจริงประกอบด้วยหลาย object ที่ทำงานร่วมกัน มิใช่ object เดียว | ผ่าน |
| d02-stateless-vs-stateful | ถามก่อน scale: ถ้าลบหรือเพิ่ม Pod แล้ว ข้อมูลต้องติดตามตัวนั้นไปด้วยหรือไม่? ⇒ ข้อพิจารณาก่อน scale: เมื่อลบหรือเพิ่ม Pod ข้อมูลต้องติดตาม Pod หรือไม่<br>Stateless ทิ้งและสร้างใหม่ได้ · Stateful ต้องวางแผน storage, identity และ backup ⇒ Stateless ลบและสร้างใหม่ได้ · Stateful ต้องวางแผน storage, identity และ backup | ผ่าน |
| d03-ingress-vs-nodeport | Ingress คือ reverse proxy ของ Kubernetes: ประตูเดียวเลือก backend ด้วย path/host ⇒ Ingress คือ reverse proxy ของ Kubernetes: จุดเข้าเดียวเลือก backend ด้วย path/host<br>Ingress · ประตูเดียว ⇒ Ingress · จุดเข้าเดียว | ผ่าน |
| d04-rolling-update-timeline | RollingUpdate แบบไม่สะดุด ⇒ RollingUpdate โดยไม่หยุดชะงัก<br>replicas=3 · maxSurge=1 · maxUnavailable=0 · readiness เป็นประตูก่อนรับ traffic ⇒ replicas=3 · maxSurge=1 · maxUnavailable=0 · readiness เป็นเงื่อนไขก่อนรับ traffic<br>maxUnavailable=0 เก็บตัวเก่าที่พร้อมไว้เสมอ · maxSurge=1 เพิ่มตัวใหม่ครั้งละ 1 ⇒ maxUnavailable=0 คง Pod เดิมที่พร้อมไว้เสมอ · maxSurge=1 เพิ่ม Pod ใหม่ครั้งละ 1<br>t2 · Ready แล้ว ⇒ t2 · สถานะ Ready<br>t4 · เสร็จ ⇒ t4 · สมบูรณ์ | ผ่าน |
| d05-requests-limits-scheduling | requests ใช้ตอนวาง Pod · limits บังคับตอน container กำลังรัน ⇒ requests ใช้ขณะจัดวาง Pod · limits บังคับขณะ container ทำงาน<br>requests = ทรัพยากรที่ “จอง” / limits = เพดานตอนรัน ⇒ requests = ทรัพยากรที่ “จอง” / limits = ขีดจำกัดขณะทำงาน<br>Scheduler นับ requests แล้วเลือก node ⇒ Scheduler คำนวณ requests แล้วเลือก node<br>devtools-worker · เหลือพอ / devtools-worker2 · ไม่พอ / Pod ลง worker ที่รับได้ ⇒ devtools-worker · เพียงพอ / devtools-worker2 · ไม่เพียงพอ / Pod จัดวางบน worker ที่รองรับ<br>requests ใหญ่เกิน → Pending · memory เกิน limits → OOMKilled ⇒ requests สูงเกิน → Pending · memory เกิน limits → OOMKilled | ผ่าน |
| d06-troubleshooting-ladder | ไล่จากอาการกว้าง → หลักฐานเฉพาะ แล้วจับคู่กับชั้นที่พัง ⇒ ตรวจจากอาการทั่วไป → หลักฐานเฉพาะ แล้วระบุชั้นที่ล้มเหลว<br>ลำดับตรวจ · ทำซ้ำทุกเคส ⇒ ลำดับตรวจสอบ · ทุกกรณี<br>3 · kubectl logs --previous / เสียงของแอปที่ crash ⇒ 3 · kubectl logs --previous / ข้อมูลจากแอปที่ crash<br>4 อาการ → จุดที่ควรสงสัย ⇒ 4 อาการ → จุดที่ควรตรวจสอบ | ผ่าน |
| d07-request-path-browser-to-postgres | Response เดินย้อนเส้นทางเดิม · ตัด object ใด อาการปรากฏที่ชั้นถัดไป ⇒ Response ส่งกลับตามเส้นทางเดิม · ยกเลิก object ใด อาการปรากฏที่ชั้นถัดไป | ผ่าน |
| d08-learning-loop | วงจรการเรียนรู้แบบลงมือจริง ⇒ วงจรการเรียนรู้จากการปฏิบัติจริง<br>ทุกแล็บเริ่มจากการคาดการณ์ แล้วใช้หลักฐานอธิบายสิ่งที่เกิดขึ้น ⇒ ทุกปฏิบัติการเริ่มจากการคาดการณ์ แล้วใช้หลักฐานอธิบายสิ่งที่เกิดขึ้น<br>1 · ทายผล ⇒ 1 · คาดการณ์ผลลัพธ์<br>2 · รัน ⇒ 2 · เรียกใช้<br>5 · ทำให้พัง ⇒ 5 · จำลองความล้มเหลว<br>6 · แก้กลับ ⇒ 6 · คืนสภาพ<br>หลักฐาน 3 มุม ⇒ หลักฐาน 3 แหล่ง<br>ทาย → รัน → สังเกต → อธิบาย → ทำให้พัง → แก้กลับ → ทายใหม่ ⇒ คาดการณ์ → เรียกใช้ → สังเกต → อธิบาย → จำลองความล้มเหลว → คืนสภาพ → คาดการณ์ใหม่ | ผ่าน |
| lab015-architecture | Lab 015 · ประกอบ web + api ⇒ Lab 015 · รวมระบบ web + api<br>ยังไม่มี database · ทุก object มาจาก manifests/01..06 และ apply ซ้ำเพื่อซ่อมได้ ⇒ ยังไม่มี database · ทุก object อ้างอิง manifests/01..06 และ apply ซ้ำเพื่อคืนสภาพได้<br>db: ยังไม่มีในแล็บนี้ ⇒ db: ยังไม่มีในปฏิบัติการนี้<br>ลบ Service / Ingress / ConfigMap แล้ว apply ทั้งโฟลเดอร์เพื่อซ่อม object ที่หาย ⇒ ลบ Service / Ingress / ConfigMap แล้ว apply ทั้งโฟลเดอร์เพื่อคืนสภาพ object ที่ขาดหาย | ผ่าน |
| lab016-architecture | ครบ 3 ชั้น: web + api + PostgreSQL + Secret + PVC ⇒ ระบบ 3 ชั้นครบถ้วน: web + api + PostgreSQL + Secret + PVC<br>ลบ Pod db แล้ว Deployment สร้างใหม่ · ข้อมูลยังอยู่ใน PVC เดิม ⇒ ลบ Pod db แล้ว Deployment สร้างใหม่ · ข้อมูลยังคงอยู่ใน PVC เดิม | ผ่าน |
| lab017-architecture | Lab 017 · Ingress คือประตูหน้าบ้าน ⇒ Lab 017 · Ingress คือจุดเข้าใช้งานระบบ<br>ชั้นข้อมูลจาก Lab 016 ยังอยู่ครบ ⇒ ชั้นข้อมูลจาก Lab 016 ยังคงอยู่ครบถ้วน<br>ประตูเดียวแยก backend ด้วย path · ไม่ต้องเปิด NodePort ใหม่ทุก Service ⇒ จุดเข้าเดียวแยก backend ด้วย path · ไม่จำเป็นต้องเปิด NodePort ใหม่ทุก Service | ผ่าน |
| lab018-architecture | Pod ใหม่ผ่าน readiness ก่อน จึงค่อยลด Pod เก่า · curl loop ไม่เห็น FAIL ⇒ Pod ใหม่ผ่าน readiness ก่อน จึงลด Pod เดิม · curl loop ไม่ปรากฏ FAIL | ผ่าน |
| lab019-architecture | Lab 019 · บอก Kubernetes ว่า Pod ต้องการเท่าไร ⇒ Lab 019 · กำหนดทรัพยากรที่ Pod ต้องการ<br>requests มีผลตอน schedule · limits มีผลตอนรัน · Browser ยังผ่าน Ingress ตามเดิม ⇒ requests มีผลขณะ schedule · limits มีผลขณะทำงาน · Browser ยังผ่าน Ingress ตามเดิม<br>requests ใหญ่เกิน = Pending · memory เกิน limits = OOMKilled ⇒ requests สูงเกิน = Pending · memory เกิน limits = OOMKilled<br>Scheduler นับ requests แล้วเลือก node ⇒ Scheduler คำนวณ requests แล้วเลือก node<br>devtools-worker · รับได้ / devtools-worker2 · ที่จองไม่พอ / ขอเกินทั้งหมด → Pending ⇒ devtools-worker · เพียงพอ / devtools-worker2 · ไม่เพียงพอ / requests เกินรวม → Pending | ผ่าน |
| lab020-architecture | Lab 020 · อ่านอาการเพื่อหาชั้นที่พัง ⇒ Lab 020 · ระบุชั้นที่ล้มเหลวจากอาการ<br>ชุด broken/ สร้าง 4 อาการ แล้วไล่ get → describe → logs → events ⇒ ชุด broken/ สร้าง 4 อาการ แล้วตรวจตามลำดับ get → describe → logs → events<br>Running ไม่ได้แปลว่าเชื่อมต่อได้ · ตรวจ Service, endpoints และ readiness ต่อ ⇒ Running มิได้หมายถึงเชื่อมต่อได้ · ตรวจ Service, endpoints และ readiness เพิ่มเติม<br>1 · get / เห็นอาการ ⇒ 1 · get / ตรวจสอบอาการ<br>2 · describe / ดู Events ⇒ 2 · describe / ตรวจ Events<br>3 · logs --previous / ฟังแอป ⇒ 3 · logs --previous / ตรวจสอบแอป<br>4 · events / มองตามเวลา ⇒ 4 · events / ตรวจสอบตามเวลา<br>แก้ fixed/ / apply คืน ⇒ แก้ fixed/ / apply คืนค่า | ผ่าน |
| lab021-architecture | Lab 021 · ภาพใหญ่ของระบบ SkillSpace ⇒ Lab 021 · ภาพรวมของระบบ SkillSpace<br>ลองตัด Service db · Service api · Ingress · scale web=0 แล้วสังเกตอาการ ⇒ ทดลองตัด Service db · Service api · Ingress · scale web=0 แล้วสังเกตอาการ | ผ่าน |

ไฟล์ที่ไม่ต้องแก้จำนวน 4 คู่ (`SVG 4 + Excalidraw 4`):

- `01_Session1_Kubernetes_Basics/d09-yaml-anatomy`
- `02_Session2_Networking_Config_Storage/d07-request-path-web-api-db`
- `02_Session2_Networking_Config_Storage/d09-yaml-anatomy`
- `03_Session3_Application_Deployment/d09-yaml-anatomy`

ผลตรวจสุดท้าย: ไม่พบคำภาษาพูดที่กำหนด, ข้อความ SVG/Excalidraw ตรงกันครบ 48 คู่, และการเปรียบเทียบกับสำเนาก่อนแก้ยืนยันว่าไม่มีคุณสมบัติอื่นของไดอะแกรมเปลี่ยนแปลงครับ