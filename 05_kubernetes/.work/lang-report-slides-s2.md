ปรับไฟล์ [Kubernetes_Session2_Slides.html](/root/workspace/DevTools/05_kubernetes/02_Session2_Networking_Config_Storage/Kubernetes_Session2_Slides.html) เรียบร้อยแล้ว

ผลลัพธ์:

- แก้ไขข้อความครบ 88/88 หน้า
- HARD: 64 → 0
- SOFT: 22 → 0 จึงไม่มีรายการที่ต้องให้เหตุผลเพิ่มเติม
- อัปเดต SVG ครบ 30 คีย์ โดยคง ASSETS ทั้ง 60 คีย์ตามเดิม
- `d09-yaml-anatomy.svg` ซึ่งฝังผ่าน `window.D09_YAML` อยู่แล้ว มี SHA-256 ตรงกับไฟล์ล่าสุด จึงไม่แก้ JS

ผลตรวจ:

- `check-lang-diff.py`: `OK`
  - `<pre>` เหมือนเดิม
  - ASSETS keys เหมือนเดิม
  - จำนวนหน้า 88 หน้าเท่าเดิม
  - CSS เหมือนเดิม
- `check-register.py`: `HARD=0 · SOFT=0`
- `check-slides.js`: 88 หน้า, `brokenImages=0`, `externalRefs=0`, `externalImports=0`, `pageErrors=0`
- รูปภาพทั้งหมด 48 ภาพ และไม่มีภาพที่ขาด `alt`
- Overflow ที่ viewport 1600×900: `overflowPages=[]`
- ตรวจ PNG ด้วยตา 18 หน้า ได้แก่ 1, 2, 3, 4, 11, 14, 15, 16, 20, 30, 41, 52, 64, 74, 75, 86, 87 และ 88 ไม่พบข้อความล้น ทับ footer หรือวางผิดตำแหน่ง

ตัวอย่างก่อน → หลัง:

1. `ของที่เกิดใหม่ตลอดเวลา` → `ทรัพยากรที่ถูกสร้างใหม่เสมอ`
2. `เส้นทางวันนี้ — 7 ปัญหา 7 แล็บ` → `โครงสร้างการเรียนรู้ — 7 ปัญหา 7 ปฏิบัติการ`
3. `ทาย → รัน → สังเกต → อธิบาย → ทำให้พัง → แก้กลับ` → `คาดผล → ใช้งาน → สังเกต → อธิบาย → จำลองข้อขัดข้อง → แก้ไข`
4. `มองภาพนี้:` → `ข้อสังเกตจากภาพ:`
5. `คำถามก่อนเริ่ม — ทายก่อนกด Enter` → `คำถามนำก่อนเริ่มปฏิบัติการ — คาดผลก่อนใช้คำสั่ง`
6. `รันคำสั่ง — อ่านผลที่ API server เก็บจริง` → `ใช้คำสั่ง — ตรวจสอบผลที่ API server บันทึก`
7. `ผิดบ่อย` → `ข้อผิดพลาดที่พบบ่อย`
8. `เช็กลิสต์ก่อนจบ` → `รายการตรวจสอบก่อนจบปฏิบัติการ`

ไม่ได้แก้ `<pre>/<code>`, คำสั่งและผลลัพธ์, CSS/JS, ป้ายอังกฤษ, path/ชื่อไฟล์, จำนวนและลำดับหน้า รวมถึง asset ที่ไม่ใช่ SVG เพราะเป็นส่วนที่ระบุให้คงเดิมทั้งหมด.