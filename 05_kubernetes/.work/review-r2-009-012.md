# ผลตรวจเชิงลึก (yolo1 reviewer) แล็บ 009-012 — 009 ต้องแก้ (WARN) · 010/011/012 ต้องแก้ (FAIL)
## ข้ามแล็บ
- A FAIL (010/011/012): ส่วน 0 ไม่มี `docker exec -it devtools-k8s bash` และไม่มี `k8s-bootstrap` → ทำแบบ 009 L62-66 (docker exec -it → k8s-bootstrap → kubectl get nodes → crictl images | grep k8s-lab)
- B FAIL (010/011/012): `curl http://localhost:8080/info` ใน container ต้องเป็น `http://localhost/info` (port 80) — 8080 เฉพาะเบราว์เซอร์
## LAB 009
- L186-199 WARN port-forward foreground ล็อก terminal แต่หัวข้อ 6-8/Cleanup(302) สมมุติว่ายังรัน → บอกเปิด terminal ที่สอง (`docker exec -it devtools-k8s bash`) หรือใส่ & และวิธีหยุด
- L139-153 WARN ไม่มี `kubectl get nodes -o wide` (outline ขั้น 2) จึงพิสูจน์ "ถึง web แม้ control-plane ไม่มี web Pod" ไม่ได้ → เพิ่มก่อน loop หรือพิมพ์ชื่อ node คู่ IP
- L273,311 WARN hard-code 172.18.0.3 → `NODE_IP=$(kubectl get node devtools-control-plane -o jsonpath='{.status.addresses[0].address}')`
- L207,214-221 WARN logs --tail=8 จริงมี `GET /ready 503` 6 บรรทัด (runlog 125-132) ตัดทิ้งไม่อธิบาย → แสดงครบ + โน้ต "503 ปกติ แล็บนี้ยังไม่มี db" หรือ grep /health
- L130-133,281-288,327-330 WARN Expected พิมพ์ใหม่ตัดคอลัมน์/ผสมหลายคำสั่ง (runlog 205-217) → วางผลจริง ตัดได้เฉพาะแถว + …
- L173-182 NIT timeout 6 บรรทัด (curlrc retry=5) README แสดง 5 และเงียบ ~30 วิ → บอกรอ/ใส่ --retry 0 · L69 NIT 📝 ไม่อธิบาย docker exec -it/k8s-bootstrap · L28 NIT ประโยค NodePort ให้เขียน "ClusterIP ที่เพิ่มทางเข้าบนทุก node" · L145 NIT 📝 ข้าม printf/curl -s/jq -r
## LAB 010
- L53-66 FAIL (A) · L117,186,206,249,285,331 FAIL (B)
- L329-347 WARN Clean Re-run apply 01 แล้ว 03 คำสั่งเดียว → มี `configured` (runlog 1065) ไม่อยู่ใน Expected; curl ทันทีได้ 503 → jq parse error (runlog 1050) → ใส่ `kubectl rollout status -n lab010 deployment/web --timeout=120s` ก่อน curl + บรรทัด configured + โน้ตรอ 2-3 วิ
- L71-74 NIT nodes 12m ยกจาก lab011 → ใช้ของแล็บนี้ (runlog 151-153) · L248-259 NIT get events จริง ~30 บรรทัด (runlog 949-978) แสดง 1 → grep -i configmap หรือกำกับ · L283-299 NIT get all ตัดเหลือ 1 บรรทัดไม่มีหัวตาราง · L89-93 NIT pwd ไม่มีในบล็อก · L119,104/170 NIT อธิบาย `|| true`, `--timeout` · L267 NIT "ยกเลิก RS ที่พัง" → "scale RS ที่พังลง 0" · L399 NIT ปี 2026 vs 2569
## LAB 011
- L52-55 FAIL (A) · L167,240,271,301,338 FAIL (B)
- L197,204-211 WARN env | grep ^DB_ จริง 11 บรรทัด (runlog 173-184 มี DB_PORT_5432_TCP, DB_SERVICE_HOST จาก Service env) README ลบ 6 บรรทัด → grep -E '^DB_(HOST|PORT|NAME|USER|PASSWORD)=' หรือแสดงครบ + อธิบาย 1 บรรทัด
- L218-234 WARN wget /ready ครั้งแรกได้ Connection refused (runlog 209-212) ก่อน 503 (242-246) → โน้ต "รอ 2-3 วิแล้วรันซ้ำ" + วางผล wget ครบ 4 บรรทัด
- L239,247-249 WARN logs --tail=20 ณ จุดนี้คือ traceback 20 บรรทัด (runlog 264-284) บรรทัด startup db=down มาจากครั้งก่อน → ใช้ `logs deployment/api | grep -E 'startup|/ready' | tail -3` + วางบรรทัดจริง
- L162 WARN โน้ตกระบวนการผู้เขียนหลุด ("ระหว่างทดสอบจริงต้องกำหนด…") และสอน Service env injection นอกแนวคิด → ย่อ/ตัด
- L263 NIT ประโยค 3 ใจความ → แยก 2 กรณี (ไม่มี volume → initdb ใหม่ / มี volume → รหัสตั้งตอน initdb ครั้งแรก) · L62-64,173-187,309-316,348-358 NIT ตัดหัวตาราง/(1 row)/created (runlog 334-341) · L145 NIT อธิบาย wait --for --timeout · L86 NIT pwd · L399 NIT ปี
## LAB 012
- L52-55 FAIL (A) · L244,262,314 FAIL (B)
- L149-165 WARN Expected มีบรรทัด db-…-k2jwp (runlog 88-90) จาก get pods ครั้งที่สองที่บล็อกไม่มี → เติม `kubectl get pods -n lab012 -l app=db -o wide` หลัง wait
- L152,212,228,261,313,209-211 WARN psql ทันทีหลัง wait/kill 1 ต้อง retry ทุกครั้ง (runlog 86,117,256,271,319) → ใส่ใต้แต่ละขั้น "ถ้า psql ต่อไม่ได้/RESTARTS ยัง 0 ให้รอ 5-10 วิแล้วรันซ้ำ"
- L193-206 WARN `get endpointslices` + ย่อหน้า named port <unset> เป็นร่องรอยข้อผิดพลาดตอนพัฒนา นอกแนวคิด → ตัด คงตารางแก้ปัญหาแถว 301
- L97-107,159,176,234 NIT apply -f manifests/ ตัด secret/service/ingress created (runlog 24-31); "deleted from lab012 namespace" (kubectl 1.37); (1 row) → วางจริง · L116 NIT โน้ตวิธีผลิตเอกสาร ตัด · L112,118 NIT ใช้คำบนจอ "รอรับเรื่อง 3→4", "กระดานงานซ่อม 6→7" · L185-191 NIT snippet ไม่แสดง PGDATA ที่ manifest เพิ่ม → แสดง+เหตุผล หรือตัดจาก manifest · L207,240 NIT บอกรอการ์ด DB เขียวก่อนกรอกฟอร์ม (runlog 282) · L19-23 NIT คอลัมน์ข้อความจัดชิดขวา · L80,375 NIT pwd; ปี
