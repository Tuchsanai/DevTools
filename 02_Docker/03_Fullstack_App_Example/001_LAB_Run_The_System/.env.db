# ค่าตั้งต้นของฐานข้อมูล CampusOps (ตาม docs/02_contract.md §2)
# ใช้กับ  docker run --env-file .env.db ...
# ไฟล์แบบนี้ในงานจริงต้องอยู่ใน .gitignore — ที่นี่เก็บไว้เพราะเป็นรหัสผ่านของแล็บ
POSTGRES_DB=campusops
POSTGRES_USER=opsuser
POSTGRES_PASSWORD=labpass
