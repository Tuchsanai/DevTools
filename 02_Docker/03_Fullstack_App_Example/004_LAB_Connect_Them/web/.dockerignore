# ของที่ไม่ควรถูกส่งเข้า build context
# node_modules ตัวใหญ่ที่สุดและถูกสร้างใหม่ด้วย npm ci ในชั้น deps อยู่แล้ว
# ถ้าปล่อยให้ติดไปด้วย build จะช้าลงมากและอาจได้ binary ที่คอมไพล์มาจากคนละ OS
node_modules
.next
npm-debug.log*
.git
.gitignore
Dockerfile
.dockerignore
README.md
.env*.local
