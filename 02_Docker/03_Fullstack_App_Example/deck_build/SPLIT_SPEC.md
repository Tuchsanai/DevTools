# SPLIT_SPEC — สไลด์ที่เกินงบ 21 หน้า ต้องกลายเป็นอะไร

อ้างเลขหน้าจากเด็ครุ่น 60 สไลด์ (baseline ที่ `logs/u-d0-baseline.log`)
ทุกหน้าใหม่ต้องผ่าน `python3 tools/ui/check_deck.py` — prose ≤ 780 · code ≤ 14 บรรทัด · ตาราง ≤ 13 แถว · font ≥ 12px · ของหนัก ≤ 1 ชิ้น

> **นี่คือการ "ย้ายรายละเอียดไป readme" ไม่ใช่ "ทิ้งรายละเอียด"**
> ทุกบล็อกที่ตัดออกต้องมีบรรทัด pointer แทน เช่น `เต็ม ๆ ที่ 002_LAB_Build_The_API/readme.md · การทดลองที่ 10`

| หน้า | ไฟล์ part | ปัญหา | ต้องกลายเป็น |
|---|---|---|---|
| 2 | s0_open | prose 898 | **คงหน้าเดียว** — ย่อคำบรรยายการ์ดเหลือ ≤ 28 ตัวอักษร · ตัด `.sub` ให้สั้น · **แก้สถิติ `45` → `48` และ `แล็บละ 9 อัน` → `10 · 11 · 9 · 9 · 9`** |

| 10 | s2_require | prose 1134 · 8 แถว | **2 หน้า** — (ก) US-1…US-4 งานซ่อมตั้งแต่แจ้งจนปิด (ข) US-5…US-7 รายงาน · ยืม-คืน · อะไหล่ + ประโยคปิด "ทุกแถวมีคอลัมน์ขวาสุดเสมอ…" |
| 11 | s2_require | prose 1445 · 13 แถว · font 11.8 | **2 หน้า** — (ก) REQ-01…06 (ข) REQ-07…12 · ฟอนต์กลับไป ≥ 12px |
| 12 | s2_require | prose 855 | **2 หน้า** — (ก) การ์ด NFR-1…3 + เกณฑ์ผ่าน + พิสูจน์ที่แล็บไหน (ข) ตาราง "ลูกค้าพูดว่า → NFR → เทคนิค Docker" |
| 16 | s3_design | prose 924 · code 16 · font 11.8 | **2 หน้า** — (ก) schema 5 ตาราง (code ≤ 14 บรรทัด ย่อคอลัมน์รองด้วย `…`) (ข) ส่วนงาน→ตาราง→REQ + สถิติ seed 12·8·3·6 + SLA |
| 17 | s3_design | prose 805 | **2 หน้า** — (ก) แผนภาพ browser→web→api→db + "ผลที่ตามมา 3 อย่าง" (ข) ตารางตัวแปรสภาพแวดล้อม 4 แถว + "web เป็น server-rendered ทั้งหมด" |
| 24 | s4_lab1 | prose 1073 | **2 หน้า** — (ก) คำถามเดียว · ต้องผ่านอะไรมาก่อน · จบแล้วทำได้ · ยังไม่สอน (ข) สิ่งที่มักเข้าใจผิด 3 ข้อ + เตรียมเครื่องเรียน |
| 26 | s4_lab1 | prose 1429 · code 32 | **3 หน้า** — (ก) ไม่ใส่ `POSTGRES_PASSWORD` แล้วกล่องไม่ขึ้น (ข) คำสั่งที่ถูก + คอลัมน์ PORTS ไม่มี `0.0.0.0` = NFR-3 เริ่มที่บรรทัดนี้ (ค) `initdb.d` ส่ง schema+seed → 5 ตาราง · 12·8·3·6 คือสัญญาระหว่างแล็บ |
| 27 | s4_lab1 | prose 991 · code 28 · หนัก 2 | **คงหน้าเดียวแบบเปรียบเทียบ** — ใส่ `class="slide cmp"` แล้ว**ย่อ transcript สองฝั่งให้เหลือฝั่งละ ≤ 8 บรรทัด** (เก็บเฉพาะบรรทัดที่เป็นคำตอบ: `count 9` → `docker rm -f` → `count 8` เทียบกับฝั่งที่มี `-v ops-pgdata` แล้วได้แถว `9 | ไมโครโฟน…`) · prose ต้องลงมา ≤ 800 · การวางคู่กันคือหัวใจของสไลด์นี้ ห้ามแยกหน้า |
| 30 | s5_lab2 | prose 980 · code 24 · หนัก 2 | **2 หน้า** — (ก) `api/Dockerfile` (code ≤ 14 บรรทัด) + "6 คำสั่งสร้าง layer" (ข) ผล build `[1/6]`–`[6/6]` + context 28.54kB + คำสั่งที่ไม่สร้าง layer |
| 33 | s5_lab2 | prose 872 · code 28 | **2 หน้า** — (ก) `.dockerignore` ย่อเหลือ ≤ 14 บรรทัดโดยบอกว่าไฟล์จริง 17 บรรทัด (ข) log 5MB → context 66B · **เปลี่ยน `docker build … 2>&1 \| grep -A2 …` เป็น `docker build …` ธรรมดาแล้วชี้บรรทัดที่ต้องดู** |
| 36 | s5_lab2 | prose 1443 · code 33 · หนัก 3 | **2 หน้า** — (ก) REQ-01 `POST /api/tickets` → `HTTP 201` + body ย่อ (ข) REQ-02 `NEW → DONE` → `HTTP 409` + `INVALID_TRANSITION` · **ตัดบล็อก `curl … \| python3 -c '…'` ทิ้ง** เหลือ pointer ไป readme |
| 39 | s6_lab3 | prose 945 · code 23 · หนัก 2 | **2 หน้า** — (ก) `docker history` ไม่มีบรรทัด `npm` (ข) `node_modules` 12 vs 35 + ไม่มี `typescript` → คำตอบให้ลูกค้า |
| 41 | s6_lab3 | prose 821 · code 17 · หนัก 2 | **คงหน้าเดียวแบบเปรียบเทียบ** — `class="slide cmp"` ฝั่งละ ≤ 8 บรรทัด (exec form → PID 1 = `next-server` → `real 0m0.244s` เทียบ shell form → PID 1 = `/bin/sh -c` → `real 0m10.281s`) · prose ≤ 800 |
| 51 | s8_lab5 | prose 1237 · code 51 · หนัก 2 | **2 หน้า** — (ก) `db:` (volume + healthcheck + ไม่มี `ports:`) (ข) `api:` + `web:` (`ports:` บรรทัดเดียวทั้งไฟล์ + `depends_on: service_healthy`) · ทั้งสองหน้า code ≤ 14 บรรทัด ย่อส่วนที่ไม่ใช่ประเด็นด้วย `…` · กฎ `-p campusops` ย้ายไปสไลด์ปิดตอน |
| 52 | s8_lab5 | prose 1158 · code 27 | **2 หน้า** — (ก) `up -d --build` ลำดับ `Starting → Healthy` + `real 1m10.147s` (ข) `ps` คอลัมน์ PORTS + `GET` 4 หน้าได้ 200 + หมายเหตุ `start_period` |
| 54 | s8_lab5 | prose 972 · code 20 · font 11 · หนัก 3 | **2 หน้า** — (ก) `class="slide cmp"` วาง `down` เทียบ `down -v` ฝั่งละ ≤ 8 บรรทัด (`9` ใบยังอยู่ เทียบ `8` ใบที่เป็น seed) (ข) ตาราง "ของที่ compose สร้าง → `down` / `down -v`" ฟอนต์ ≥ 12px |
| 58 | s9_summary | prose 1380 · font 11.8 | **2 หน้า** — (ก) NFR-1 (สองแถว) + NFR-2 (ข) NFR-3 + REQ-01…12 + ประโยคปิด "คำถามเดียวของทั้งชุดถูกตอบที่ตารางนี้" |
| 59 | s9_summary | prose 1549 · 26 แถว · font 11 | **2 หน้า** — หน้าละ ≤ 13 แถว ฟอนต์ ≥ 12px · (ก) run/logs/exec/rm/-p/EXPOSE/volume/env/Dockerfile/cache/.dockerignore (ข) `-f`/history/multi-stage/ARG-ENV/CMD/inspect/DNS/network connect/compose/healthcheck/down/Hub + หมายเหตุข้อยกเว้น |
| 60 | s9_summary | prose 1258 | **2 หน้า** — (ก) "จบชุดนี้แล้วต้องทำให้เห็นได้ว่า…" 7 ข้อ (ข) สถิติ (**แก้ `45` → `48` · `แล็บละ 9 อัน` → `10 · 11 · 9 · 9 · 9`**) + `verify.sh` + คำถามเดียวปิดท้าย |

## สไลด์ใหม่ที่ต้องเพิ่ม (N1) — ปิดท้ายทุกตอนแล็บ ตอนละ 1 แผ่น

`s4_lab1` `s5_lab2` `s6_lab3` `s7_lab4` `s8_lab5` — หัวข้อ **"ตอนนี้ได้อะไร → ทำต่อที่แล็บไหน"**
เนื้อหาคงที่ 4 บล็อก: ทฤษฎีที่เพิ่งจบ 3 บรรทัด · REQ/NFR ที่ถูกพิสูจน์ · แล็บและจำนวนการทดลอง · คำสั่งเปิดแล็บ 1 บรรทัด

## ป้ายบน eyebrow (N2)

ทุกสไลด์ที่ไม่ใช่ `.cover` / `.section` ต้องขึ้นต้นด้วย `ตอนที่ N ·` และมีคำใดคำหนึ่งใน `ทฤษฎี` `ลงมือ` `หลักฐาน`
เช่น `ตอนที่ 5 · ทฤษฎี · layer cache` · `ตอนที่ 5 · ลงมือ · การทดลองที่ 3 · 4` · `ตอนที่ 5 · หลักฐาน · Swagger UI`

## ประมาณจำนวนหน้าใหม่

| part | เดิม | ใหม่ |
|---|---|---|
| s0_open | 3 | 3 |
| s1_customer | 5 | 5 |
| s2_require | 5 | 8 |
| s3_design | 8 | 10 |
| s4_lab1 | 7 | 11 |
| s5_lab2 | 8 | 13 |
| s6_lab3 | 7 | 9 |
| s7_lab4 | 5 | 6 |
| s8_lab5 | 8 | 12 |
| s9_summary | 4 | 7 |
| **รวม** | **60** | **~82** |

> **นับบรรทัดโค้ดแบบที่ gate ใช้ = บรรทัดที่ render จริง** บรรทัดยาวที่ตัดขึ้นบรรทัดใหม่ถูกนับด้วย
> ตัวเลขข้างบนอัปเดตแล้วตามวิธีนับนี้
