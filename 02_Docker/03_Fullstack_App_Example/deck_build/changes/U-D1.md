# U-D1 — บันทึกการตัด ย้าย และแตกสไลด์

ขอบเขต: หน้าเดิม 1–21 (`s0_open.html` ถึง `s3_design.html`) · ไม่เพิ่มสไลด์ปิดตอน · waiver 0

| หน้าเดิม | การเปลี่ยนแปลง | บล็อกที่ตัด/ย้าย/แตก | ปลายทางจริง / pointer ที่คงไว้ |
|---:|---|---|---|
| 2 | คง 1 หน้า | ย่อ `.sub` และคำบรรยายการ์ดทั้ง 10 ใบให้เหลือหัวใจของแต่ละตอน | การ์ดยังคง `data-go`; ตอน 1 → `s1_customer.html`, ตอน 2 → `s2_require.html`, ตอน 3 → `s3_design.html`, LAB 1–5 → `s4_lab1.html`…`s8_lab5.html`, สรุป → `s9_summary.html` |
| 2 | แก้ข้อเท็จจริง | `45 การทดลอง · แล็บละ 9 อัน` → `48 · 10 · 11 · 9 · 9 · 9`; เวลารวม → `4:15` | ตารางความจริงใน `deck_build/BRIEF_DECK.md` |
| 10 | แตก 2 หน้า | US-1…US-4 แยกจาก US-5…US-7; ไม่มีแถวใดถูกทิ้ง | ทั้งสองหน้าคง pointer `docs/01_requirements.md` · หัวข้อ 1 “แปลงคำบ่นเป็น User Story” |
| 10 | ย้ายประโยคปิด | “ทุกแถวมีคอลัมน์ขวาสุดเสมอ…” ย้ายไปหน้าครึ่งหลัง เพื่อปิดชุด US-5…US-7 | หน้าใหม่ US-5…US-7 และ `docs/01_requirements.md` |
| 11 | แตก 2 หน้า | REQ-01…06 แยกจาก REQ-07…12; คงข้อกำหนด เกณฑ์ผ่าน และคอลัมน์ที่มาเดิมครบ | ทั้งสองหน้าคง pointer `docs/01_requirements.md` · หัวข้อ 2 “ข้อกำหนดที่ทดสอบได้” |
| 11 | แก้การอ่าน | ตาราง `tbl xs` ถูกแยกเป็น `tbl sm`; ไม่ลดฟอนต์ | รายละเอียดเดิมอยู่ครบในสองหน้าใหม่ |
| 12 | แตก 2 หน้า | การ์ด NFR-1…3 แยกจากตาราง “ลูกค้าพูดว่า → NFR → เทคนิค Docker” | หน้าแรกชี้ `docs/01_requirements.md`; หน้าหลังชี้ `readme.md` |
| 16 | แตก 2 หน้า | schema 5 ตารางแยกจากตารางส่วนงาน, seed `12 · 8 · 3 · 6` และ SLA | ทั้งสองหน้าชี้ `docs/02_contract.md` · หัวข้อ 3 |
| 16 | ย่อ code | ใช้ `…` แทนคอลัมน์รอง: `created_at`, `DEFAULT 'NEW'`, `assignee`, `closed_at`, `returned_at`, `reorder_point` และรายละเอียดท้าย `stock_moves` | schema ครบอยู่ที่ `docs/02_contract.md` · หัวข้อ 3 และ `001_LAB_Run_The_System/db/initdb/01-schema.sql` |
| 16 | ย้ายคำอธิบาย | รายละเอียดว่า SLA ใช้คำนวณ `overdue` ของ REQ-09 และนิยามสถานะครุภัณฑ์ฉบับเต็มออกจากสไลด์ | `docs/02_contract.md` · หัวข้อ 3 |
| 17 | แตก 2 หน้า | flow `browser → web → api → db` + ผลที่ตามมา 3 ข้อ แยกจากตารางตัวแปร 4 แถว + server-rendered | หน้า flow ชี้ `docs/02_contract.md` หัวข้อ 1; หน้าตัวแปรชี้หัวข้อ 2 |
| 1–21 | ติดป้าย | eyebrow ของทุกสไลด์ปกติใน part U-D1 เปลี่ยนให้ขึ้นต้น `ตอนที่ N ·` และมี `ทฤษฎี` หรือ `หลักฐาน` | ปฏิบัติตาม N2; cover/section คงข้อยกเว้นเดิม |

สรุป: สไลด์ใน part U-D1 จาก 21 → 26 หน้า · ไม่มี waiver · ไม่มีข้อความย้ายเข้า SVG
